//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputSingleMedia extends TlObject {

    public static final int ID = 482797855;

    public TlObject media;
	public long random_id;
	public String message;
	public ArrayList<TlObject> entities;

    public InputSingleMedia(TlObject media, long random_id, String message, ArrayList<TlObject> entities) {
        this.media = media;
		this.random_id = random_id;
		this.message = message;
		this.entities = entities;
    }

    public static InputSingleMedia read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject media = TlObject.read(bytes);
		long random_id = LongPrimitive.read(bytes).getValue();
		String message = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> entities = (flags & (1 << 0)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		
		return new InputSingleMedia(media, random_id, message, entities);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.entities != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.media.write());
		bOut.write(new LongPrimitive(this.random_id).write());
		bOut.write(new StringPrimitive(this.message).write());
		if(this.entities != null)
			bOut.write(new VectorPrimitive(this.entities).write());
		
		return bOut.toByteArray();
    }
}
