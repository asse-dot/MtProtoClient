//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PremiumSubscriptionOption extends TlObject {

    public static final int ID = 1596792306;

    public boolean current;
	public boolean can_purchase_upgrade;
	public String transaction;
	public int months;
	public String currency;
	public long amount;
	public String bot_url;
	public String store_product;

    public PremiumSubscriptionOption(boolean current, boolean can_purchase_upgrade, String transaction, int months, String currency, long amount, String bot_url, String store_product) {
        this.current = current;
		this.can_purchase_upgrade = can_purchase_upgrade;
		this.transaction = transaction;
		this.months = months;
		this.currency = currency;
		this.amount = amount;
		this.bot_url = bot_url;
		this.store_product = store_product;
    }

    public static PremiumSubscriptionOption read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean current = (flags & (1 << 1)) != 0;
		boolean can_purchase_upgrade = (flags & (1 << 2)) != 0;
		String transaction = (flags & (1 << 3)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		int months = IntPrimitive.read(bytes).getValue();
		String currency = StringPrimitive.read(bytes).getValue();
		long amount = LongPrimitive.read(bytes).getValue();
		String bot_url = StringPrimitive.read(bytes).getValue();
		String store_product = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new PremiumSubscriptionOption(current, can_purchase_upgrade, transaction, months, currency, amount, bot_url, store_product);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.current ? (1 << 1) : 0;
		flags |= this.can_purchase_upgrade ? (1 << 2) : 0;
		flags |= (this.transaction != null) ? (1 << 3) : 0;
		flags |= (this.store_product != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.transaction != null)
			bOut.write(new StringPrimitive(this.transaction).write());
		bOut.write(new IntPrimitive(this.months).write());
		bOut.write(new StringPrimitive(this.currency).write());
		bOut.write(new LongPrimitive(this.amount).write());
		bOut.write(new StringPrimitive(this.bot_url).write());
		if(this.store_product != null)
			bOut.write(new StringPrimitive(this.store_product).write());
		
		return bOut.toByteArray();
    }
}
