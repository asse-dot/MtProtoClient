//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PhoneConnectionWebrtc extends TlObject {

    public static final int ID = 1667228533;

    public boolean turn;
	public boolean stun;
	public long id;
	public String ip;
	public String ipv6;
	public int port;
	public String username;
	public String password;

    public PhoneConnectionWebrtc(boolean turn, boolean stun, long id, String ip, String ipv6, int port, String username, String password) {
        this.turn = turn;
		this.stun = stun;
		this.id = id;
		this.ip = ip;
		this.ipv6 = ipv6;
		this.port = port;
		this.username = username;
		this.password = password;
    }

    public static PhoneConnectionWebrtc read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean turn = (flags & (1 << 0)) != 0;
		boolean stun = (flags & (1 << 1)) != 0;
		long id = LongPrimitive.read(bytes).getValue();
		String ip = StringPrimitive.read(bytes).getValue();
		String ipv6 = StringPrimitive.read(bytes).getValue();
		int port = IntPrimitive.read(bytes).getValue();
		String username = StringPrimitive.read(bytes).getValue();
		String password = StringPrimitive.read(bytes).getValue();
		
		return new PhoneConnectionWebrtc(turn, stun, id, ip, ipv6, port, username, password);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.turn ? (1 << 0) : 0;
		flags |= this.stun ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new StringPrimitive(this.ip).write());
		bOut.write(new StringPrimitive(this.ipv6).write());
		bOut.write(new IntPrimitive(this.port).write());
		bOut.write(new StringPrimitive(this.username).write());
		bOut.write(new StringPrimitive(this.password).write());
		
		return bOut.toByteArray();
    }
}
