//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateBotCallbackQuery extends TlObject {

    public static final int ID = -1177566067;

    public long query_id;
	public long user_id;
	public TlObject peer;
	public int msg_id;
	public long chat_instance;
	public byte[] data;
	public String game_short_name;

    public UpdateBotCallbackQuery(long query_id, long user_id, TlObject peer, int msg_id, long chat_instance, byte[] data, String game_short_name) {
        this.query_id = query_id;
		this.user_id = user_id;
		this.peer = peer;
		this.msg_id = msg_id;
		this.chat_instance = chat_instance;
		this.data = data;
		this.game_short_name = game_short_name;
    }

    public static UpdateBotCallbackQuery read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long query_id = LongPrimitive.read(bytes).getValue();
		long user_id = LongPrimitive.read(bytes).getValue();
		TlObject peer = TlObject.read(bytes);
		int msg_id = IntPrimitive.read(bytes).getValue();
		long chat_instance = LongPrimitive.read(bytes).getValue();
		byte[] data = (flags & (1 << 0)) != 0 ?  BytesPrimitive.read(bytes).getValue() : null;
		String game_short_name = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new UpdateBotCallbackQuery(query_id, user_id, peer, msg_id, chat_instance, data, game_short_name);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.data != null) ? (1 << 0) : 0;
		flags |= (this.game_short_name != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.query_id).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.msg_id).write());
		bOut.write(new LongPrimitive(this.chat_instance).write());
		if(this.data != null)
			bOut.write(new BytesPrimitive(this.data).write());
		if(this.game_short_name != null)
			bOut.write(new StringPrimitive(this.game_short_name).write());
		
		return bOut.toByteArray();
    }
}
