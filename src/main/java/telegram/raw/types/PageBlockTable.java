//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PageBlockTable extends TlObject {

    public static final int ID = -1085412734;

    public boolean bordered;
	public boolean striped;
	public TlObject title;
	public ArrayList<TlObject> rows;

    public PageBlockTable(boolean bordered, boolean striped, TlObject title, ArrayList<TlObject> rows) {
        this.bordered = bordered;
		this.striped = striped;
		this.title = title;
		this.rows = rows;
    }

    public static PageBlockTable read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean bordered = (flags & (1 << 0)) != 0;
		boolean striped = (flags & (1 << 1)) != 0;
		TlObject title = TlObject.read(bytes);
		ArrayList<TlObject> rows = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new PageBlockTable(bordered, striped, title, rows);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.bordered ? (1 << 0) : 0;
		flags |= this.striped ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.title.write());
		bOut.write(new VectorPrimitive(this.rows).write());
		
		return bOut.toByteArray();
    }
}
