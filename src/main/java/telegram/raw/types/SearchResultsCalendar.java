//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SearchResultsCalendar extends TlObject {

    public static final int ID = 343859772;

    public boolean inexact;
	public int count;
	public int min_date;
	public int min_msg_id;
	public int offset_id_offset;
	public ArrayList<TlObject> periods;
	public ArrayList<TlObject> messages;
	public ArrayList<TlObject> chats;
	public ArrayList<TlObject> users;

    public SearchResultsCalendar(boolean inexact, int count, int min_date, int min_msg_id, int offset_id_offset, ArrayList<TlObject> periods, ArrayList<TlObject> messages, ArrayList<TlObject> chats, ArrayList<TlObject> users) {
        this.inexact = inexact;
		this.count = count;
		this.min_date = min_date;
		this.min_msg_id = min_msg_id;
		this.offset_id_offset = offset_id_offset;
		this.periods = periods;
		this.messages = messages;
		this.chats = chats;
		this.users = users;
    }

    public static SearchResultsCalendar read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean inexact = (flags & (1 << 0)) != 0;
		int count = IntPrimitive.read(bytes).getValue();
		int min_date = IntPrimitive.read(bytes).getValue();
		int min_msg_id = IntPrimitive.read(bytes).getValue();
		int offset_id_offset = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		ArrayList<TlObject> periods = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> messages = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> chats = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new SearchResultsCalendar(inexact, count, min_date, min_msg_id, offset_id_offset, periods, messages, chats, users);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.inexact ? (1 << 0) : 0;
		flags |= (this.offset_id_offset != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.count).write());
		bOut.write(new IntPrimitive(this.min_date).write());
		bOut.write(new IntPrimitive(this.min_msg_id).write());
		if(this.offset_id_offset != 0)
			bOut.write(new IntPrimitive(this.offset_id_offset).write());
		bOut.write(new VectorPrimitive(this.periods).write());
		bOut.write(new VectorPrimitive(this.messages).write());
		bOut.write(new VectorPrimitive(this.chats).write());
		bOut.write(new VectorPrimitive(this.users).write());
		
		return bOut.toByteArray();
    }
}
