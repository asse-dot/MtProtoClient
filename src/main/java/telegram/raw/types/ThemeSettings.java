//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ThemeSettings extends TlObject {

    public static final int ID = -94849324;

    public boolean message_colors_animated;
	public TlObject base_theme;
	public int accent_color;
	public int outbox_accent_color;
	public ArrayList<TlObject> message_colors;
	public TlObject wallpaper;

    public ThemeSettings(boolean message_colors_animated, TlObject base_theme, int accent_color, int outbox_accent_color, ArrayList<TlObject> message_colors, TlObject wallpaper) {
        this.message_colors_animated = message_colors_animated;
		this.base_theme = base_theme;
		this.accent_color = accent_color;
		this.outbox_accent_color = outbox_accent_color;
		this.message_colors = message_colors;
		this.wallpaper = wallpaper;
    }

    public static ThemeSettings read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean message_colors_animated = (flags & (1 << 2)) != 0;
		TlObject base_theme = TlObject.read(bytes);
		int accent_color = IntPrimitive.read(bytes).getValue();
		int outbox_accent_color = (flags & (1 << 3)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		ArrayList<TlObject> message_colors = (flags & (1 << 0)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		TlObject wallpaper = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		
		return new ThemeSettings(message_colors_animated, base_theme, accent_color, outbox_accent_color, message_colors, wallpaper);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.message_colors_animated ? (1 << 2) : 0;
		flags |= (this.outbox_accent_color != 0) ? (1 << 3) : 0;
		flags |= (this.message_colors != null) ? (1 << 0) : 0;
		flags |= (this.wallpaper != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.base_theme.write());
		bOut.write(new IntPrimitive(this.accent_color).write());
		if(this.outbox_accent_color != 0)
			bOut.write(new IntPrimitive(this.outbox_accent_color).write());
		if(this.message_colors != null)
			bOut.write(new VectorPrimitive(this.message_colors).write());
		if(this.wallpaper != null)
			bOut.write(this.wallpaper.write());
		
		return bOut.toByteArray();
    }
}
