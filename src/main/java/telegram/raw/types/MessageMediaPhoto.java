//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageMediaPhoto extends TlObject {

    public static final int ID = 1766936791;

    public boolean spoiler;
	public TlObject photo;
	public int ttl_seconds;

    public MessageMediaPhoto(boolean spoiler, TlObject photo, int ttl_seconds) {
        this.spoiler = spoiler;
		this.photo = photo;
		this.ttl_seconds = ttl_seconds;
    }

    public static MessageMediaPhoto read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean spoiler = (flags & (1 << 3)) != 0;
		TlObject photo = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		int ttl_seconds = (flags & (1 << 2)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new MessageMediaPhoto(spoiler, photo, ttl_seconds);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.spoiler ? (1 << 3) : 0;
		flags |= (this.photo != null) ? (1 << 0) : 0;
		flags |= (this.ttl_seconds != 0) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.photo != null)
			bOut.write(this.photo.write());
		if(this.ttl_seconds != 0)
			bOut.write(new IntPrimitive(this.ttl_seconds).write());
		
		return bOut.toByteArray();
    }
}
