//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GroupCall extends TlObject {

    public static final int ID = -1636664659;

    public TlObject call;
	public ArrayList<TlObject> participants;
	public String participants_next_offset;
	public ArrayList<TlObject> chats;
	public ArrayList<TlObject> users;

    public GroupCall(TlObject call, ArrayList<TlObject> participants, String participants_next_offset, ArrayList<TlObject> chats, ArrayList<TlObject> users) {
        this.call = call;
		this.participants = participants;
		this.participants_next_offset = participants_next_offset;
		this.chats = chats;
		this.users = users;
    }

    public static GroupCall read(ByteArrayInputStream bytes) throws IOException {
        TlObject call = TlObject.read(bytes);
		ArrayList<TlObject> participants = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		String participants_next_offset = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> chats = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new GroupCall(call, participants, participants_next_offset, chats, users);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.call.write());
		bOut.write(new VectorPrimitive(this.participants).write());
		bOut.write(new StringPrimitive(this.participants_next_offset).write());
		bOut.write(new VectorPrimitive(this.chats).write());
		bOut.write(new VectorPrimitive(this.users).write());
		
		return bOut.toByteArray();
    }
}
