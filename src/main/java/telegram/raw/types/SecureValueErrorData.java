//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SecureValueErrorData extends TlObject {

    public static final int ID = -391902247;

    public TlObject type;
	public byte[] data_hash;
	public String field;
	public String text;

    public SecureValueErrorData(TlObject type, byte[] data_hash, String field, String text) {
        this.type = type;
		this.data_hash = data_hash;
		this.field = field;
		this.text = text;
    }

    public static SecureValueErrorData read(ByteArrayInputStream bytes) throws IOException {
        TlObject type = TlObject.read(bytes);
		byte[] data_hash = BytesPrimitive.read(bytes).getValue();
		String field = StringPrimitive.read(bytes).getValue();
		String text = StringPrimitive.read(bytes).getValue();
		
		return new SecureValueErrorData(type, data_hash, field, text);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.type.write());
		bOut.write(new BytesPrimitive(this.data_hash).write());
		bOut.write(new StringPrimitive(this.field).write());
		bOut.write(new StringPrimitive(this.text).write());
		
		return bOut.toByteArray();
    }
}
