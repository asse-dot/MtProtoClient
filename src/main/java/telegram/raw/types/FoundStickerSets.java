//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class FoundStickerSets extends TlObject {

    public static final int ID = -1963942446;

    public long hash;
	public ArrayList<TlObject> sets;

    public FoundStickerSets(long hash, ArrayList<TlObject> sets) {
        this.hash = hash;
		this.sets = sets;
    }

    public static FoundStickerSets read(ByteArrayInputStream bytes) throws IOException {
        long hash = LongPrimitive.read(bytes).getValue();
		ArrayList<TlObject> sets = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new FoundStickerSets(hash, sets);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.hash).write());
		bOut.write(new VectorPrimitive(this.sets).write());
		
		return bOut.toByteArray();
    }
}
