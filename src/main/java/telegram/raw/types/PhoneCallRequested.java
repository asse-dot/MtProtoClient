//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PhoneCallRequested extends TlObject {

    public static final int ID = 347139340;

    public boolean video;
	public long id;
	public long access_hash;
	public int date;
	public long admin_id;
	public long participant_id;
	public byte[] g_a_hash;
	public TlObject protocol;

    public PhoneCallRequested(boolean video, long id, long access_hash, int date, long admin_id, long participant_id, byte[] g_a_hash, TlObject protocol) {
        this.video = video;
		this.id = id;
		this.access_hash = access_hash;
		this.date = date;
		this.admin_id = admin_id;
		this.participant_id = participant_id;
		this.g_a_hash = g_a_hash;
		this.protocol = protocol;
    }

    public static PhoneCallRequested read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean video = (flags & (1 << 6)) != 0;
		long id = LongPrimitive.read(bytes).getValue();
		long access_hash = LongPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		long admin_id = LongPrimitive.read(bytes).getValue();
		long participant_id = LongPrimitive.read(bytes).getValue();
		byte[] g_a_hash = BytesPrimitive.read(bytes).getValue();
		TlObject protocol = TlObject.read(bytes);
		
		return new PhoneCallRequested(video, id, access_hash, date, admin_id, participant_id, g_a_hash, protocol);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.video ? (1 << 6) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new LongPrimitive(this.admin_id).write());
		bOut.write(new LongPrimitive(this.participant_id).write());
		bOut.write(new BytesPrimitive(this.g_a_hash).write());
		bOut.write(this.protocol.write());
		
		return bOut.toByteArray();
    }
}
