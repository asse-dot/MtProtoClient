//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputPhoneContact extends TlObject {

    public static final int ID = -208488460;

    public long client_id;
	public String phone;
	public String first_name;
	public String last_name;

    public InputPhoneContact(long client_id, String phone, String first_name, String last_name) {
        this.client_id = client_id;
		this.phone = phone;
		this.first_name = first_name;
		this.last_name = last_name;
    }

    public static InputPhoneContact read(ByteArrayInputStream bytes) throws IOException {
        long client_id = LongPrimitive.read(bytes).getValue();
		String phone = StringPrimitive.read(bytes).getValue();
		String first_name = StringPrimitive.read(bytes).getValue();
		String last_name = StringPrimitive.read(bytes).getValue();
		
		return new InputPhoneContact(client_id, phone, first_name, last_name);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.client_id).write());
		bOut.write(new StringPrimitive(this.phone).write());
		bOut.write(new StringPrimitive(this.first_name).write());
		bOut.write(new StringPrimitive(this.last_name).write());
		
		return bOut.toByteArray();
    }
}
