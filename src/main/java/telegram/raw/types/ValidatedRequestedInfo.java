//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.payments;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ValidatedRequestedInfo extends TlObject {

    public static final int ID = -784000893;

    public String id;
	public ArrayList<TlObject> shipping_options;

    public ValidatedRequestedInfo(String id, ArrayList<TlObject> shipping_options) {
        this.id = id;
		this.shipping_options = shipping_options;
    }

    public static ValidatedRequestedInfo read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String id = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		ArrayList<TlObject> shipping_options = (flags & (1 << 1)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		
		return new ValidatedRequestedInfo(id, shipping_options);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.id != null) ? (1 << 0) : 0;
		flags |= (this.shipping_options != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.id != null)
			bOut.write(new StringPrimitive(this.id).write());
		if(this.shipping_options != null)
			bOut.write(new VectorPrimitive(this.shipping_options).write());
		
		return bOut.toByteArray();
    }
}
