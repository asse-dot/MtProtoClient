//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SearchResultsCalendarPeriod extends TlObject {

    public static final int ID = -911191137;

    public int date;
	public int min_msg_id;
	public int max_msg_id;
	public int count;

    public SearchResultsCalendarPeriod(int date, int min_msg_id, int max_msg_id, int count) {
        this.date = date;
		this.min_msg_id = min_msg_id;
		this.max_msg_id = max_msg_id;
		this.count = count;
    }

    public static SearchResultsCalendarPeriod read(ByteArrayInputStream bytes) throws IOException {
        int date = IntPrimitive.read(bytes).getValue();
		int min_msg_id = IntPrimitive.read(bytes).getValue();
		int max_msg_id = IntPrimitive.read(bytes).getValue();
		int count = IntPrimitive.read(bytes).getValue();
		
		return new SearchResultsCalendarPeriod(date, min_msg_id, max_msg_id, count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new IntPrimitive(this.min_msg_id).write());
		bOut.write(new IntPrimitive(this.max_msg_id).write());
		bOut.write(new IntPrimitive(this.count).write());
		
		return bOut.toByteArray();
    }
}
