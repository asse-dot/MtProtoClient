//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputChatUploadedPhoto extends TlObject {

    public static final int ID = -1110593856;

    public TlObject file;
	public TlObject video;
	public TlObject video_start_ts;
	public TlObject video_emoji_markup;

    public InputChatUploadedPhoto(TlObject file, TlObject video, TlObject video_start_ts, TlObject video_emoji_markup) {
        this.file = file;
		this.video = video;
		this.video_start_ts = video_start_ts;
		this.video_emoji_markup = video_emoji_markup;
    }

    public static InputChatUploadedPhoto read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject file = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		TlObject video = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		TlObject video_start_ts = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		TlObject video_emoji_markup = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		
		return new InputChatUploadedPhoto(file, video, video_start_ts, video_emoji_markup);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.file != null) ? (1 << 0) : 0;
		flags |= (this.video != null) ? (1 << 1) : 0;
		flags |= (this.video_start_ts != null) ? (1 << 2) : 0;
		flags |= (this.video_emoji_markup != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.file != null)
			bOut.write(this.file.write());
		if(this.video != null)
			bOut.write(this.video.write());
		if(this.video_start_ts != null)
			bOut.write(this.video_start_ts.write());
		if(this.video_emoji_markup != null)
			bOut.write(this.video_emoji_markup.write());
		
		return bOut.toByteArray();
    }
}
