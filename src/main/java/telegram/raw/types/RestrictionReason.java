//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RestrictionReason extends TlObject {

    public static final int ID = -797791052;

    public String platform;
	public String reason;
	public String text;

    public RestrictionReason(String platform, String reason, String text) {
        this.platform = platform;
		this.reason = reason;
		this.text = text;
    }

    public static RestrictionReason read(ByteArrayInputStream bytes) throws IOException {
        String platform = StringPrimitive.read(bytes).getValue();
		String reason = StringPrimitive.read(bytes).getValue();
		String text = StringPrimitive.read(bytes).getValue();
		
		return new RestrictionReason(platform, reason, text);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.platform).write());
		bOut.write(new StringPrimitive(this.reason).write());
		bOut.write(new StringPrimitive(this.text).write());
		
		return bOut.toByteArray();
    }
}
