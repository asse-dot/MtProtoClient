//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateChatParticipant extends TlObject {

    public static final int ID = -796432838;

    public long chat_id;
	public int date;
	public long actor_id;
	public long user_id;
	public TlObject prev_participant;
	public TlObject new_participant;
	public TlObject invite;
	public int qts;

    public UpdateChatParticipant(long chat_id, int date, long actor_id, long user_id, TlObject prev_participant, TlObject new_participant, TlObject invite, int qts) {
        this.chat_id = chat_id;
		this.date = date;
		this.actor_id = actor_id;
		this.user_id = user_id;
		this.prev_participant = prev_participant;
		this.new_participant = new_participant;
		this.invite = invite;
		this.qts = qts;
    }

    public static UpdateChatParticipant read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long chat_id = LongPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		long actor_id = LongPrimitive.read(bytes).getValue();
		long user_id = LongPrimitive.read(bytes).getValue();
		TlObject prev_participant = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		TlObject new_participant = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		TlObject invite = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		int qts = IntPrimitive.read(bytes).getValue();
		
		return new UpdateChatParticipant(chat_id, date, actor_id, user_id, prev_participant, new_participant, invite, qts);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.prev_participant != null) ? (1 << 0) : 0;
		flags |= (this.new_participant != null) ? (1 << 1) : 0;
		flags |= (this.invite != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.chat_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new LongPrimitive(this.actor_id).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		if(this.prev_participant != null)
			bOut.write(this.prev_participant.write());
		if(this.new_participant != null)
			bOut.write(this.new_participant.write());
		if(this.invite != null)
			bOut.write(this.invite.write());
		bOut.write(new IntPrimitive(this.qts).write());
		
		return bOut.toByteArray();
    }
}
