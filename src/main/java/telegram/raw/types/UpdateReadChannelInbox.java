//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateReadChannelInbox extends TlObject {

    public static final int ID = -1842450928;

    public int folder_id;
	public long channel_id;
	public int max_id;
	public int still_unread_count;
	public int pts;

    public UpdateReadChannelInbox(int folder_id, long channel_id, int max_id, int still_unread_count, int pts) {
        this.folder_id = folder_id;
		this.channel_id = channel_id;
		this.max_id = max_id;
		this.still_unread_count = still_unread_count;
		this.pts = pts;
    }

    public static UpdateReadChannelInbox read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		int folder_id = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		long channel_id = LongPrimitive.read(bytes).getValue();
		int max_id = IntPrimitive.read(bytes).getValue();
		int still_unread_count = IntPrimitive.read(bytes).getValue();
		int pts = IntPrimitive.read(bytes).getValue();
		
		return new UpdateReadChannelInbox(folder_id, channel_id, max_id, still_unread_count, pts);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.folder_id != 0) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.folder_id != 0)
			bOut.write(new IntPrimitive(this.folder_id).write());
		bOut.write(new LongPrimitive(this.channel_id).write());
		bOut.write(new IntPrimitive(this.max_id).write());
		bOut.write(new IntPrimitive(this.still_unread_count).write());
		bOut.write(new IntPrimitive(this.pts).write());
		
		return bOut.toByteArray();
    }
}
