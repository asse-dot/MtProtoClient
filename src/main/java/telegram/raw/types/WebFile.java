//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.upload;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class WebFile extends TlObject {

    public static final int ID = 568808380;

    public int size;
	public String mime_type;
	public TlObject file_type;
	public int mtime;
	public byte[] bytesv;

    public WebFile(int size, String mime_type, TlObject file_type, int mtime, byte[] bytesv) {
        this.size = size;
		this.mime_type = mime_type;
		this.file_type = file_type;
		this.mtime = mtime;
		this.bytesv = bytesv;
    }

    public static WebFile read(ByteArrayInputStream bytes) throws IOException {
        int size = IntPrimitive.read(bytes).getValue();
		String mime_type = StringPrimitive.read(bytes).getValue();
		TlObject file_type = TlObject.read(bytes);
		int mtime = IntPrimitive.read(bytes).getValue();
		byte[] bytesv = BytesPrimitive.read(bytes).getValue();
		
		return new WebFile(size, mime_type, file_type, mtime, bytesv);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.size).write());
		bOut.write(new StringPrimitive(this.mime_type).write());
		bOut.write(this.file_type.write());
		bOut.write(new IntPrimitive(this.mtime).write());
		bOut.write(new BytesPrimitive(this.bytesv).write());
		
		return bOut.toByteArray();
    }
}
