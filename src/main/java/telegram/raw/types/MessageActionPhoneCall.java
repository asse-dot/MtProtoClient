//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageActionPhoneCall extends TlObject {

    public static final int ID = -2132731265;

    public boolean video;
	public long call_id;
	public TlObject reason;
	public int duration;

    public MessageActionPhoneCall(boolean video, long call_id, TlObject reason, int duration) {
        this.video = video;
		this.call_id = call_id;
		this.reason = reason;
		this.duration = duration;
    }

    public static MessageActionPhoneCall read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean video = (flags & (1 << 2)) != 0;
		long call_id = LongPrimitive.read(bytes).getValue();
		TlObject reason = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		int duration = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new MessageActionPhoneCall(video, call_id, reason, duration);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.video ? (1 << 2) : 0;
		flags |= (this.reason != null) ? (1 << 0) : 0;
		flags |= (this.duration != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.call_id).write());
		if(this.reason != null)
			bOut.write(this.reason.write());
		if(this.duration != 0)
			bOut.write(new IntPrimitive(this.duration).write());
		
		return bOut.toByteArray();
    }
}
