//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.help;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PromoData extends TlObject {

    public static final int ID = -1942390465;

    public boolean proxy;
	public int expires;
	public TlObject peer;
	public ArrayList<TlObject> chats;
	public ArrayList<TlObject> users;
	public String psa_type;
	public String psa_message;

    public PromoData(boolean proxy, int expires, TlObject peer, ArrayList<TlObject> chats, ArrayList<TlObject> users, String psa_type, String psa_message) {
        this.proxy = proxy;
		this.expires = expires;
		this.peer = peer;
		this.chats = chats;
		this.users = users;
		this.psa_type = psa_type;
		this.psa_message = psa_message;
    }

    public static PromoData read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean proxy = (flags & (1 << 0)) != 0;
		int expires = IntPrimitive.read(bytes).getValue();
		TlObject peer = TlObject.read(bytes);
		ArrayList<TlObject> chats = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		String psa_type = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String psa_message = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new PromoData(proxy, expires, peer, chats, users, psa_type, psa_message);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.proxy ? (1 << 0) : 0;
		flags |= (this.psa_type != null) ? (1 << 1) : 0;
		flags |= (this.psa_message != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.expires).write());
		bOut.write(this.peer.write());
		bOut.write(new VectorPrimitive(this.chats).write());
		bOut.write(new VectorPrimitive(this.users).write());
		if(this.psa_type != null)
			bOut.write(new StringPrimitive(this.psa_type).write());
		if(this.psa_message != null)
			bOut.write(new StringPrimitive(this.psa_message).write());
		
		return bOut.toByteArray();
    }
}
