//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class StickerKeyword extends TlObject {

    public static final int ID = -50416996;

    public long document_id;
	public ArrayList<TlObject> keyword;

    public StickerKeyword(long document_id, ArrayList<TlObject> keyword) {
        this.document_id = document_id;
		this.keyword = keyword;
    }

    public static StickerKeyword read(ByteArrayInputStream bytes) throws IOException {
        long document_id = LongPrimitive.read(bytes).getValue();
		ArrayList<TlObject> keyword = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new StickerKeyword(document_id, keyword);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.document_id).write());
		bOut.write(new VectorPrimitive(this.keyword).write());
		
		return bOut.toByteArray();
    }
}
