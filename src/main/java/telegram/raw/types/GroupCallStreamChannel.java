//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GroupCallStreamChannel extends TlObject {

    public static final int ID = -2132064081;

    public int channel;
	public int scale;
	public long last_timestamp_ms;

    public GroupCallStreamChannel(int channel, int scale, long last_timestamp_ms) {
        this.channel = channel;
		this.scale = scale;
		this.last_timestamp_ms = last_timestamp_ms;
    }

    public static GroupCallStreamChannel read(ByteArrayInputStream bytes) throws IOException {
        int channel = IntPrimitive.read(bytes).getValue();
		int scale = IntPrimitive.read(bytes).getValue();
		long last_timestamp_ms = LongPrimitive.read(bytes).getValue();
		
		return new GroupCallStreamChannel(channel, scale, last_timestamp_ms);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.channel).write());
		bOut.write(new IntPrimitive(this.scale).write());
		bOut.write(new LongPrimitive(this.last_timestamp_ms).write());
		
		return bOut.toByteArray();
    }
}
