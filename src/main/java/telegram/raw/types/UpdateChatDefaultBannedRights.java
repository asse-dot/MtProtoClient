//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateChatDefaultBannedRights extends TlObject {

    public static final int ID = 1421875280;

    public TlObject peer;
	public TlObject default_banned_rights;
	public int version;

    public UpdateChatDefaultBannedRights(TlObject peer, TlObject default_banned_rights, int version) {
        this.peer = peer;
		this.default_banned_rights = default_banned_rights;
		this.version = version;
    }

    public static UpdateChatDefaultBannedRights read(ByteArrayInputStream bytes) throws IOException {
        TlObject peer = TlObject.read(bytes);
		TlObject default_banned_rights = TlObject.read(bytes);
		int version = IntPrimitive.read(bytes).getValue();
		
		return new UpdateChatDefaultBannedRights(peer, default_banned_rights, version);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.peer.write());
		bOut.write(this.default_banned_rights.write());
		bOut.write(new IntPrimitive(this.version).write());
		
		return bOut.toByteArray();
    }
}
