//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ReplyKeyboardForceReply extends TlObject {

    public static final int ID = -2035021048;

    public boolean single_use;
	public boolean selective;
	public String placeholder;

    public ReplyKeyboardForceReply(boolean single_use, boolean selective, String placeholder) {
        this.single_use = single_use;
		this.selective = selective;
		this.placeholder = placeholder;
    }

    public static ReplyKeyboardForceReply read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean single_use = (flags & (1 << 1)) != 0;
		boolean selective = (flags & (1 << 2)) != 0;
		String placeholder = (flags & (1 << 3)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new ReplyKeyboardForceReply(single_use, selective, placeholder);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.single_use ? (1 << 1) : 0;
		flags |= this.selective ? (1 << 2) : 0;
		flags |= (this.placeholder != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.placeholder != null)
			bOut.write(new StringPrimitive(this.placeholder).write());
		
		return bOut.toByteArray();
    }
}
