//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ContentSettings extends TlObject {

    public static final int ID = 1474462241;

    public boolean sensitive_enabled;
	public boolean sensitive_can_change;

    public ContentSettings(boolean sensitive_enabled, boolean sensitive_can_change) {
        this.sensitive_enabled = sensitive_enabled;
		this.sensitive_can_change = sensitive_can_change;
    }

    public static ContentSettings read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean sensitive_enabled = (flags & (1 << 0)) != 0;
		boolean sensitive_can_change = (flags & (1 << 1)) != 0;
		
		return new ContentSettings(sensitive_enabled, sensitive_can_change);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.sensitive_enabled ? (1 << 0) : 0;
		flags |= this.sensitive_can_change ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		
		return bOut.toByteArray();
    }
}
