//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputPhotoLegacyFileLocation extends TlObject {

    public static final int ID = -667654413;

    public long id;
	public long access_hash;
	public byte[] file_reference;
	public long volume_id;
	public int local_id;
	public long secret;

    public InputPhotoLegacyFileLocation(long id, long access_hash, byte[] file_reference, long volume_id, int local_id, long secret) {
        this.id = id;
		this.access_hash = access_hash;
		this.file_reference = file_reference;
		this.volume_id = volume_id;
		this.local_id = local_id;
		this.secret = secret;
    }

    public static InputPhotoLegacyFileLocation read(ByteArrayInputStream bytes) throws IOException {
        long id = LongPrimitive.read(bytes).getValue();
		long access_hash = LongPrimitive.read(bytes).getValue();
		byte[] file_reference = BytesPrimitive.read(bytes).getValue();
		long volume_id = LongPrimitive.read(bytes).getValue();
		int local_id = IntPrimitive.read(bytes).getValue();
		long secret = LongPrimitive.read(bytes).getValue();
		
		return new InputPhotoLegacyFileLocation(id, access_hash, file_reference, volume_id, local_id, secret);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new BytesPrimitive(this.file_reference).write());
		bOut.write(new LongPrimitive(this.volume_id).write());
		bOut.write(new IntPrimitive(this.local_id).write());
		bOut.write(new LongPrimitive(this.secret).write());
		
		return bOut.toByteArray();
    }
}
