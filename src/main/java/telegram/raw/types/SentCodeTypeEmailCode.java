//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.auth;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SentCodeTypeEmailCode extends TlObject {

    public static final int ID = -196020837;

    public boolean apple_signin_allowed;
	public boolean google_signin_allowed;
	public String email_pattern;
	public int length;
	public int reset_available_period;
	public int reset_pending_date;

    public SentCodeTypeEmailCode(boolean apple_signin_allowed, boolean google_signin_allowed, String email_pattern, int length, int reset_available_period, int reset_pending_date) {
        this.apple_signin_allowed = apple_signin_allowed;
		this.google_signin_allowed = google_signin_allowed;
		this.email_pattern = email_pattern;
		this.length = length;
		this.reset_available_period = reset_available_period;
		this.reset_pending_date = reset_pending_date;
    }

    public static SentCodeTypeEmailCode read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean apple_signin_allowed = (flags & (1 << 0)) != 0;
		boolean google_signin_allowed = (flags & (1 << 1)) != 0;
		String email_pattern = StringPrimitive.read(bytes).getValue();
		int length = IntPrimitive.read(bytes).getValue();
		int reset_available_period = (flags & (1 << 3)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int reset_pending_date = (flags & (1 << 4)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new SentCodeTypeEmailCode(apple_signin_allowed, google_signin_allowed, email_pattern, length, reset_available_period, reset_pending_date);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.apple_signin_allowed ? (1 << 0) : 0;
		flags |= this.google_signin_allowed ? (1 << 1) : 0;
		flags |= (this.reset_available_period != 0) ? (1 << 3) : 0;
		flags |= (this.reset_pending_date != 0) ? (1 << 4) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.email_pattern).write());
		bOut.write(new IntPrimitive(this.length).write());
		if(this.reset_available_period != 0)
			bOut.write(new IntPrimitive(this.reset_available_period).write());
		if(this.reset_pending_date != 0)
			bOut.write(new IntPrimitive(this.reset_pending_date).write());
		
		return bOut.toByteArray();
    }
}
