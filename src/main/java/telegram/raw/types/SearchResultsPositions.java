//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SearchResultsPositions extends TlObject {

    public static final int ID = 1404185519;

    public int count;
	public ArrayList<TlObject> positions;

    public SearchResultsPositions(int count, ArrayList<TlObject> positions) {
        this.count = count;
		this.positions = positions;
    }

    public static SearchResultsPositions read(ByteArrayInputStream bytes) throws IOException {
        int count = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> positions = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new SearchResultsPositions(count, positions);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.count).write());
		bOut.write(new VectorPrimitive(this.positions).write());
		
		return bOut.toByteArray();
    }
}
