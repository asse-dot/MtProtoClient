//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DialogFilterChatlist extends TlObject {

    public static final int ID = -699792216;

    public boolean has_my_invites;
	public int id;
	public String title;
	public String emoticon;
	public ArrayList<TlObject> pinned_peers;
	public ArrayList<TlObject> include_peers;

    public DialogFilterChatlist(boolean has_my_invites, int id, String title, String emoticon, ArrayList<TlObject> pinned_peers, ArrayList<TlObject> include_peers) {
        this.has_my_invites = has_my_invites;
		this.id = id;
		this.title = title;
		this.emoticon = emoticon;
		this.pinned_peers = pinned_peers;
		this.include_peers = include_peers;
    }

    public static DialogFilterChatlist read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean has_my_invites = (flags & (1 << 26)) != 0;
		int id = IntPrimitive.read(bytes).getValue();
		String title = StringPrimitive.read(bytes).getValue();
		String emoticon = (flags & (1 << 25)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		ArrayList<TlObject> pinned_peers = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> include_peers = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new DialogFilterChatlist(has_my_invites, id, title, emoticon, pinned_peers, include_peers);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.has_my_invites ? (1 << 26) : 0;
		flags |= (this.emoticon != null) ? (1 << 25) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.id).write());
		bOut.write(new StringPrimitive(this.title).write());
		if(this.emoticon != null)
			bOut.write(new StringPrimitive(this.emoticon).write());
		bOut.write(new VectorPrimitive(this.pinned_peers).write());
		bOut.write(new VectorPrimitive(this.include_peers).write());
		
		return bOut.toByteArray();
    }
}
