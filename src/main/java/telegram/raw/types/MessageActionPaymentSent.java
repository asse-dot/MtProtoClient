//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageActionPaymentSent extends TlObject {

    public static final int ID = -1776926890;

    public boolean recurring_init;
	public boolean recurring_used;
	public String currency;
	public long total_amount;
	public String invoice_slug;

    public MessageActionPaymentSent(boolean recurring_init, boolean recurring_used, String currency, long total_amount, String invoice_slug) {
        this.recurring_init = recurring_init;
		this.recurring_used = recurring_used;
		this.currency = currency;
		this.total_amount = total_amount;
		this.invoice_slug = invoice_slug;
    }

    public static MessageActionPaymentSent read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean recurring_init = (flags & (1 << 2)) != 0;
		boolean recurring_used = (flags & (1 << 3)) != 0;
		String currency = StringPrimitive.read(bytes).getValue();
		long total_amount = LongPrimitive.read(bytes).getValue();
		String invoice_slug = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new MessageActionPaymentSent(recurring_init, recurring_used, currency, total_amount, invoice_slug);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.recurring_init ? (1 << 2) : 0;
		flags |= this.recurring_used ? (1 << 3) : 0;
		flags |= (this.invoice_slug != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.currency).write());
		bOut.write(new LongPrimitive(this.total_amount).write());
		if(this.invoice_slug != null)
			bOut.write(new StringPrimitive(this.invoice_slug).write());
		
		return bOut.toByteArray();
    }
}
