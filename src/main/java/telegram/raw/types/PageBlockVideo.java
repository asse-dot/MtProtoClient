//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PageBlockVideo extends TlObject {

    public static final int ID = 2089805750;

    public boolean autoplay;
	public boolean loop;
	public long video_id;
	public TlObject caption;

    public PageBlockVideo(boolean autoplay, boolean loop, long video_id, TlObject caption) {
        this.autoplay = autoplay;
		this.loop = loop;
		this.video_id = video_id;
		this.caption = caption;
    }

    public static PageBlockVideo read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean autoplay = (flags & (1 << 0)) != 0;
		boolean loop = (flags & (1 << 1)) != 0;
		long video_id = LongPrimitive.read(bytes).getValue();
		TlObject caption = TlObject.read(bytes);
		
		return new PageBlockVideo(autoplay, loop, video_id, caption);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.autoplay ? (1 << 0) : 0;
		flags |= this.loop ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.video_id).write());
		bOut.write(this.caption.write());
		
		return bOut.toByteArray();
    }
}
