//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class TextUrl extends TlObject {

    public static final int ID = 1009288385;

    public TlObject text;
	public String url;
	public long webpage_id;

    public TextUrl(TlObject text, String url, long webpage_id) {
        this.text = text;
		this.url = url;
		this.webpage_id = webpage_id;
    }

    public static TextUrl read(ByteArrayInputStream bytes) throws IOException {
        TlObject text = TlObject.read(bytes);
		String url = StringPrimitive.read(bytes).getValue();
		long webpage_id = LongPrimitive.read(bytes).getValue();
		
		return new TextUrl(text, url, webpage_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.text.write());
		bOut.write(new StringPrimitive(this.url).write());
		bOut.write(new LongPrimitive(this.webpage_id).write());
		
		return bOut.toByteArray();
    }
}
