//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageActionGameScore extends TlObject {

    public static final int ID = -1834538890;

    public long game_id;
	public int score;

    public MessageActionGameScore(long game_id, int score) {
        this.game_id = game_id;
		this.score = score;
    }

    public static MessageActionGameScore read(ByteArrayInputStream bytes) throws IOException {
        long game_id = LongPrimitive.read(bytes).getValue();
		int score = IntPrimitive.read(bytes).getValue();
		
		return new MessageActionGameScore(game_id, score);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.game_id).write());
		bOut.write(new IntPrimitive(this.score).write());
		
		return bOut.toByteArray();
    }
}
