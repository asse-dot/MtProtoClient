//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.contacts;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class Contacts extends TlObject {

    public static final int ID = -353862078;

    public ArrayList<TlObject> contacts;
	public int saved_count;
	public ArrayList<TlObject> users;

    public Contacts(ArrayList<TlObject> contacts, int saved_count, ArrayList<TlObject> users) {
        this.contacts = contacts;
		this.saved_count = saved_count;
		this.users = users;
    }

    public static Contacts read(ByteArrayInputStream bytes) throws IOException {
        ArrayList<TlObject> contacts = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		int saved_count = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new Contacts(contacts, saved_count, users);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new VectorPrimitive(this.contacts).write());
		bOut.write(new IntPrimitive(this.saved_count).write());
		bOut.write(new VectorPrimitive(this.users).write());
		
		return bOut.toByteArray();
    }
}
