//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class BotInlineMessageMediaInvoice extends TlObject {

    public static final int ID = 894081801;

    public boolean shipping_address_requested;
	public boolean test;
	public String title;
	public String description;
	public TlObject photo;
	public String currency;
	public long total_amount;
	public TlObject reply_markup;

    public BotInlineMessageMediaInvoice(boolean shipping_address_requested, boolean test, String title, String description, TlObject photo, String currency, long total_amount, TlObject reply_markup) {
        this.shipping_address_requested = shipping_address_requested;
		this.test = test;
		this.title = title;
		this.description = description;
		this.photo = photo;
		this.currency = currency;
		this.total_amount = total_amount;
		this.reply_markup = reply_markup;
    }

    public static BotInlineMessageMediaInvoice read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean shipping_address_requested = (flags & (1 << 1)) != 0;
		boolean test = (flags & (1 << 3)) != 0;
		String title = StringPrimitive.read(bytes).getValue();
		String description = StringPrimitive.read(bytes).getValue();
		TlObject photo = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		String currency = StringPrimitive.read(bytes).getValue();
		long total_amount = LongPrimitive.read(bytes).getValue();
		TlObject reply_markup = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new BotInlineMessageMediaInvoice(shipping_address_requested, test, title, description, photo, currency, total_amount, reply_markup);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.shipping_address_requested ? (1 << 1) : 0;
		flags |= this.test ? (1 << 3) : 0;
		flags |= (this.photo != null) ? (1 << 0) : 0;
		flags |= (this.reply_markup != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.title).write());
		bOut.write(new StringPrimitive(this.description).write());
		if(this.photo != null)
			bOut.write(this.photo.write());
		bOut.write(new StringPrimitive(this.currency).write());
		bOut.write(new LongPrimitive(this.total_amount).write());
		if(this.reply_markup != null)
			bOut.write(this.reply_markup.write());
		
		return bOut.toByteArray();
    }
}
