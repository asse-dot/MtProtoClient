//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChatAdminWithInvites extends TlObject {

    public static final int ID = -219353309;

    public long admin_id;
	public int invites_count;
	public int revoked_invites_count;

    public ChatAdminWithInvites(long admin_id, int invites_count, int revoked_invites_count) {
        this.admin_id = admin_id;
		this.invites_count = invites_count;
		this.revoked_invites_count = revoked_invites_count;
    }

    public static ChatAdminWithInvites read(ByteArrayInputStream bytes) throws IOException {
        long admin_id = LongPrimitive.read(bytes).getValue();
		int invites_count = IntPrimitive.read(bytes).getValue();
		int revoked_invites_count = IntPrimitive.read(bytes).getValue();
		
		return new ChatAdminWithInvites(admin_id, invites_count, revoked_invites_count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.admin_id).write());
		bOut.write(new IntPrimitive(this.invites_count).write());
		bOut.write(new IntPrimitive(this.revoked_invites_count).write());
		
		return bOut.toByteArray();
    }
}
