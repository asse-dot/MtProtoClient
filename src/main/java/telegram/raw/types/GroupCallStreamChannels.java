//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GroupCallStreamChannels extends TlObject {

    public static final int ID = -790330702;

    public ArrayList<TlObject> channels;

    public GroupCallStreamChannels(ArrayList<TlObject> channels) {
        this.channels = channels;
    }

    public static GroupCallStreamChannels read(ByteArrayInputStream bytes) throws IOException {
        ArrayList<TlObject> channels = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new GroupCallStreamChannels(channels);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new VectorPrimitive(this.channels).write());
		
		return bOut.toByteArray();
    }
}
