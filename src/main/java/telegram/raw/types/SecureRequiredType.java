//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SecureRequiredType extends TlObject {

    public static final int ID = -2103600678;

    public boolean native_names;
	public boolean selfie_required;
	public boolean translation_required;
	public TlObject type;

    public SecureRequiredType(boolean native_names, boolean selfie_required, boolean translation_required, TlObject type) {
        this.native_names = native_names;
		this.selfie_required = selfie_required;
		this.translation_required = translation_required;
		this.type = type;
    }

    public static SecureRequiredType read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean native_names = (flags & (1 << 0)) != 0;
		boolean selfie_required = (flags & (1 << 1)) != 0;
		boolean translation_required = (flags & (1 << 2)) != 0;
		TlObject type = TlObject.read(bytes);
		
		return new SecureRequiredType(native_names, selfie_required, translation_required, type);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.native_names ? (1 << 0) : 0;
		flags |= this.selfie_required ? (1 << 1) : 0;
		flags |= this.translation_required ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.type.write());
		
		return bOut.toByteArray();
    }
}
