//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class Page extends TlObject {

    public static final int ID = -1738178803;

    public boolean part;
	public boolean rtl;
	public boolean v2;
	public String url;
	public ArrayList<TlObject> blocks;
	public ArrayList<TlObject> photos;
	public ArrayList<TlObject> documents;
	public int views;

    public Page(boolean part, boolean rtl, boolean v2, String url, ArrayList<TlObject> blocks, ArrayList<TlObject> photos, ArrayList<TlObject> documents, int views) {
        this.part = part;
		this.rtl = rtl;
		this.v2 = v2;
		this.url = url;
		this.blocks = blocks;
		this.photos = photos;
		this.documents = documents;
		this.views = views;
    }

    public static Page read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean part = (flags & (1 << 0)) != 0;
		boolean rtl = (flags & (1 << 1)) != 0;
		boolean v2 = (flags & (1 << 2)) != 0;
		String url = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> blocks = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> photos = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> documents = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		int views = (flags & (1 << 3)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new Page(part, rtl, v2, url, blocks, photos, documents, views);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.part ? (1 << 0) : 0;
		flags |= this.rtl ? (1 << 1) : 0;
		flags |= this.v2 ? (1 << 2) : 0;
		flags |= (this.views != 0) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.url).write());
		bOut.write(new VectorPrimitive(this.blocks).write());
		bOut.write(new VectorPrimitive(this.photos).write());
		bOut.write(new VectorPrimitive(this.documents).write());
		if(this.views != 0)
			bOut.write(new IntPrimitive(this.views).write());
		
		return bOut.toByteArray();
    }
}
