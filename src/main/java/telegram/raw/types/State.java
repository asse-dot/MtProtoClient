//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.updates;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class State extends TlObject {

    public static final int ID = -1519637954;

    public int pts;
	public int qts;
	public int date;
	public int seq;
	public int unread_count;

    public State(int pts, int qts, int date, int seq, int unread_count) {
        this.pts = pts;
		this.qts = qts;
		this.date = date;
		this.seq = seq;
		this.unread_count = unread_count;
    }

    public static State read(ByteArrayInputStream bytes) throws IOException {
        int pts = IntPrimitive.read(bytes).getValue();
		int qts = IntPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		int seq = IntPrimitive.read(bytes).getValue();
		int unread_count = IntPrimitive.read(bytes).getValue();
		
		return new State(pts, qts, date, seq, unread_count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.pts).write());
		bOut.write(new IntPrimitive(this.qts).write());
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new IntPrimitive(this.seq).write());
		bOut.write(new IntPrimitive(this.unread_count).write());
		
		return bOut.toByteArray();
    }
}
