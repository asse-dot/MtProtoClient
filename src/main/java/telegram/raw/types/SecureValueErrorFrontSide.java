//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SecureValueErrorFrontSide extends TlObject {

    public static final int ID = 12467706;

    public TlObject type;
	public byte[] file_hash;
	public String text;

    public SecureValueErrorFrontSide(TlObject type, byte[] file_hash, String text) {
        this.type = type;
		this.file_hash = file_hash;
		this.text = text;
    }

    public static SecureValueErrorFrontSide read(ByteArrayInputStream bytes) throws IOException {
        TlObject type = TlObject.read(bytes);
		byte[] file_hash = BytesPrimitive.read(bytes).getValue();
		String text = StringPrimitive.read(bytes).getValue();
		
		return new SecureValueErrorFrontSide(type, file_hash, text);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.type.write());
		bOut.write(new BytesPrimitive(this.file_hash).write());
		bOut.write(new StringPrimitive(this.text).write());
		
		return bOut.toByteArray();
    }
}
