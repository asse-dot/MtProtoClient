//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class Authorizations extends TlObject {

    public static final int ID = 1275039392;

    public int authorization_ttl_days;
	public ArrayList<TlObject> authorizations;

    public Authorizations(int authorization_ttl_days, ArrayList<TlObject> authorizations) {
        this.authorization_ttl_days = authorization_ttl_days;
		this.authorizations = authorizations;
    }

    public static Authorizations read(ByteArrayInputStream bytes) throws IOException {
        int authorization_ttl_days = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> authorizations = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new Authorizations(authorization_ttl_days, authorizations);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.authorization_ttl_days).write());
		bOut.write(new VectorPrimitive(this.authorizations).write());
		
		return bOut.toByteArray();
    }
}
