//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ReactionCount extends TlObject {

    public static final int ID = -1546531968;

    public int chosen_order;
	public TlObject reaction;
	public int count;

    public ReactionCount(int chosen_order, TlObject reaction, int count) {
        this.chosen_order = chosen_order;
		this.reaction = reaction;
		this.count = count;
    }

    public static ReactionCount read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		int chosen_order = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		TlObject reaction = TlObject.read(bytes);
		int count = IntPrimitive.read(bytes).getValue();
		
		return new ReactionCount(chosen_order, reaction, count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.chosen_order != 0) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.chosen_order != 0)
			bOut.write(new IntPrimitive(this.chosen_order).write());
		bOut.write(this.reaction.write());
		bOut.write(new IntPrimitive(this.count).write());
		
		return bOut.toByteArray();
    }
}
