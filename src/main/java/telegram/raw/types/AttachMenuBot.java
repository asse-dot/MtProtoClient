//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class AttachMenuBot extends TlObject {

    public static final int ID = -928371502;

    public boolean inactive;
	public boolean has_settings;
	public boolean request_write_access;
	public long bot_id;
	public String short_name;
	public ArrayList<TlObject> peer_types;
	public ArrayList<TlObject> icons;

    public AttachMenuBot(boolean inactive, boolean has_settings, boolean request_write_access, long bot_id, String short_name, ArrayList<TlObject> peer_types, ArrayList<TlObject> icons) {
        this.inactive = inactive;
		this.has_settings = has_settings;
		this.request_write_access = request_write_access;
		this.bot_id = bot_id;
		this.short_name = short_name;
		this.peer_types = peer_types;
		this.icons = icons;
    }

    public static AttachMenuBot read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean inactive = (flags & (1 << 0)) != 0;
		boolean has_settings = (flags & (1 << 1)) != 0;
		boolean request_write_access = (flags & (1 << 2)) != 0;
		long bot_id = LongPrimitive.read(bytes).getValue();
		String short_name = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> peer_types = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> icons = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new AttachMenuBot(inactive, has_settings, request_write_access, bot_id, short_name, peer_types, icons);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.inactive ? (1 << 0) : 0;
		flags |= this.has_settings ? (1 << 1) : 0;
		flags |= this.request_write_access ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.bot_id).write());
		bOut.write(new StringPrimitive(this.short_name).write());
		bOut.write(new VectorPrimitive(this.peer_types).write());
		bOut.write(new VectorPrimitive(this.icons).write());
		
		return bOut.toByteArray();
    }
}
