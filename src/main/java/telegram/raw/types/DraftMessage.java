//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DraftMessage extends TlObject {

    public static final int ID = -40996577;

    public boolean no_webpage;
	public int reply_to_msg_id;
	public String message;
	public ArrayList<TlObject> entities;
	public int date;

    public DraftMessage(boolean no_webpage, int reply_to_msg_id, String message, ArrayList<TlObject> entities, int date) {
        this.no_webpage = no_webpage;
		this.reply_to_msg_id = reply_to_msg_id;
		this.message = message;
		this.entities = entities;
		this.date = date;
    }

    public static DraftMessage read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean no_webpage = (flags & (1 << 1)) != 0;
		int reply_to_msg_id = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String message = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> entities = (flags & (1 << 3)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		int date = IntPrimitive.read(bytes).getValue();
		
		return new DraftMessage(no_webpage, reply_to_msg_id, message, entities, date);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.no_webpage ? (1 << 1) : 0;
		flags |= (this.reply_to_msg_id != 0) ? (1 << 0) : 0;
		flags |= (this.entities != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.reply_to_msg_id != 0)
			bOut.write(new IntPrimitive(this.reply_to_msg_id).write());
		bOut.write(new StringPrimitive(this.message).write());
		if(this.entities != null)
			bOut.write(new VectorPrimitive(this.entities).write());
		bOut.write(new IntPrimitive(this.date).write());
		
		return bOut.toByteArray();
    }
}
