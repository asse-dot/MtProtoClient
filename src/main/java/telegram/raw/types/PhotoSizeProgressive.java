//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PhotoSizeProgressive extends TlObject {

    public static final int ID = -96535659;

    public String type;
	public int w;
	public int h;
	public ArrayList<TlObject> sizes;

    public PhotoSizeProgressive(String type, int w, int h, ArrayList<TlObject> sizes) {
        this.type = type;
		this.w = w;
		this.h = h;
		this.sizes = sizes;
    }

    public static PhotoSizeProgressive read(ByteArrayInputStream bytes) throws IOException {
        String type = StringPrimitive.read(bytes).getValue();
		int w = IntPrimitive.read(bytes).getValue();
		int h = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> sizes = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new PhotoSizeProgressive(type, w, h, sizes);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.type).write());
		bOut.write(new IntPrimitive(this.w).write());
		bOut.write(new IntPrimitive(this.h).write());
		bOut.write(new VectorPrimitive(this.sizes).write());
		
		return bOut.toByteArray();
    }
}
