//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DialogFolder extends TlObject {

    public static final int ID = 1908216652;

    public boolean pinned;
	public TlObject folder;
	public TlObject peer;
	public int top_message;
	public int unread_muted_peers_count;
	public int unread_unmuted_peers_count;
	public int unread_muted_messages_count;
	public int unread_unmuted_messages_count;

    public DialogFolder(boolean pinned, TlObject folder, TlObject peer, int top_message, int unread_muted_peers_count, int unread_unmuted_peers_count, int unread_muted_messages_count, int unread_unmuted_messages_count) {
        this.pinned = pinned;
		this.folder = folder;
		this.peer = peer;
		this.top_message = top_message;
		this.unread_muted_peers_count = unread_muted_peers_count;
		this.unread_unmuted_peers_count = unread_unmuted_peers_count;
		this.unread_muted_messages_count = unread_muted_messages_count;
		this.unread_unmuted_messages_count = unread_unmuted_messages_count;
    }

    public static DialogFolder read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean pinned = (flags & (1 << 2)) != 0;
		TlObject folder = TlObject.read(bytes);
		TlObject peer = TlObject.read(bytes);
		int top_message = IntPrimitive.read(bytes).getValue();
		int unread_muted_peers_count = IntPrimitive.read(bytes).getValue();
		int unread_unmuted_peers_count = IntPrimitive.read(bytes).getValue();
		int unread_muted_messages_count = IntPrimitive.read(bytes).getValue();
		int unread_unmuted_messages_count = IntPrimitive.read(bytes).getValue();
		
		return new DialogFolder(pinned, folder, peer, top_message, unread_muted_peers_count, unread_unmuted_peers_count, unread_muted_messages_count, unread_unmuted_messages_count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.pinned ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.folder.write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.top_message).write());
		bOut.write(new IntPrimitive(this.unread_muted_peers_count).write());
		bOut.write(new IntPrimitive(this.unread_unmuted_peers_count).write());
		bOut.write(new IntPrimitive(this.unread_muted_messages_count).write());
		bOut.write(new IntPrimitive(this.unread_unmuted_messages_count).write());
		
		return bOut.toByteArray();
    }
}
