//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EmailVerifiedLogin extends TlObject {

    public static final int ID = -507835039;

    public String email;
	public TlObject sent_code;

    public EmailVerifiedLogin(String email, TlObject sent_code) {
        this.email = email;
		this.sent_code = sent_code;
    }

    public static EmailVerifiedLogin read(ByteArrayInputStream bytes) throws IOException {
        String email = StringPrimitive.read(bytes).getValue();
		TlObject sent_code = TlObject.read(bytes);
		
		return new EmailVerifiedLogin(email, sent_code);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.email).write());
		bOut.write(this.sent_code.write());
		
		return bOut.toByteArray();
    }
}
