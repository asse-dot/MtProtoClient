//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ExportedChatInviteReplaced extends TlObject {

    public static final int ID = 572915951;

    public TlObject invite;
	public TlObject new_invite;
	public ArrayList<TlObject> users;

    public ExportedChatInviteReplaced(TlObject invite, TlObject new_invite, ArrayList<TlObject> users) {
        this.invite = invite;
		this.new_invite = new_invite;
		this.users = users;
    }

    public static ExportedChatInviteReplaced read(ByteArrayInputStream bytes) throws IOException {
        TlObject invite = TlObject.read(bytes);
		TlObject new_invite = TlObject.read(bytes);
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ExportedChatInviteReplaced(invite, new_invite, users);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.invite.write());
		bOut.write(this.new_invite.write());
		bOut.write(new VectorPrimitive(this.users).write());
		
		return bOut.toByteArray();
    }
}
