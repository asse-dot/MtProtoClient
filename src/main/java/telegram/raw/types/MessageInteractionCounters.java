//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageInteractionCounters extends TlObject {

    public static final int ID = -1387279939;

    public int msg_id;
	public int views;
	public int forwards;

    public MessageInteractionCounters(int msg_id, int views, int forwards) {
        this.msg_id = msg_id;
		this.views = views;
		this.forwards = forwards;
    }

    public static MessageInteractionCounters read(ByteArrayInputStream bytes) throws IOException {
        int msg_id = IntPrimitive.read(bytes).getValue();
		int views = IntPrimitive.read(bytes).getValue();
		int forwards = IntPrimitive.read(bytes).getValue();
		
		return new MessageInteractionCounters(msg_id, views, forwards);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.msg_id).write());
		bOut.write(new IntPrimitive(this.views).write());
		bOut.write(new IntPrimitive(this.forwards).write());
		
		return bOut.toByteArray();
    }
}
