//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChatParticipants extends TlObject {

    public static final int ID = 1018991608;

    public long chat_id;
	public ArrayList<TlObject> participants;
	public int version;

    public ChatParticipants(long chat_id, ArrayList<TlObject> participants, int version) {
        this.chat_id = chat_id;
		this.participants = participants;
		this.version = version;
    }

    public static ChatParticipants read(ByteArrayInputStream bytes) throws IOException {
        long chat_id = LongPrimitive.read(bytes).getValue();
		ArrayList<TlObject> participants = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		int version = IntPrimitive.read(bytes).getValue();
		
		return new ChatParticipants(chat_id, participants, version);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.chat_id).write());
		bOut.write(new VectorPrimitive(this.participants).write());
		bOut.write(new IntPrimitive(this.version).write());
		
		return bOut.toByteArray();
    }
}
