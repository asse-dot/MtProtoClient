//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateChannelUserTyping extends TlObject {

    public static final int ID = -1937192669;

    public long channel_id;
	public int top_msg_id;
	public TlObject from_id;
	public TlObject action;

    public UpdateChannelUserTyping(long channel_id, int top_msg_id, TlObject from_id, TlObject action) {
        this.channel_id = channel_id;
		this.top_msg_id = top_msg_id;
		this.from_id = from_id;
		this.action = action;
    }

    public static UpdateChannelUserTyping read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long channel_id = LongPrimitive.read(bytes).getValue();
		int top_msg_id = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		TlObject from_id = TlObject.read(bytes);
		TlObject action = TlObject.read(bytes);
		
		return new UpdateChannelUserTyping(channel_id, top_msg_id, from_id, action);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.top_msg_id != 0) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.channel_id).write());
		if(this.top_msg_id != 0)
			bOut.write(new IntPrimitive(this.top_msg_id).write());
		bOut.write(this.from_id.write());
		bOut.write(this.action.write());
		
		return bOut.toByteArray();
    }
}
