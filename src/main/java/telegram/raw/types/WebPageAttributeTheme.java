//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class WebPageAttributeTheme extends TlObject {

    public static final int ID = 1421174295;

    public ArrayList<TlObject> documents;
	public TlObject settings;

    public WebPageAttributeTheme(ArrayList<TlObject> documents, TlObject settings) {
        this.documents = documents;
		this.settings = settings;
    }

    public static WebPageAttributeTheme read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> documents = (flags & (1 << 0)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		TlObject settings = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		
		return new WebPageAttributeTheme(documents, settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.documents != null) ? (1 << 0) : 0;
		flags |= (this.settings != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.documents != null)
			bOut.write(new VectorPrimitive(this.documents).write());
		if(this.settings != null)
			bOut.write(this.settings.write());
		
		return bOut.toByteArray();
    }
}
