//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PasswordSettings extends TlObject {

    public static final int ID = -1705233435;

    public String email;
	public TlObject secure_settings;

    public PasswordSettings(String email, TlObject secure_settings) {
        this.email = email;
		this.secure_settings = secure_settings;
    }

    public static PasswordSettings read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String email = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject secure_settings = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		
		return new PasswordSettings(email, secure_settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.email != null) ? (1 << 0) : 0;
		flags |= (this.secure_settings != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.email != null)
			bOut.write(new StringPrimitive(this.email).write());
		if(this.secure_settings != null)
			bOut.write(this.secure_settings.write());
		
		return bOut.toByteArray();
    }
}
