//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EmojiKeyword extends TlObject {

    public static final int ID = -709641735;

    public String keyword;
	public ArrayList<TlObject> emoticons;

    public EmojiKeyword(String keyword, ArrayList<TlObject> emoticons) {
        this.keyword = keyword;
		this.emoticons = emoticons;
    }

    public static EmojiKeyword read(ByteArrayInputStream bytes) throws IOException {
        String keyword = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> emoticons = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new EmojiKeyword(keyword, emoticons);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.keyword).write());
		bOut.write(new VectorPrimitive(this.emoticons).write());
		
		return bOut.toByteArray();
    }
}
