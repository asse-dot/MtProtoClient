//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateServiceNotification extends TlObject {

    public static final int ID = -337352679;

    public boolean popup;
	public int inbox_date;
	public String type;
	public String message;
	public TlObject media;
	public ArrayList<TlObject> entities;

    public UpdateServiceNotification(boolean popup, int inbox_date, String type, String message, TlObject media, ArrayList<TlObject> entities) {
        this.popup = popup;
		this.inbox_date = inbox_date;
		this.type = type;
		this.message = message;
		this.media = media;
		this.entities = entities;
    }

    public static UpdateServiceNotification read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean popup = (flags & (1 << 0)) != 0;
		int inbox_date = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String type = StringPrimitive.read(bytes).getValue();
		String message = StringPrimitive.read(bytes).getValue();
		TlObject media = TlObject.read(bytes);
		ArrayList<TlObject> entities = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new UpdateServiceNotification(popup, inbox_date, type, message, media, entities);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.popup ? (1 << 0) : 0;
		flags |= (this.inbox_date != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.inbox_date != 0)
			bOut.write(new IntPrimitive(this.inbox_date).write());
		bOut.write(new StringPrimitive(this.type).write());
		bOut.write(new StringPrimitive(this.message).write());
		bOut.write(this.media.write());
		bOut.write(new VectorPrimitive(this.entities).write());
		
		return bOut.toByteArray();
    }
}
