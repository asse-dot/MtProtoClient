//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateReadMessagesContents extends TlObject {

    public static final int ID = 1757493555;

    public ArrayList<TlObject> messages;
	public int pts;
	public int pts_count;

    public UpdateReadMessagesContents(ArrayList<TlObject> messages, int pts, int pts_count) {
        this.messages = messages;
		this.pts = pts;
		this.pts_count = pts_count;
    }

    public static UpdateReadMessagesContents read(ByteArrayInputStream bytes) throws IOException {
        ArrayList<TlObject> messages = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		int pts = IntPrimitive.read(bytes).getValue();
		int pts_count = IntPrimitive.read(bytes).getValue();
		
		return new UpdateReadMessagesContents(messages, pts, pts_count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new VectorPrimitive(this.messages).write());
		bOut.write(new IntPrimitive(this.pts).write());
		bOut.write(new IntPrimitive(this.pts_count).write());
		
		return bOut.toByteArray();
    }
}
