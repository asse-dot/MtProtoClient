//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MaskCoords extends TlObject {

    public static final int ID = -1361650766;

    public int n;
	public TlObject x;
	public TlObject y;
	public TlObject zoom;

    public MaskCoords(int n, TlObject x, TlObject y, TlObject zoom) {
        this.n = n;
		this.x = x;
		this.y = y;
		this.zoom = zoom;
    }

    public static MaskCoords read(ByteArrayInputStream bytes) throws IOException {
        int n = IntPrimitive.read(bytes).getValue();
		TlObject x = TlObject.read(bytes);
		TlObject y = TlObject.read(bytes);
		TlObject zoom = TlObject.read(bytes);
		
		return new MaskCoords(n, x, y, zoom);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.n).write());
		bOut.write(this.x.write());
		bOut.write(this.y.write());
		bOut.write(this.zoom.write());
		
		return bOut.toByteArray();
    }
}
