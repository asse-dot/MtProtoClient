//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.help;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class CountriesList extends TlObject {

    public static final int ID = -2016381538;

    public ArrayList<TlObject> countries;
	public int hash;

    public CountriesList(ArrayList<TlObject> countries, int hash) {
        this.countries = countries;
		this.hash = hash;
    }

    public static CountriesList read(ByteArrayInputStream bytes) throws IOException {
        ArrayList<TlObject> countries = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		int hash = IntPrimitive.read(bytes).getValue();
		
		return new CountriesList(countries, hash);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new VectorPrimitive(this.countries).write());
		bOut.write(new IntPrimitive(this.hash).write());
		
		return bOut.toByteArray();
    }
}
