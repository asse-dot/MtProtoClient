//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DocumentAttributeCustomEmoji extends TlObject {

    public static final int ID = -48981863;

    public boolean free;
	public boolean text_color;
	public String alt;
	public TlObject stickerset;

    public DocumentAttributeCustomEmoji(boolean free, boolean text_color, String alt, TlObject stickerset) {
        this.free = free;
		this.text_color = text_color;
		this.alt = alt;
		this.stickerset = stickerset;
    }

    public static DocumentAttributeCustomEmoji read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean free = (flags & (1 << 0)) != 0;
		boolean text_color = (flags & (1 << 1)) != 0;
		String alt = StringPrimitive.read(bytes).getValue();
		TlObject stickerset = TlObject.read(bytes);
		
		return new DocumentAttributeCustomEmoji(free, text_color, alt, stickerset);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.free ? (1 << 0) : 0;
		flags |= this.text_color ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.alt).write());
		bOut.write(this.stickerset.write());
		
		return bOut.toByteArray();
    }
}
