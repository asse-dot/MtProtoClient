//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.bots;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class BotInfo extends TlObject {

    public static final int ID = -391678544;

    public String name;
	public String about;
	public String description;

    public BotInfo(String name, String about, String description) {
        this.name = name;
		this.about = about;
		this.description = description;
    }

    public static BotInfo read(ByteArrayInputStream bytes) throws IOException {
        String name = StringPrimitive.read(bytes).getValue();
		String about = StringPrimitive.read(bytes).getValue();
		String description = StringPrimitive.read(bytes).getValue();
		
		return new BotInfo(name, about, description);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.name).write());
		bOut.write(new StringPrimitive(this.about).write());
		bOut.write(new StringPrimitive(this.description).write());
		
		return bOut.toByteArray();
    }
}
