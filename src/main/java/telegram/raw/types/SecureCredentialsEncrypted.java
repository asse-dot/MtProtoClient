//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SecureCredentialsEncrypted extends TlObject {

    public static final int ID = 871426631;

    public byte[] data;
	public byte[] hash;
	public byte[] secret;

    public SecureCredentialsEncrypted(byte[] data, byte[] hash, byte[] secret) {
        this.data = data;
		this.hash = hash;
		this.secret = secret;
    }

    public static SecureCredentialsEncrypted read(ByteArrayInputStream bytes) throws IOException {
        byte[] data = BytesPrimitive.read(bytes).getValue();
		byte[] hash = BytesPrimitive.read(bytes).getValue();
		byte[] secret = BytesPrimitive.read(bytes).getValue();
		
		return new SecureCredentialsEncrypted(data, hash, secret);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new BytesPrimitive(this.data).write());
		bOut.write(new BytesPrimitive(this.hash).write());
		bOut.write(new BytesPrimitive(this.secret).write());
		
		return bOut.toByteArray();
    }
}
