//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputBotInlineResultDocument extends TlObject {

    public static final int ID = -459324;

    public String id;
	public String type;
	public String title;
	public String description;
	public TlObject document;
	public TlObject send_message;

    public InputBotInlineResultDocument(String id, String type, String title, String description, TlObject document, TlObject send_message) {
        this.id = id;
		this.type = type;
		this.title = title;
		this.description = description;
		this.document = document;
		this.send_message = send_message;
    }

    public static InputBotInlineResultDocument read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String id = StringPrimitive.read(bytes).getValue();
		String type = StringPrimitive.read(bytes).getValue();
		String title = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String description = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject document = TlObject.read(bytes);
		TlObject send_message = TlObject.read(bytes);
		
		return new InputBotInlineResultDocument(id, type, title, description, document, send_message);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.title != null) ? (1 << 1) : 0;
		flags |= (this.description != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.id).write());
		bOut.write(new StringPrimitive(this.type).write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		if(this.description != null)
			bOut.write(new StringPrimitive(this.description).write());
		bOut.write(this.document.write());
		bOut.write(this.send_message.write());
		
		return bOut.toByteArray();
    }
}
