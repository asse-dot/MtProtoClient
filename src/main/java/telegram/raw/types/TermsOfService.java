//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.help;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class TermsOfService extends TlObject {

    public static final int ID = 2013922064;

    public boolean popup;
	public TlObject id;
	public String text;
	public ArrayList<TlObject> entities;
	public int min_age_confirm;

    public TermsOfService(boolean popup, TlObject id, String text, ArrayList<TlObject> entities, int min_age_confirm) {
        this.popup = popup;
		this.id = id;
		this.text = text;
		this.entities = entities;
		this.min_age_confirm = min_age_confirm;
    }

    public static TermsOfService read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean popup = (flags & (1 << 0)) != 0;
		TlObject id = TlObject.read(bytes);
		String text = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> entities = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		int min_age_confirm = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new TermsOfService(popup, id, text, entities, min_age_confirm);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.popup ? (1 << 0) : 0;
		flags |= (this.min_age_confirm != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.id.write());
		bOut.write(new StringPrimitive(this.text).write());
		bOut.write(new VectorPrimitive(this.entities).write());
		if(this.min_age_confirm != 0)
			bOut.write(new IntPrimitive(this.min_age_confirm).write());
		
		return bOut.toByteArray();
    }
}
