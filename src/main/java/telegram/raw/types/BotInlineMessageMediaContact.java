//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class BotInlineMessageMediaContact extends TlObject {

    public static final int ID = 416402882;

    public String phone_number;
	public String first_name;
	public String last_name;
	public String vcard;
	public TlObject reply_markup;

    public BotInlineMessageMediaContact(String phone_number, String first_name, String last_name, String vcard, TlObject reply_markup) {
        this.phone_number = phone_number;
		this.first_name = first_name;
		this.last_name = last_name;
		this.vcard = vcard;
		this.reply_markup = reply_markup;
    }

    public static BotInlineMessageMediaContact read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String phone_number = StringPrimitive.read(bytes).getValue();
		String first_name = StringPrimitive.read(bytes).getValue();
		String last_name = StringPrimitive.read(bytes).getValue();
		String vcard = StringPrimitive.read(bytes).getValue();
		TlObject reply_markup = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new BotInlineMessageMediaContact(phone_number, first_name, last_name, vcard, reply_markup);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.reply_markup != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.phone_number).write());
		bOut.write(new StringPrimitive(this.first_name).write());
		bOut.write(new StringPrimitive(this.last_name).write());
		bOut.write(new StringPrimitive(this.vcard).write());
		if(this.reply_markup != null)
			bOut.write(this.reply_markup.write());
		
		return bOut.toByteArray();
    }
}
