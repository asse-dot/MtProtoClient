//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.payments;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SavedInfo extends TlObject {

    public static final int ID = -74456004;

    public boolean has_saved_credentials;
	public TlObject saved_info;

    public SavedInfo(boolean has_saved_credentials, TlObject saved_info) {
        this.has_saved_credentials = has_saved_credentials;
		this.saved_info = saved_info;
    }

    public static SavedInfo read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean has_saved_credentials = (flags & (1 << 1)) != 0;
		TlObject saved_info = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new SavedInfo(has_saved_credentials, saved_info);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.has_saved_credentials ? (1 << 1) : 0;
		flags |= (this.saved_info != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.saved_info != null)
			bOut.write(this.saved_info.write());
		
		return bOut.toByteArray();
    }
}
