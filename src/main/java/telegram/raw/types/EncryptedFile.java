//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EncryptedFile extends TlObject {

    public static final int ID = -1476358952;

    public long id;
	public long access_hash;
	public long size;
	public int dc_id;
	public int key_fingerprint;

    public EncryptedFile(long id, long access_hash, long size, int dc_id, int key_fingerprint) {
        this.id = id;
		this.access_hash = access_hash;
		this.size = size;
		this.dc_id = dc_id;
		this.key_fingerprint = key_fingerprint;
    }

    public static EncryptedFile read(ByteArrayInputStream bytes) throws IOException {
        long id = LongPrimitive.read(bytes).getValue();
		long access_hash = LongPrimitive.read(bytes).getValue();
		long size = LongPrimitive.read(bytes).getValue();
		int dc_id = IntPrimitive.read(bytes).getValue();
		int key_fingerprint = IntPrimitive.read(bytes).getValue();
		
		return new EncryptedFile(id, access_hash, size, dc_id, key_fingerprint);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new LongPrimitive(this.size).write());
		bOut.write(new IntPrimitive(this.dc_id).write());
		bOut.write(new IntPrimitive(this.key_fingerprint).write());
		
		return bOut.toByteArray();
    }
}
