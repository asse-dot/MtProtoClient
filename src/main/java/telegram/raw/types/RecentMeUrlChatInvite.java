//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RecentMeUrlChatInvite extends TlObject {

    public static final int ID = -347535331;

    public String url;
	public TlObject chat_invite;

    public RecentMeUrlChatInvite(String url, TlObject chat_invite) {
        this.url = url;
		this.chat_invite = chat_invite;
    }

    public static RecentMeUrlChatInvite read(ByteArrayInputStream bytes) throws IOException {
        String url = StringPrimitive.read(bytes).getValue();
		TlObject chat_invite = TlObject.read(bytes);
		
		return new RecentMeUrlChatInvite(url, chat_invite);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.url).write());
		bOut.write(this.chat_invite.write());
		
		return bOut.toByteArray();
    }
}
