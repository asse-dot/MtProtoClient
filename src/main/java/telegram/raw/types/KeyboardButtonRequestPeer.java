//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class KeyboardButtonRequestPeer extends TlObject {

    public static final int ID = 218842764;

    public String text;
	public int button_id;
	public TlObject peer_type;

    public KeyboardButtonRequestPeer(String text, int button_id, TlObject peer_type) {
        this.text = text;
		this.button_id = button_id;
		this.peer_type = peer_type;
    }

    public static KeyboardButtonRequestPeer read(ByteArrayInputStream bytes) throws IOException {
        String text = StringPrimitive.read(bytes).getValue();
		int button_id = IntPrimitive.read(bytes).getValue();
		TlObject peer_type = TlObject.read(bytes);
		
		return new KeyboardButtonRequestPeer(text, button_id, peer_type);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.text).write());
		bOut.write(new IntPrimitive(this.button_id).write());
		bOut.write(this.peer_type.write());
		
		return bOut.toByteArray();
    }
}
