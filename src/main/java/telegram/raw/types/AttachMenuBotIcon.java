//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class AttachMenuBotIcon extends TlObject {

    public static final int ID = -1297663893;

    public String name;
	public TlObject icon;
	public ArrayList<TlObject> colors;

    public AttachMenuBotIcon(String name, TlObject icon, ArrayList<TlObject> colors) {
        this.name = name;
		this.icon = icon;
		this.colors = colors;
    }

    public static AttachMenuBotIcon read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String name = StringPrimitive.read(bytes).getValue();
		TlObject icon = TlObject.read(bytes);
		ArrayList<TlObject> colors = (flags & (1 << 0)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		
		return new AttachMenuBotIcon(name, icon, colors);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.colors != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.name).write());
		bOut.write(this.icon.write());
		if(this.colors != null)
			bOut.write(new VectorPrimitive(this.colors).write());
		
		return bOut.toByteArray();
    }
}
