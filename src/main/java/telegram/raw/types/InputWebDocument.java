//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputWebDocument extends TlObject {

    public static final int ID = -1678949555;

    public String url;
	public int size;
	public String mime_type;
	public ArrayList<TlObject> attributes;

    public InputWebDocument(String url, int size, String mime_type, ArrayList<TlObject> attributes) {
        this.url = url;
		this.size = size;
		this.mime_type = mime_type;
		this.attributes = attributes;
    }

    public static InputWebDocument read(ByteArrayInputStream bytes) throws IOException {
        String url = StringPrimitive.read(bytes).getValue();
		int size = IntPrimitive.read(bytes).getValue();
		String mime_type = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> attributes = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new InputWebDocument(url, size, mime_type, attributes);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.url).write());
		bOut.write(new IntPrimitive(this.size).write());
		bOut.write(new StringPrimitive(this.mime_type).write());
		bOut.write(new VectorPrimitive(this.attributes).write());
		
		return bOut.toByteArray();
    }
}
