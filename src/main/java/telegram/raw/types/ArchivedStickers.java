//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ArchivedStickers extends TlObject {

    public static final int ID = 1338747336;

    public int count;
	public ArrayList<TlObject> sets;

    public ArchivedStickers(int count, ArrayList<TlObject> sets) {
        this.count = count;
		this.sets = sets;
    }

    public static ArchivedStickers read(ByteArrayInputStream bytes) throws IOException {
        int count = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> sets = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ArchivedStickers(count, sets);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.count).write());
		bOut.write(new VectorPrimitive(this.sets).write());
		
		return bOut.toByteArray();
    }
}
