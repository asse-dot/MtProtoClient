//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateUserName extends TlObject {

    public static final int ID = -1484486364;

    public long user_id;
	public String first_name;
	public String last_name;
	public ArrayList<TlObject> usernames;

    public UpdateUserName(long user_id, String first_name, String last_name, ArrayList<TlObject> usernames) {
        this.user_id = user_id;
		this.first_name = first_name;
		this.last_name = last_name;
		this.usernames = usernames;
    }

    public static UpdateUserName read(ByteArrayInputStream bytes) throws IOException {
        long user_id = LongPrimitive.read(bytes).getValue();
		String first_name = StringPrimitive.read(bytes).getValue();
		String last_name = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> usernames = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new UpdateUserName(user_id, first_name, last_name, usernames);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new StringPrimitive(this.first_name).write());
		bOut.write(new StringPrimitive(this.last_name).write());
		bOut.write(new VectorPrimitive(this.usernames).write());
		
		return bOut.toByteArray();
    }
}
