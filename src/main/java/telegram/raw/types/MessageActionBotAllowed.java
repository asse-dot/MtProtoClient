//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageActionBotAllowed extends TlObject {

    public static final int ID = -988359047;

    public boolean attach_menu;
	public String domain;
	public TlObject app;

    public MessageActionBotAllowed(boolean attach_menu, String domain, TlObject app) {
        this.attach_menu = attach_menu;
		this.domain = domain;
		this.app = app;
    }

    public static MessageActionBotAllowed read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean attach_menu = (flags & (1 << 1)) != 0;
		String domain = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject app = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new MessageActionBotAllowed(attach_menu, domain, app);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.attach_menu ? (1 << 1) : 0;
		flags |= (this.domain != null) ? (1 << 0) : 0;
		flags |= (this.app != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.domain != null)
			bOut.write(new StringPrimitive(this.domain).write());
		if(this.app != null)
			bOut.write(this.app.write());
		
		return bOut.toByteArray();
    }
}
