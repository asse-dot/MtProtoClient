//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class VideoSize extends TlObject {

    public static final int ID = -567037804;

    public String type;
	public int w;
	public int h;
	public int size;
	public TlObject video_start_ts;

    public VideoSize(String type, int w, int h, int size, TlObject video_start_ts) {
        this.type = type;
		this.w = w;
		this.h = h;
		this.size = size;
		this.video_start_ts = video_start_ts;
    }

    public static VideoSize read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String type = StringPrimitive.read(bytes).getValue();
		int w = IntPrimitive.read(bytes).getValue();
		int h = IntPrimitive.read(bytes).getValue();
		int size = IntPrimitive.read(bytes).getValue();
		TlObject video_start_ts = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new VideoSize(type, w, h, size, video_start_ts);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.video_start_ts != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.type).write());
		bOut.write(new IntPrimitive(this.w).write());
		bOut.write(new IntPrimitive(this.h).write());
		bOut.write(new IntPrimitive(this.size).write());
		if(this.video_start_ts != null)
			bOut.write(this.video_start_ts.write());
		
		return bOut.toByteArray();
    }
}
