//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class KeyboardButtonCallback extends TlObject {

    public static final int ID = 901503851;

    public boolean requires_password;
	public String text;
	public byte[] data;

    public KeyboardButtonCallback(boolean requires_password, String text, byte[] data) {
        this.requires_password = requires_password;
		this.text = text;
		this.data = data;
    }

    public static KeyboardButtonCallback read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean requires_password = (flags & (1 << 0)) != 0;
		String text = StringPrimitive.read(bytes).getValue();
		byte[] data = BytesPrimitive.read(bytes).getValue();
		
		return new KeyboardButtonCallback(requires_password, text, data);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.requires_password ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.text).write());
		bOut.write(new BytesPrimitive(this.data).write());
		
		return bOut.toByteArray();
    }
}
