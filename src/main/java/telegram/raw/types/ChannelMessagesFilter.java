//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChannelMessagesFilter extends TlObject {

    public static final int ID = -847783593;

    public boolean exclude_new_messages;
	public ArrayList<TlObject> ranges;

    public ChannelMessagesFilter(boolean exclude_new_messages, ArrayList<TlObject> ranges) {
        this.exclude_new_messages = exclude_new_messages;
		this.ranges = ranges;
    }

    public static ChannelMessagesFilter read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean exclude_new_messages = (flags & (1 << 1)) != 0;
		ArrayList<TlObject> ranges = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ChannelMessagesFilter(exclude_new_messages, ranges);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.exclude_new_messages ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new VectorPrimitive(this.ranges).write());
		
		return bOut.toByteArray();
    }
}
