//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class LangPackLanguage extends TlObject {

    public static final int ID = -288727837;

    public boolean official;
	public boolean rtl;
	public boolean beta;
	public String name;
	public String native_name;
	public String lang_code;
	public String base_lang_code;
	public String plural_code;
	public int strings_count;
	public int translated_count;
	public String translations_url;

    public LangPackLanguage(boolean official, boolean rtl, boolean beta, String name, String native_name, String lang_code, String base_lang_code, String plural_code, int strings_count, int translated_count, String translations_url) {
        this.official = official;
		this.rtl = rtl;
		this.beta = beta;
		this.name = name;
		this.native_name = native_name;
		this.lang_code = lang_code;
		this.base_lang_code = base_lang_code;
		this.plural_code = plural_code;
		this.strings_count = strings_count;
		this.translated_count = translated_count;
		this.translations_url = translations_url;
    }

    public static LangPackLanguage read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean official = (flags & (1 << 0)) != 0;
		boolean rtl = (flags & (1 << 2)) != 0;
		boolean beta = (flags & (1 << 3)) != 0;
		String name = StringPrimitive.read(bytes).getValue();
		String native_name = StringPrimitive.read(bytes).getValue();
		String lang_code = StringPrimitive.read(bytes).getValue();
		String base_lang_code = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String plural_code = StringPrimitive.read(bytes).getValue();
		int strings_count = IntPrimitive.read(bytes).getValue();
		int translated_count = IntPrimitive.read(bytes).getValue();
		String translations_url = StringPrimitive.read(bytes).getValue();
		
		return new LangPackLanguage(official, rtl, beta, name, native_name, lang_code, base_lang_code, plural_code, strings_count, translated_count, translations_url);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.official ? (1 << 0) : 0;
		flags |= this.rtl ? (1 << 2) : 0;
		flags |= this.beta ? (1 << 3) : 0;
		flags |= (this.base_lang_code != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.name).write());
		bOut.write(new StringPrimitive(this.native_name).write());
		bOut.write(new StringPrimitive(this.lang_code).write());
		if(this.base_lang_code != null)
			bOut.write(new StringPrimitive(this.base_lang_code).write());
		bOut.write(new StringPrimitive(this.plural_code).write());
		bOut.write(new IntPrimitive(this.strings_count).write());
		bOut.write(new IntPrimitive(this.translated_count).write());
		bOut.write(new StringPrimitive(this.translations_url).write());
		
		return bOut.toByteArray();
    }
}
