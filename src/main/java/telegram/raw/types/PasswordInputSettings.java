//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PasswordInputSettings extends TlObject {

    public static final int ID = -1036572727;

    public TlObject new_algo;
	public byte[] new_password_hash;
	public String hint;
	public String email;
	public TlObject new_secure_settings;

    public PasswordInputSettings(TlObject new_algo, byte[] new_password_hash, String hint, String email, TlObject new_secure_settings) {
        this.new_algo = new_algo;
		this.new_password_hash = new_password_hash;
		this.hint = hint;
		this.email = email;
		this.new_secure_settings = new_secure_settings;
    }

    public static PasswordInputSettings read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject new_algo = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		byte[] new_password_hash = (flags & (1 << 0)) != 0 ?  BytesPrimitive.read(bytes).getValue() : null;
		String hint = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String email = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject new_secure_settings = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new PasswordInputSettings(new_algo, new_password_hash, hint, email, new_secure_settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.new_algo != null) ? (1 << 0) : 0;
		flags |= (this.new_password_hash != null) ? (1 << 0) : 0;
		flags |= (this.hint != null) ? (1 << 0) : 0;
		flags |= (this.email != null) ? (1 << 1) : 0;
		flags |= (this.new_secure_settings != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.new_algo != null)
			bOut.write(this.new_algo.write());
		if(this.new_password_hash != null)
			bOut.write(new BytesPrimitive(this.new_password_hash).write());
		if(this.hint != null)
			bOut.write(new StringPrimitive(this.hint).write());
		if(this.email != null)
			bOut.write(new StringPrimitive(this.email).write());
		if(this.new_secure_settings != null)
			bOut.write(this.new_secure_settings.write());
		
		return bOut.toByteArray();
    }
}
