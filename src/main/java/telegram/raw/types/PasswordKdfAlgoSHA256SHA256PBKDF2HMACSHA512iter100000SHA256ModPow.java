//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow extends TlObject {

    public static final int ID = 982592842;

    public byte[] salt1;
	public byte[] salt2;
	public int g;
	public byte[] p;

    public PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow(byte[] salt1, byte[] salt2, int g, byte[] p) {
        this.salt1 = salt1;
		this.salt2 = salt2;
		this.g = g;
		this.p = p;
    }

    public static PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow read(ByteArrayInputStream bytes) throws IOException {
        byte[] salt1 = BytesPrimitive.read(bytes).getValue();
		byte[] salt2 = BytesPrimitive.read(bytes).getValue();
		int g = IntPrimitive.read(bytes).getValue();
		byte[] p = BytesPrimitive.read(bytes).getValue();
		
		return new PasswordKdfAlgoSHA256SHA256PBKDF2HMACSHA512iter100000SHA256ModPow(salt1, salt2, g, p);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new BytesPrimitive(this.salt1).write());
		bOut.write(new BytesPrimitive(this.salt2).write());
		bOut.write(new IntPrimitive(this.g).write());
		bOut.write(new BytesPrimitive(this.p).write());
		
		return bOut.toByteArray();
    }
}
