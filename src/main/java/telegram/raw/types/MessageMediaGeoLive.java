//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageMediaGeoLive extends TlObject {

    public static final int ID = -1186937242;

    public TlObject geo;
	public int heading;
	public int period;
	public int proximity_notification_radius;

    public MessageMediaGeoLive(TlObject geo, int heading, int period, int proximity_notification_radius) {
        this.geo = geo;
		this.heading = heading;
		this.period = period;
		this.proximity_notification_radius = proximity_notification_radius;
    }

    public static MessageMediaGeoLive read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject geo = TlObject.read(bytes);
		int heading = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int period = IntPrimitive.read(bytes).getValue();
		int proximity_notification_radius = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new MessageMediaGeoLive(geo, heading, period, proximity_notification_radius);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.heading != 0) ? (1 << 0) : 0;
		flags |= (this.proximity_notification_radius != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.geo.write());
		if(this.heading != 0)
			bOut.write(new IntPrimitive(this.heading).write());
		bOut.write(new IntPrimitive(this.period).write());
		if(this.proximity_notification_radius != 0)
			bOut.write(new IntPrimitive(this.proximity_notification_radius).write());
		
		return bOut.toByteArray();
    }
}
