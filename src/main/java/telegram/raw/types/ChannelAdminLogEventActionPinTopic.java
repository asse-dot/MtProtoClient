//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChannelAdminLogEventActionPinTopic extends TlObject {

    public static final int ID = 1569535291;

    public TlObject prev_topic;
	public TlObject new_topic;

    public ChannelAdminLogEventActionPinTopic(TlObject prev_topic, TlObject new_topic) {
        this.prev_topic = prev_topic;
		this.new_topic = new_topic;
    }

    public static ChannelAdminLogEventActionPinTopic read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject prev_topic = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		TlObject new_topic = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		
		return new ChannelAdminLogEventActionPinTopic(prev_topic, new_topic);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.prev_topic != null) ? (1 << 0) : 0;
		flags |= (this.new_topic != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.prev_topic != null)
			bOut.write(this.prev_topic.write());
		if(this.new_topic != null)
			bOut.write(this.new_topic.write());
		
		return bOut.toByteArray();
    }
}
