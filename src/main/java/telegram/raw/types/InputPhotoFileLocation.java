//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputPhotoFileLocation extends TlObject {

    public static final int ID = 1075322878;

    public long id;
	public long access_hash;
	public byte[] file_reference;
	public String thumb_size;

    public InputPhotoFileLocation(long id, long access_hash, byte[] file_reference, String thumb_size) {
        this.id = id;
		this.access_hash = access_hash;
		this.file_reference = file_reference;
		this.thumb_size = thumb_size;
    }

    public static InputPhotoFileLocation read(ByteArrayInputStream bytes) throws IOException {
        long id = LongPrimitive.read(bytes).getValue();
		long access_hash = LongPrimitive.read(bytes).getValue();
		byte[] file_reference = BytesPrimitive.read(bytes).getValue();
		String thumb_size = StringPrimitive.read(bytes).getValue();
		
		return new InputPhotoFileLocation(id, access_hash, file_reference, thumb_size);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new BytesPrimitive(this.file_reference).write());
		bOut.write(new StringPrimitive(this.thumb_size).write());
		
		return bOut.toByteArray();
    }
}
