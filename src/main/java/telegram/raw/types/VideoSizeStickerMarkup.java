//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class VideoSizeStickerMarkup extends TlObject {

    public static final int ID = 228623102;

    public TlObject stickerset;
	public long sticker_id;
	public ArrayList<TlObject> background_colors;

    public VideoSizeStickerMarkup(TlObject stickerset, long sticker_id, ArrayList<TlObject> background_colors) {
        this.stickerset = stickerset;
		this.sticker_id = sticker_id;
		this.background_colors = background_colors;
    }

    public static VideoSizeStickerMarkup read(ByteArrayInputStream bytes) throws IOException {
        TlObject stickerset = TlObject.read(bytes);
		long sticker_id = LongPrimitive.read(bytes).getValue();
		ArrayList<TlObject> background_colors = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new VideoSizeStickerMarkup(stickerset, sticker_id, background_colors);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.stickerset.write());
		bOut.write(new LongPrimitive(this.sticker_id).write());
		bOut.write(new VectorPrimitive(this.background_colors).write());
		
		return bOut.toByteArray();
    }
}
