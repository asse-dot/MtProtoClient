//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChatInviteImporter extends TlObject {

    public static final int ID = -1940201511;

    public boolean requested;
	public boolean via_chatlist;
	public long user_id;
	public int date;
	public String about;
	public long approved_by;

    public ChatInviteImporter(boolean requested, boolean via_chatlist, long user_id, int date, String about, long approved_by) {
        this.requested = requested;
		this.via_chatlist = via_chatlist;
		this.user_id = user_id;
		this.date = date;
		this.about = about;
		this.approved_by = approved_by;
    }

    public static ChatInviteImporter read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean requested = (flags & (1 << 0)) != 0;
		boolean via_chatlist = (flags & (1 << 3)) != 0;
		long user_id = LongPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		String about = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		long approved_by = (flags & (1 << 1)) != 0 ?  LongPrimitive.read(bytes).getValue() : 0;
		
		return new ChatInviteImporter(requested, via_chatlist, user_id, date, about, approved_by);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.requested ? (1 << 0) : 0;
		flags |= this.via_chatlist ? (1 << 3) : 0;
		flags |= (this.about != null) ? (1 << 2) : 0;
		flags |= (this.approved_by != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		if(this.about != null)
			bOut.write(new StringPrimitive(this.about).write());
		if(this.approved_by != 0)
			bOut.write(new LongPrimitive(this.approved_by).write());
		
		return bOut.toByteArray();
    }
}
