//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateBotShippingQuery extends TlObject {

    public static final int ID = -1246823043;

    public long query_id;
	public long user_id;
	public byte[] payload;
	public TlObject shipping_address;

    public UpdateBotShippingQuery(long query_id, long user_id, byte[] payload, TlObject shipping_address) {
        this.query_id = query_id;
		this.user_id = user_id;
		this.payload = payload;
		this.shipping_address = shipping_address;
    }

    public static UpdateBotShippingQuery read(ByteArrayInputStream bytes) throws IOException {
        long query_id = LongPrimitive.read(bytes).getValue();
		long user_id = LongPrimitive.read(bytes).getValue();
		byte[] payload = BytesPrimitive.read(bytes).getValue();
		TlObject shipping_address = TlObject.read(bytes);
		
		return new UpdateBotShippingQuery(query_id, user_id, payload, shipping_address);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.query_id).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new BytesPrimitive(this.payload).write());
		bOut.write(this.shipping_address.write());
		
		return bOut.toByteArray();
    }
}
