//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.updates;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChannelDifference extends TlObject {

    public static final int ID = 543450958;

    public boolean finalv;
	public int pts;
	public int timeout;
	public ArrayList<TlObject> new_messages;
	public ArrayList<TlObject> other_updates;
	public ArrayList<TlObject> chats;
	public ArrayList<TlObject> users;

    public ChannelDifference(boolean finalv, int pts, int timeout, ArrayList<TlObject> new_messages, ArrayList<TlObject> other_updates, ArrayList<TlObject> chats, ArrayList<TlObject> users) {
        this.finalv = finalv;
		this.pts = pts;
		this.timeout = timeout;
		this.new_messages = new_messages;
		this.other_updates = other_updates;
		this.chats = chats;
		this.users = users;
    }

    public static ChannelDifference read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean finalv = (flags & (1 << 0)) != 0;
		int pts = IntPrimitive.read(bytes).getValue();
		int timeout = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		ArrayList<TlObject> new_messages = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> other_updates = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> chats = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ChannelDifference(finalv, pts, timeout, new_messages, other_updates, chats, users);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.finalv ? (1 << 0) : 0;
		flags |= (this.timeout != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.pts).write());
		if(this.timeout != 0)
			bOut.write(new IntPrimitive(this.timeout).write());
		bOut.write(new VectorPrimitive(this.new_messages).write());
		bOut.write(new VectorPrimitive(this.other_updates).write());
		bOut.write(new VectorPrimitive(this.chats).write());
		bOut.write(new VectorPrimitive(this.users).write());
		
		return bOut.toByteArray();
    }
}
