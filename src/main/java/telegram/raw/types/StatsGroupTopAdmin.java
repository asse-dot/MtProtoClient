//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class StatsGroupTopAdmin extends TlObject {

    public static final int ID = -682079097;

    public long user_id;
	public int deleted;
	public int kicked;
	public int banned;

    public StatsGroupTopAdmin(long user_id, int deleted, int kicked, int banned) {
        this.user_id = user_id;
		this.deleted = deleted;
		this.kicked = kicked;
		this.banned = banned;
    }

    public static StatsGroupTopAdmin read(ByteArrayInputStream bytes) throws IOException {
        long user_id = LongPrimitive.read(bytes).getValue();
		int deleted = IntPrimitive.read(bytes).getValue();
		int kicked = IntPrimitive.read(bytes).getValue();
		int banned = IntPrimitive.read(bytes).getValue();
		
		return new StatsGroupTopAdmin(user_id, deleted, kicked, banned);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new IntPrimitive(this.deleted).write());
		bOut.write(new IntPrimitive(this.kicked).write());
		bOut.write(new IntPrimitive(this.banned).write());
		
		return bOut.toByteArray();
    }
}
