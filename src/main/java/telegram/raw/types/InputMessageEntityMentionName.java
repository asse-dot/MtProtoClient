//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputMessageEntityMentionName extends TlObject {

    public static final int ID = 546203849;

    public int offset;
	public int length;
	public TlObject user_id;

    public InputMessageEntityMentionName(int offset, int length, TlObject user_id) {
        this.offset = offset;
		this.length = length;
		this.user_id = user_id;
    }

    public static InputMessageEntityMentionName read(ByteArrayInputStream bytes) throws IOException {
        int offset = IntPrimitive.read(bytes).getValue();
		int length = IntPrimitive.read(bytes).getValue();
		TlObject user_id = TlObject.read(bytes);
		
		return new InputMessageEntityMentionName(offset, length, user_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.offset).write());
		bOut.write(new IntPrimitive(this.length).write());
		bOut.write(this.user_id.write());
		
		return bOut.toByteArray();
    }
}
