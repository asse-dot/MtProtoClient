//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChatInviteExported extends TlObject {

    public static final int ID = 179611673;

    public boolean revoked;
	public boolean permanent;
	public boolean request_needed;
	public String link;
	public long admin_id;
	public int date;
	public int start_date;
	public int expire_date;
	public int usage_limit;
	public int usage;
	public int requested;
	public String title;

    public ChatInviteExported(boolean revoked, boolean permanent, boolean request_needed, String link, long admin_id, int date, int start_date, int expire_date, int usage_limit, int usage, int requested, String title) {
        this.revoked = revoked;
		this.permanent = permanent;
		this.request_needed = request_needed;
		this.link = link;
		this.admin_id = admin_id;
		this.date = date;
		this.start_date = start_date;
		this.expire_date = expire_date;
		this.usage_limit = usage_limit;
		this.usage = usage;
		this.requested = requested;
		this.title = title;
    }

    public static ChatInviteExported read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean revoked = (flags & (1 << 0)) != 0;
		boolean permanent = (flags & (1 << 5)) != 0;
		boolean request_needed = (flags & (1 << 6)) != 0;
		String link = StringPrimitive.read(bytes).getValue();
		long admin_id = LongPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		int start_date = (flags & (1 << 4)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int expire_date = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int usage_limit = (flags & (1 << 2)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int usage = (flags & (1 << 3)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int requested = (flags & (1 << 7)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String title = (flags & (1 << 8)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new ChatInviteExported(revoked, permanent, request_needed, link, admin_id, date, start_date, expire_date, usage_limit, usage, requested, title);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.revoked ? (1 << 0) : 0;
		flags |= this.permanent ? (1 << 5) : 0;
		flags |= this.request_needed ? (1 << 6) : 0;
		flags |= (this.start_date != 0) ? (1 << 4) : 0;
		flags |= (this.expire_date != 0) ? (1 << 1) : 0;
		flags |= (this.usage_limit != 0) ? (1 << 2) : 0;
		flags |= (this.usage != 0) ? (1 << 3) : 0;
		flags |= (this.requested != 0) ? (1 << 7) : 0;
		flags |= (this.title != null) ? (1 << 8) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.link).write());
		bOut.write(new LongPrimitive(this.admin_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		if(this.start_date != 0)
			bOut.write(new IntPrimitive(this.start_date).write());
		if(this.expire_date != 0)
			bOut.write(new IntPrimitive(this.expire_date).write());
		if(this.usage_limit != 0)
			bOut.write(new IntPrimitive(this.usage_limit).write());
		if(this.usage != 0)
			bOut.write(new IntPrimitive(this.usage).write());
		if(this.requested != 0)
			bOut.write(new IntPrimitive(this.requested).write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		
		return bOut.toByteArray();
    }
}
