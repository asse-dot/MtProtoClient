//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputWebFileGeoPointLocation extends TlObject {

    public static final int ID = -1625153079;

    public TlObject geo_point;
	public long access_hash;
	public int w;
	public int h;
	public int zoom;
	public int scale;

    public InputWebFileGeoPointLocation(TlObject geo_point, long access_hash, int w, int h, int zoom, int scale) {
        this.geo_point = geo_point;
		this.access_hash = access_hash;
		this.w = w;
		this.h = h;
		this.zoom = zoom;
		this.scale = scale;
    }

    public static InputWebFileGeoPointLocation read(ByteArrayInputStream bytes) throws IOException {
        TlObject geo_point = TlObject.read(bytes);
		long access_hash = LongPrimitive.read(bytes).getValue();
		int w = IntPrimitive.read(bytes).getValue();
		int h = IntPrimitive.read(bytes).getValue();
		int zoom = IntPrimitive.read(bytes).getValue();
		int scale = IntPrimitive.read(bytes).getValue();
		
		return new InputWebFileGeoPointLocation(geo_point, access_hash, w, h, zoom, scale);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.geo_point.write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new IntPrimitive(this.w).write());
		bOut.write(new IntPrimitive(this.h).write());
		bOut.write(new IntPrimitive(this.zoom).write());
		bOut.write(new IntPrimitive(this.scale).write());
		
		return bOut.toByteArray();
    }
}
