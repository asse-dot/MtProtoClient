//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateChatParticipantAdd extends TlObject {

    public static final int ID = 1037718609;

    public long chat_id;
	public long user_id;
	public long inviter_id;
	public int date;
	public int version;

    public UpdateChatParticipantAdd(long chat_id, long user_id, long inviter_id, int date, int version) {
        this.chat_id = chat_id;
		this.user_id = user_id;
		this.inviter_id = inviter_id;
		this.date = date;
		this.version = version;
    }

    public static UpdateChatParticipantAdd read(ByteArrayInputStream bytes) throws IOException {
        long chat_id = LongPrimitive.read(bytes).getValue();
		long user_id = LongPrimitive.read(bytes).getValue();
		long inviter_id = LongPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		int version = IntPrimitive.read(bytes).getValue();
		
		return new UpdateChatParticipantAdd(chat_id, user_id, inviter_id, date, version);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.chat_id).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new LongPrimitive(this.inviter_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new IntPrimitive(this.version).write());
		
		return bOut.toByteArray();
    }
}
