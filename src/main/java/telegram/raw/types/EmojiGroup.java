//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EmojiGroup extends TlObject {

    public static final int ID = 2056961449;

    public String title;
	public long icon_emoji_id;
	public ArrayList<TlObject> emoticons;

    public EmojiGroup(String title, long icon_emoji_id, ArrayList<TlObject> emoticons) {
        this.title = title;
		this.icon_emoji_id = icon_emoji_id;
		this.emoticons = emoticons;
    }

    public static EmojiGroup read(ByteArrayInputStream bytes) throws IOException {
        String title = StringPrimitive.read(bytes).getValue();
		long icon_emoji_id = LongPrimitive.read(bytes).getValue();
		ArrayList<TlObject> emoticons = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new EmojiGroup(title, icon_emoji_id, emoticons);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new StringPrimitive(this.title).write());
		bOut.write(new LongPrimitive(this.icon_emoji_id).write());
		bOut.write(new VectorPrimitive(this.emoticons).write());
		
		return bOut.toByteArray();
    }
}
