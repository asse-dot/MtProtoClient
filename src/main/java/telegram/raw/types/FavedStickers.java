//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class FavedStickers extends TlObject {

    public static final int ID = 750063767;

    public long hash;
	public ArrayList<TlObject> packs;
	public ArrayList<TlObject> stickers;

    public FavedStickers(long hash, ArrayList<TlObject> packs, ArrayList<TlObject> stickers) {
        this.hash = hash;
		this.packs = packs;
		this.stickers = stickers;
    }

    public static FavedStickers read(ByteArrayInputStream bytes) throws IOException {
        long hash = LongPrimitive.read(bytes).getValue();
		ArrayList<TlObject> packs = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> stickers = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new FavedStickers(hash, packs, stickers);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.hash).write());
		bOut.write(new VectorPrimitive(this.packs).write());
		bOut.write(new VectorPrimitive(this.stickers).write());
		
		return bOut.toByteArray();
    }
}
