//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RequestPeerTypeUser extends TlObject {

    public static final int ID = 1597737472;

    public TlObject bot;
	public TlObject premium;

    public RequestPeerTypeUser(TlObject bot, TlObject premium) {
        this.bot = bot;
		this.premium = premium;
    }

    public static RequestPeerTypeUser read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject bot = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		TlObject premium = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		
		return new RequestPeerTypeUser(bot, premium);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.bot != null) ? (1 << 0) : 0;
		flags |= (this.premium != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.bot != null)
			bOut.write(this.bot.write());
		if(this.premium != null)
			bOut.write(this.premium.write());
		
		return bOut.toByteArray();
    }
}
