//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChannelAdminLogEventActionParticipantJoinByInvite extends TlObject {

    public static final int ID = -23084712;

    public boolean via_chatlist;
	public TlObject invite;

    public ChannelAdminLogEventActionParticipantJoinByInvite(boolean via_chatlist, TlObject invite) {
        this.via_chatlist = via_chatlist;
		this.invite = invite;
    }

    public static ChannelAdminLogEventActionParticipantJoinByInvite read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean via_chatlist = (flags & (1 << 0)) != 0;
		TlObject invite = TlObject.read(bytes);
		
		return new ChannelAdminLogEventActionParticipantJoinByInvite(via_chatlist, invite);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.via_chatlist ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.invite.write());
		
		return bOut.toByteArray();
    }
}
