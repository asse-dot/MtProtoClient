//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class WallPaperNoFile extends TlObject {

    public static final int ID = -528465642;

    public long id;
	public boolean defaultv;
	public boolean dark;
	public TlObject settings;

    public WallPaperNoFile(long id, boolean defaultv, boolean dark, TlObject settings) {
        this.id = id;
		this.defaultv = defaultv;
		this.dark = dark;
		this.settings = settings;
    }

    public static WallPaperNoFile read(ByteArrayInputStream bytes) throws IOException {
        long id = LongPrimitive.read(bytes).getValue();
		int flags = IntPrimitive.read(bytes).getValue();
		boolean defaultv = (flags & (1 << 1)) != 0;
		boolean dark = (flags & (1 << 4)) != 0;
		TlObject settings = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new WallPaperNoFile(id, defaultv, dark, settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.defaultv ? (1 << 1) : 0;
		flags |= this.dark ? (1 << 4) : 0;
		flags |= (this.settings != null) ? (1 << 2) : 0;
		
		
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new IntPrimitive(flags).write());
		if(this.settings != null)
			bOut.write(this.settings.write());
		
		return bOut.toByteArray();
    }
}
