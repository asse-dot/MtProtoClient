//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class AffectedFoundMessages extends TlObject {

    public static final int ID = -275956116;

    public int pts;
	public int pts_count;
	public int offset;
	public ArrayList<TlObject> messages;

    public AffectedFoundMessages(int pts, int pts_count, int offset, ArrayList<TlObject> messages) {
        this.pts = pts;
		this.pts_count = pts_count;
		this.offset = offset;
		this.messages = messages;
    }

    public static AffectedFoundMessages read(ByteArrayInputStream bytes) throws IOException {
        int pts = IntPrimitive.read(bytes).getValue();
		int pts_count = IntPrimitive.read(bytes).getValue();
		int offset = IntPrimitive.read(bytes).getValue();
		ArrayList<TlObject> messages = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new AffectedFoundMessages(pts, pts_count, offset, messages);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.pts).write());
		bOut.write(new IntPrimitive(this.pts_count).write());
		bOut.write(new IntPrimitive(this.offset).write());
		bOut.write(new VectorPrimitive(this.messages).write());
		
		return bOut.toByteArray();
    }
}
