//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageReplyHeader extends TlObject {

    public static final int ID = -1495959709;

    public boolean reply_to_scheduled;
	public boolean forum_topic;
	public int reply_to_msg_id;
	public TlObject reply_to_peer_id;
	public int reply_to_top_id;

    public MessageReplyHeader(boolean reply_to_scheduled, boolean forum_topic, int reply_to_msg_id, TlObject reply_to_peer_id, int reply_to_top_id) {
        this.reply_to_scheduled = reply_to_scheduled;
		this.forum_topic = forum_topic;
		this.reply_to_msg_id = reply_to_msg_id;
		this.reply_to_peer_id = reply_to_peer_id;
		this.reply_to_top_id = reply_to_top_id;
    }

    public static MessageReplyHeader read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean reply_to_scheduled = (flags & (1 << 2)) != 0;
		boolean forum_topic = (flags & (1 << 3)) != 0;
		int reply_to_msg_id = IntPrimitive.read(bytes).getValue();
		TlObject reply_to_peer_id = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		int reply_to_top_id = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new MessageReplyHeader(reply_to_scheduled, forum_topic, reply_to_msg_id, reply_to_peer_id, reply_to_top_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.reply_to_scheduled ? (1 << 2) : 0;
		flags |= this.forum_topic ? (1 << 3) : 0;
		flags |= (this.reply_to_peer_id != null) ? (1 << 0) : 0;
		flags |= (this.reply_to_top_id != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.reply_to_msg_id).write());
		if(this.reply_to_peer_id != null)
			bOut.write(this.reply_to_peer_id.write());
		if(this.reply_to_top_id != 0)
			bOut.write(new IntPrimitive(this.reply_to_top_id).write());
		
		return bOut.toByteArray();
    }
}
