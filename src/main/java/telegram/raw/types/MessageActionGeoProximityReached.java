//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageActionGeoProximityReached extends TlObject {

    public static final int ID = -1730095465;

    public TlObject from_id;
	public TlObject to_id;
	public int distance;

    public MessageActionGeoProximityReached(TlObject from_id, TlObject to_id, int distance) {
        this.from_id = from_id;
		this.to_id = to_id;
		this.distance = distance;
    }

    public static MessageActionGeoProximityReached read(ByteArrayInputStream bytes) throws IOException {
        TlObject from_id = TlObject.read(bytes);
		TlObject to_id = TlObject.read(bytes);
		int distance = IntPrimitive.read(bytes).getValue();
		
		return new MessageActionGeoProximityReached(from_id, to_id, distance);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.from_id.write());
		bOut.write(this.to_id.write());
		bOut.write(new IntPrimitive(this.distance).write());
		
		return bOut.toByteArray();
    }
}
