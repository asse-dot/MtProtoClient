//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class TmpPassword extends TlObject {

    public static final int ID = -614138572;

    public byte[] tmp_password;
	public int valid_until;

    public TmpPassword(byte[] tmp_password, int valid_until) {
        this.tmp_password = tmp_password;
		this.valid_until = valid_until;
    }

    public static TmpPassword read(ByteArrayInputStream bytes) throws IOException {
        byte[] tmp_password = BytesPrimitive.read(bytes).getValue();
		int valid_until = IntPrimitive.read(bytes).getValue();
		
		return new TmpPassword(tmp_password, valid_until);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new BytesPrimitive(this.tmp_password).write());
		bOut.write(new IntPrimitive(this.valid_until).write());
		
		return bOut.toByteArray();
    }
}
