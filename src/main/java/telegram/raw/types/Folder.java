//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class Folder extends TlObject {

    public static final int ID = -11252123;

    public boolean autofill_new_broadcasts;
	public boolean autofill_public_groups;
	public boolean autofill_new_correspondents;
	public int id;
	public String title;
	public TlObject photo;

    public Folder(boolean autofill_new_broadcasts, boolean autofill_public_groups, boolean autofill_new_correspondents, int id, String title, TlObject photo) {
        this.autofill_new_broadcasts = autofill_new_broadcasts;
		this.autofill_public_groups = autofill_public_groups;
		this.autofill_new_correspondents = autofill_new_correspondents;
		this.id = id;
		this.title = title;
		this.photo = photo;
    }

    public static Folder read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean autofill_new_broadcasts = (flags & (1 << 0)) != 0;
		boolean autofill_public_groups = (flags & (1 << 1)) != 0;
		boolean autofill_new_correspondents = (flags & (1 << 2)) != 0;
		int id = IntPrimitive.read(bytes).getValue();
		String title = StringPrimitive.read(bytes).getValue();
		TlObject photo = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		
		return new Folder(autofill_new_broadcasts, autofill_public_groups, autofill_new_correspondents, id, title, photo);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.autofill_new_broadcasts ? (1 << 0) : 0;
		flags |= this.autofill_public_groups ? (1 << 1) : 0;
		flags |= this.autofill_new_correspondents ? (1 << 2) : 0;
		flags |= (this.photo != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.id).write());
		bOut.write(new StringPrimitive(this.title).write());
		if(this.photo != null)
			bOut.write(this.photo.write());
		
		return bOut.toByteArray();
    }
}
