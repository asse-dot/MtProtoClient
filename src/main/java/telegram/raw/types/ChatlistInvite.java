//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.chatlists;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChatlistInvite extends TlObject {

    public static final int ID = 500007837;

    public String title;
	public String emoticon;
	public ArrayList<TlObject> peers;
	public ArrayList<TlObject> chats;
	public ArrayList<TlObject> users;

    public ChatlistInvite(String title, String emoticon, ArrayList<TlObject> peers, ArrayList<TlObject> chats, ArrayList<TlObject> users) {
        this.title = title;
		this.emoticon = emoticon;
		this.peers = peers;
		this.chats = chats;
		this.users = users;
    }

    public static ChatlistInvite read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String title = StringPrimitive.read(bytes).getValue();
		String emoticon = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		ArrayList<TlObject> peers = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> chats = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> users = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ChatlistInvite(title, emoticon, peers, chats, users);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.emoticon != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.title).write());
		if(this.emoticon != null)
			bOut.write(new StringPrimitive(this.emoticon).write());
		bOut.write(new VectorPrimitive(this.peers).write());
		bOut.write(new VectorPrimitive(this.chats).write());
		bOut.write(new VectorPrimitive(this.users).write());
		
		return bOut.toByteArray();
    }
}
