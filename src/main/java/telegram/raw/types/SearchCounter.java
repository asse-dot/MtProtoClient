//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SearchCounter extends TlObject {

    public static final int ID = -398136321;

    public boolean inexact;
	public TlObject filter;
	public int count;

    public SearchCounter(boolean inexact, TlObject filter, int count) {
        this.inexact = inexact;
		this.filter = filter;
		this.count = count;
    }

    public static SearchCounter read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean inexact = (flags & (1 << 1)) != 0;
		TlObject filter = TlObject.read(bytes);
		int count = IntPrimitive.read(bytes).getValue();
		
		return new SearchCounter(inexact, filter, count);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.inexact ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.filter.write());
		bOut.write(new IntPrimitive(this.count).write());
		
		return bOut.toByteArray();
    }
}
