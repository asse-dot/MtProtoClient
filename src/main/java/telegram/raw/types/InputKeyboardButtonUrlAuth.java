//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputKeyboardButtonUrlAuth extends TlObject {

    public static final int ID = -802258988;

    public boolean request_write_access;
	public String text;
	public String fwd_text;
	public String url;
	public TlObject bot;

    public InputKeyboardButtonUrlAuth(boolean request_write_access, String text, String fwd_text, String url, TlObject bot) {
        this.request_write_access = request_write_access;
		this.text = text;
		this.fwd_text = fwd_text;
		this.url = url;
		this.bot = bot;
    }

    public static InputKeyboardButtonUrlAuth read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean request_write_access = (flags & (1 << 0)) != 0;
		String text = StringPrimitive.read(bytes).getValue();
		String fwd_text = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String url = StringPrimitive.read(bytes).getValue();
		TlObject bot = TlObject.read(bytes);
		
		return new InputKeyboardButtonUrlAuth(request_write_access, text, fwd_text, url, bot);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.request_write_access ? (1 << 0) : 0;
		flags |= (this.fwd_text != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.text).write());
		if(this.fwd_text != null)
			bOut.write(new StringPrimitive(this.fwd_text).write());
		bOut.write(new StringPrimitive(this.url).write());
		bOut.write(this.bot.write());
		
		return bOut.toByteArray();
    }
}
