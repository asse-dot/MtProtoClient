//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DcOption extends TlObject {

    public static final int ID = 414687501;

    public boolean ipv6;
	public boolean media_only;
	public boolean tcpo_only;
	public boolean cdn;
	public boolean staticv;
	public boolean this_port_only;
	public int id;
	public String ip_address;
	public int port;
	public byte[] secret;

    public DcOption(boolean ipv6, boolean media_only, boolean tcpo_only, boolean cdn, boolean staticv, boolean this_port_only, int id, String ip_address, int port, byte[] secret) {
        this.ipv6 = ipv6;
		this.media_only = media_only;
		this.tcpo_only = tcpo_only;
		this.cdn = cdn;
		this.staticv = staticv;
		this.this_port_only = this_port_only;
		this.id = id;
		this.ip_address = ip_address;
		this.port = port;
		this.secret = secret;
    }

    public static DcOption read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean ipv6 = (flags & (1 << 0)) != 0;
		boolean media_only = (flags & (1 << 1)) != 0;
		boolean tcpo_only = (flags & (1 << 2)) != 0;
		boolean cdn = (flags & (1 << 3)) != 0;
		boolean staticv = (flags & (1 << 4)) != 0;
		boolean this_port_only = (flags & (1 << 5)) != 0;
		int id = IntPrimitive.read(bytes).getValue();
		String ip_address = StringPrimitive.read(bytes).getValue();
		int port = IntPrimitive.read(bytes).getValue();
		byte[] secret = (flags & (1 << 10)) != 0 ?  BytesPrimitive.read(bytes).getValue() : null;
		
		return new DcOption(ipv6, media_only, tcpo_only, cdn, staticv, this_port_only, id, ip_address, port, secret);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.ipv6 ? (1 << 0) : 0;
		flags |= this.media_only ? (1 << 1) : 0;
		flags |= this.tcpo_only ? (1 << 2) : 0;
		flags |= this.cdn ? (1 << 3) : 0;
		flags |= this.staticv ? (1 << 4) : 0;
		flags |= this.this_port_only ? (1 << 5) : 0;
		flags |= (this.secret != null) ? (1 << 10) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.id).write());
		bOut.write(new StringPrimitive(this.ip_address).write());
		bOut.write(new IntPrimitive(this.port).write());
		if(this.secret != null)
			bOut.write(new BytesPrimitive(this.secret).write());
		
		return bOut.toByteArray();
    }
}
