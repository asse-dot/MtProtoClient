//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class MessageMediaInvoice extends TlObject {

    public static final int ID = -156940077;

    public boolean shipping_address_requested;
	public boolean test;
	public String title;
	public String description;
	public TlObject photo;
	public int receipt_msg_id;
	public String currency;
	public long total_amount;
	public String start_param;
	public TlObject extended_media;

    public MessageMediaInvoice(boolean shipping_address_requested, boolean test, String title, String description, TlObject photo, int receipt_msg_id, String currency, long total_amount, String start_param, TlObject extended_media) {
        this.shipping_address_requested = shipping_address_requested;
		this.test = test;
		this.title = title;
		this.description = description;
		this.photo = photo;
		this.receipt_msg_id = receipt_msg_id;
		this.currency = currency;
		this.total_amount = total_amount;
		this.start_param = start_param;
		this.extended_media = extended_media;
    }

    public static MessageMediaInvoice read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean shipping_address_requested = (flags & (1 << 1)) != 0;
		boolean test = (flags & (1 << 3)) != 0;
		String title = StringPrimitive.read(bytes).getValue();
		String description = StringPrimitive.read(bytes).getValue();
		TlObject photo = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		int receipt_msg_id = (flags & (1 << 2)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String currency = StringPrimitive.read(bytes).getValue();
		long total_amount = LongPrimitive.read(bytes).getValue();
		String start_param = StringPrimitive.read(bytes).getValue();
		TlObject extended_media = (flags & (1 << 4)) != 0 ?  TlObject.read(bytes) : null;
		
		return new MessageMediaInvoice(shipping_address_requested, test, title, description, photo, receipt_msg_id, currency, total_amount, start_param, extended_media);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.shipping_address_requested ? (1 << 1) : 0;
		flags |= this.test ? (1 << 3) : 0;
		flags |= (this.photo != null) ? (1 << 0) : 0;
		flags |= (this.receipt_msg_id != 0) ? (1 << 2) : 0;
		flags |= (this.extended_media != null) ? (1 << 4) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.title).write());
		bOut.write(new StringPrimitive(this.description).write());
		if(this.photo != null)
			bOut.write(this.photo.write());
		if(this.receipt_msg_id != 0)
			bOut.write(new IntPrimitive(this.receipt_msg_id).write());
		bOut.write(new StringPrimitive(this.currency).write());
		bOut.write(new LongPrimitive(this.total_amount).write());
		bOut.write(new StringPrimitive(this.start_param).write());
		if(this.extended_media != null)
			bOut.write(this.extended_media.write());
		
		return bOut.toByteArray();
    }
}
