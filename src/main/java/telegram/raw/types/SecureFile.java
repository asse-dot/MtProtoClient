//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SecureFile extends TlObject {

    public static final int ID = 2097791614;

    public long id;
	public long access_hash;
	public long size;
	public int dc_id;
	public int date;
	public byte[] file_hash;
	public byte[] secret;

    public SecureFile(long id, long access_hash, long size, int dc_id, int date, byte[] file_hash, byte[] secret) {
        this.id = id;
		this.access_hash = access_hash;
		this.size = size;
		this.dc_id = dc_id;
		this.date = date;
		this.file_hash = file_hash;
		this.secret = secret;
    }

    public static SecureFile read(ByteArrayInputStream bytes) throws IOException {
        long id = LongPrimitive.read(bytes).getValue();
		long access_hash = LongPrimitive.read(bytes).getValue();
		long size = LongPrimitive.read(bytes).getValue();
		int dc_id = IntPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		byte[] file_hash = BytesPrimitive.read(bytes).getValue();
		byte[] secret = BytesPrimitive.read(bytes).getValue();
		
		return new SecureFile(id, access_hash, size, dc_id, date, file_hash, secret);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new LongPrimitive(this.size).write());
		bOut.write(new IntPrimitive(this.dc_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new BytesPrimitive(this.file_hash).write());
		bOut.write(new BytesPrimitive(this.secret).write());
		
		return bOut.toByteArray();
    }
}
