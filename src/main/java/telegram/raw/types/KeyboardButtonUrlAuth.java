//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class KeyboardButtonUrlAuth extends TlObject {

    public static final int ID = 280464681;

    public String text;
	public String fwd_text;
	public String url;
	public int button_id;

    public KeyboardButtonUrlAuth(String text, String fwd_text, String url, int button_id) {
        this.text = text;
		this.fwd_text = fwd_text;
		this.url = url;
		this.button_id = button_id;
    }

    public static KeyboardButtonUrlAuth read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String text = StringPrimitive.read(bytes).getValue();
		String fwd_text = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String url = StringPrimitive.read(bytes).getValue();
		int button_id = IntPrimitive.read(bytes).getValue();
		
		return new KeyboardButtonUrlAuth(text, fwd_text, url, button_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.fwd_text != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.text).write());
		if(this.fwd_text != null)
			bOut.write(new StringPrimitive(this.fwd_text).write());
		bOut.write(new StringPrimitive(this.url).write());
		bOut.write(new IntPrimitive(this.button_id).write());
		
		return bOut.toByteArray();
    }
}
