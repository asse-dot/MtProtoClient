//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChannelParticipantSelf extends TlObject {

    public static final int ID = 900251559;

    public boolean via_request;
	public long user_id;
	public long inviter_id;
	public int date;

    public ChannelParticipantSelf(boolean via_request, long user_id, long inviter_id, int date) {
        this.via_request = via_request;
		this.user_id = user_id;
		this.inviter_id = inviter_id;
		this.date = date;
    }

    public static ChannelParticipantSelf read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean via_request = (flags & (1 << 0)) != 0;
		long user_id = LongPrimitive.read(bytes).getValue();
		long inviter_id = LongPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		
		return new ChannelParticipantSelf(via_request, user_id, inviter_id, date);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.via_request ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new LongPrimitive(this.inviter_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		
		return bOut.toByteArray();
    }
}
