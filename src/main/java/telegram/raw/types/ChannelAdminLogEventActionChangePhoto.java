//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChannelAdminLogEventActionChangePhoto extends TlObject {

    public static final int ID = 1129042607;

    public TlObject prev_photo;
	public TlObject new_photo;

    public ChannelAdminLogEventActionChangePhoto(TlObject prev_photo, TlObject new_photo) {
        this.prev_photo = prev_photo;
		this.new_photo = new_photo;
    }

    public static ChannelAdminLogEventActionChangePhoto read(ByteArrayInputStream bytes) throws IOException {
        TlObject prev_photo = TlObject.read(bytes);
		TlObject new_photo = TlObject.read(bytes);
		
		return new ChannelAdminLogEventActionChangePhoto(prev_photo, new_photo);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.prev_photo.write());
		bOut.write(this.new_photo.write());
		
		return bOut.toByteArray();
    }
}
