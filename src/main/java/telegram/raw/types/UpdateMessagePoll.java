//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateMessagePoll extends TlObject {

    public static final int ID = -1398708869;

    public long poll_id;
	public TlObject poll;
	public TlObject results;

    public UpdateMessagePoll(long poll_id, TlObject poll, TlObject results) {
        this.poll_id = poll_id;
		this.poll = poll;
		this.results = results;
    }

    public static UpdateMessagePoll read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long poll_id = LongPrimitive.read(bytes).getValue();
		TlObject poll = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		TlObject results = TlObject.read(bytes);
		
		return new UpdateMessagePoll(poll_id, poll, results);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.poll != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.poll_id).write());
		if(this.poll != null)
			bOut.write(this.poll.write());
		bOut.write(this.results.write());
		
		return bOut.toByteArray();
    }
}
