//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class StickerSetFullCovered extends TlObject {

    public static final int ID = 1087454222;

    public TlObject set;
	public ArrayList<TlObject> packs;
	public ArrayList<TlObject> keywords;
	public ArrayList<TlObject> documents;

    public StickerSetFullCovered(TlObject set, ArrayList<TlObject> packs, ArrayList<TlObject> keywords, ArrayList<TlObject> documents) {
        this.set = set;
		this.packs = packs;
		this.keywords = keywords;
		this.documents = documents;
    }

    public static StickerSetFullCovered read(ByteArrayInputStream bytes) throws IOException {
        TlObject set = TlObject.read(bytes);
		ArrayList<TlObject> packs = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> keywords = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> documents = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new StickerSetFullCovered(set, packs, keywords, documents);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.set.write());
		bOut.write(new VectorPrimitive(this.packs).write());
		bOut.write(new VectorPrimitive(this.keywords).write());
		bOut.write(new VectorPrimitive(this.documents).write());
		
		return bOut.toByteArray();
    }
}
