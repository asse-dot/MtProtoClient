//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateBotPrecheckoutQuery extends TlObject {

    public static final int ID = -1934976362;

    public long query_id;
	public long user_id;
	public byte[] payload;
	public TlObject info;
	public String shipping_option_id;
	public String currency;
	public long total_amount;

    public UpdateBotPrecheckoutQuery(long query_id, long user_id, byte[] payload, TlObject info, String shipping_option_id, String currency, long total_amount) {
        this.query_id = query_id;
		this.user_id = user_id;
		this.payload = payload;
		this.info = info;
		this.shipping_option_id = shipping_option_id;
		this.currency = currency;
		this.total_amount = total_amount;
    }

    public static UpdateBotPrecheckoutQuery read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long query_id = LongPrimitive.read(bytes).getValue();
		long user_id = LongPrimitive.read(bytes).getValue();
		byte[] payload = BytesPrimitive.read(bytes).getValue();
		TlObject info = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		String shipping_option_id = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String currency = StringPrimitive.read(bytes).getValue();
		long total_amount = LongPrimitive.read(bytes).getValue();
		
		return new UpdateBotPrecheckoutQuery(query_id, user_id, payload, info, shipping_option_id, currency, total_amount);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.info != null) ? (1 << 0) : 0;
		flags |= (this.shipping_option_id != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.query_id).write());
		bOut.write(new LongPrimitive(this.user_id).write());
		bOut.write(new BytesPrimitive(this.payload).write());
		if(this.info != null)
			bOut.write(this.info.write());
		if(this.shipping_option_id != null)
			bOut.write(new StringPrimitive(this.shipping_option_id).write());
		bOut.write(new StringPrimitive(this.currency).write());
		bOut.write(new LongPrimitive(this.total_amount).write());
		
		return bOut.toByteArray();
    }
}
