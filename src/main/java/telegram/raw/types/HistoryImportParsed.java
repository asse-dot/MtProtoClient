//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class HistoryImportParsed extends TlObject {

    public static final int ID = 1578088377;

    public boolean pm;
	public boolean group;
	public String title;

    public HistoryImportParsed(boolean pm, boolean group, String title) {
        this.pm = pm;
		this.group = group;
		this.title = title;
    }

    public static HistoryImportParsed read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean pm = (flags & (1 << 0)) != 0;
		boolean group = (flags & (1 << 1)) != 0;
		String title = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new HistoryImportParsed(pm, group, title);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.pm ? (1 << 0) : 0;
		flags |= this.group ? (1 << 1) : 0;
		flags |= (this.title != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		
		return bOut.toByteArray();
    }
}
