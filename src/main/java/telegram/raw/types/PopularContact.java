//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PopularContact extends TlObject {

    public static final int ID = 1558266229;

    public long client_id;
	public int importers;

    public PopularContact(long client_id, int importers) {
        this.client_id = client_id;
		this.importers = importers;
    }

    public static PopularContact read(ByteArrayInputStream bytes) throws IOException {
        long client_id = LongPrimitive.read(bytes).getValue();
		int importers = IntPrimitive.read(bytes).getValue();
		
		return new PopularContact(client_id, importers);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.client_id).write());
		bOut.write(new IntPrimitive(this.importers).write());
		
		return bOut.toByteArray();
    }
}
