//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EncryptedMessageService extends TlObject {

    public static final int ID = 594758406;

    public long random_id;
	public int chat_id;
	public int date;
	public byte[] bytesv;

    public EncryptedMessageService(long random_id, int chat_id, int date, byte[] bytesv) {
        this.random_id = random_id;
		this.chat_id = chat_id;
		this.date = date;
		this.bytesv = bytesv;
    }

    public static EncryptedMessageService read(ByteArrayInputStream bytes) throws IOException {
        long random_id = LongPrimitive.read(bytes).getValue();
		int chat_id = IntPrimitive.read(bytes).getValue();
		int date = IntPrimitive.read(bytes).getValue();
		byte[] bytesv = BytesPrimitive.read(bytes).getValue();
		
		return new EncryptedMessageService(random_id, chat_id, date, bytesv);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.random_id).write());
		bOut.write(new IntPrimitive(this.chat_id).write());
		bOut.write(new IntPrimitive(this.date).write());
		bOut.write(new BytesPrimitive(this.bytesv).write());
		
		return bOut.toByteArray();
    }
}
