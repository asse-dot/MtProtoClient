//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class PaymentRequestedInfo extends TlObject {

    public static final int ID = -1868808300;

    public String name;
	public String phone;
	public String email;
	public TlObject shipping_address;

    public PaymentRequestedInfo(String name, String phone, String email, TlObject shipping_address) {
        this.name = name;
		this.phone = phone;
		this.email = email;
		this.shipping_address = shipping_address;
    }

    public static PaymentRequestedInfo read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String name = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String phone = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String email = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject shipping_address = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		
		return new PaymentRequestedInfo(name, phone, email, shipping_address);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.name != null) ? (1 << 0) : 0;
		flags |= (this.phone != null) ? (1 << 1) : 0;
		flags |= (this.email != null) ? (1 << 2) : 0;
		flags |= (this.shipping_address != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.name != null)
			bOut.write(new StringPrimitive(this.name).write());
		if(this.phone != null)
			bOut.write(new StringPrimitive(this.phone).write());
		if(this.email != null)
			bOut.write(new StringPrimitive(this.email).write());
		if(this.shipping_address != null)
			bOut.write(this.shipping_address.write());
		
		return bOut.toByteArray();
    }
}
