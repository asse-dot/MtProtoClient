//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class Game extends TlObject {

    public static final int ID = -1107729093;

    public long id;
	public long access_hash;
	public String short_name;
	public String title;
	public String description;
	public TlObject photo;
	public TlObject document;

    public Game(long id, long access_hash, String short_name, String title, String description, TlObject photo, TlObject document) {
        this.id = id;
		this.access_hash = access_hash;
		this.short_name = short_name;
		this.title = title;
		this.description = description;
		this.photo = photo;
		this.document = document;
    }

    public static Game read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long id = LongPrimitive.read(bytes).getValue();
		long access_hash = LongPrimitive.read(bytes).getValue();
		String short_name = StringPrimitive.read(bytes).getValue();
		String title = StringPrimitive.read(bytes).getValue();
		String description = StringPrimitive.read(bytes).getValue();
		TlObject photo = TlObject.read(bytes);
		TlObject document = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new Game(id, access_hash, short_name, title, description, photo, document);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.document != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new LongPrimitive(this.access_hash).write());
		bOut.write(new StringPrimitive(this.short_name).write());
		bOut.write(new StringPrimitive(this.title).write());
		bOut.write(new StringPrimitive(this.description).write());
		bOut.write(this.photo.write());
		if(this.document != null)
			bOut.write(this.document.write());
		
		return bOut.toByteArray();
    }
}
