//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class WebAuthorization extends TlObject {

    public static final int ID = -1493633966;

    public long hash;
	public long bot_id;
	public String domain;
	public String browser;
	public String platform;
	public int date_created;
	public int date_active;
	public String ip;
	public String region;

    public WebAuthorization(long hash, long bot_id, String domain, String browser, String platform, int date_created, int date_active, String ip, String region) {
        this.hash = hash;
		this.bot_id = bot_id;
		this.domain = domain;
		this.browser = browser;
		this.platform = platform;
		this.date_created = date_created;
		this.date_active = date_active;
		this.ip = ip;
		this.region = region;
    }

    public static WebAuthorization read(ByteArrayInputStream bytes) throws IOException {
        long hash = LongPrimitive.read(bytes).getValue();
		long bot_id = LongPrimitive.read(bytes).getValue();
		String domain = StringPrimitive.read(bytes).getValue();
		String browser = StringPrimitive.read(bytes).getValue();
		String platform = StringPrimitive.read(bytes).getValue();
		int date_created = IntPrimitive.read(bytes).getValue();
		int date_active = IntPrimitive.read(bytes).getValue();
		String ip = StringPrimitive.read(bytes).getValue();
		String region = StringPrimitive.read(bytes).getValue();
		
		return new WebAuthorization(hash, bot_id, domain, browser, platform, date_created, date_active, ip, region);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.hash).write());
		bOut.write(new LongPrimitive(this.bot_id).write());
		bOut.write(new StringPrimitive(this.domain).write());
		bOut.write(new StringPrimitive(this.browser).write());
		bOut.write(new StringPrimitive(this.platform).write());
		bOut.write(new IntPrimitive(this.date_created).write());
		bOut.write(new IntPrimitive(this.date_active).write());
		bOut.write(new StringPrimitive(this.ip).write());
		bOut.write(new StringPrimitive(this.region).write());
		
		return bOut.toByteArray();
    }
}
