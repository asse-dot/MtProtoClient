//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.types;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InputEncryptedFileUploaded extends TlObject {

    public static final int ID = 1690108678;

    public long id;
	public int parts;
	public String md5_checksum;
	public int key_fingerprint;

    public InputEncryptedFileUploaded(long id, int parts, String md5_checksum, int key_fingerprint) {
        this.id = id;
		this.parts = parts;
		this.md5_checksum = md5_checksum;
		this.key_fingerprint = key_fingerprint;
    }

    public static InputEncryptedFileUploaded read(ByteArrayInputStream bytes) throws IOException {
        long id = LongPrimitive.read(bytes).getValue();
		int parts = IntPrimitive.read(bytes).getValue();
		String md5_checksum = StringPrimitive.read(bytes).getValue();
		int key_fingerprint = IntPrimitive.read(bytes).getValue();
		
		return new InputEncryptedFileUploaded(id, parts, md5_checksum, key_fingerprint);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.id).write());
		bOut.write(new IntPrimitive(this.parts).write());
		bOut.write(new StringPrimitive(this.md5_checksum).write());
		bOut.write(new IntPrimitive(this.key_fingerprint).write());
		
		return bOut.toByteArray();
    }
}
