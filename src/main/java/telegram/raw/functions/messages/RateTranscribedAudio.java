//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RateTranscribedAudio extends TlObject {

    public static final int ID = 2132608815;

    public TlObject peer;
	public int msg_id;
	public long transcription_id;
	public TlObject good;

    public RateTranscribedAudio(TlObject peer, int msg_id, long transcription_id, TlObject good) {
        this.peer = peer;
		this.msg_id = msg_id;
		this.transcription_id = transcription_id;
		this.good = good;
    }

    public static RateTranscribedAudio read(ByteArrayInputStream bytes) throws IOException {
        TlObject peer = TlObject.read(bytes);
		int msg_id = IntPrimitive.read(bytes).getValue();
		long transcription_id = LongPrimitive.read(bytes).getValue();
		TlObject good = TlObject.read(bytes);
		
		return new RateTranscribedAudio(peer, msg_id, transcription_id, good);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.msg_id).write());
		bOut.write(new LongPrimitive(this.transcription_id).write());
		bOut.write(this.good.write());
		
		return bOut.toByteArray();
    }
}
