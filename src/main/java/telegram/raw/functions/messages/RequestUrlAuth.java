//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RequestUrlAuth extends TlObject {

    public static final int ID = 428848198;

    public TlObject peer;
	public int msg_id;
	public int button_id;
	public String url;

    public RequestUrlAuth(TlObject peer, int msg_id, int button_id, String url) {
        this.peer = peer;
		this.msg_id = msg_id;
		this.button_id = button_id;
		this.url = url;
    }

    public static RequestUrlAuth read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject peer = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		int msg_id = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int button_id = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String url = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new RequestUrlAuth(peer, msg_id, button_id, url);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.peer != null) ? (1 << 1) : 0;
		flags |= (this.msg_id != 0) ? (1 << 1) : 0;
		flags |= (this.button_id != 0) ? (1 << 1) : 0;
		flags |= (this.url != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.peer != null)
			bOut.write(this.peer.write());
		if(this.msg_id != 0)
			bOut.write(new IntPrimitive(this.msg_id).write());
		if(this.button_id != 0)
			bOut.write(new IntPrimitive(this.button_id).write());
		if(this.url != null)
			bOut.write(new StringPrimitive(this.url).write());
		
		return bOut.toByteArray();
    }
}
