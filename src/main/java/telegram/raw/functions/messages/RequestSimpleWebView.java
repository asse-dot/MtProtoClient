//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RequestSimpleWebView extends TlObject {

    public static final int ID = 698084494;

    public boolean from_switch_webview;
	public TlObject bot;
	public String url;
	public TlObject theme_params;
	public String platform;

    public RequestSimpleWebView(boolean from_switch_webview, TlObject bot, String url, TlObject theme_params, String platform) {
        this.from_switch_webview = from_switch_webview;
		this.bot = bot;
		this.url = url;
		this.theme_params = theme_params;
		this.platform = platform;
    }

    public static RequestSimpleWebView read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean from_switch_webview = (flags & (1 << 1)) != 0;
		TlObject bot = TlObject.read(bytes);
		String url = StringPrimitive.read(bytes).getValue();
		TlObject theme_params = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		String platform = StringPrimitive.read(bytes).getValue();
		
		return new RequestSimpleWebView(from_switch_webview, bot, url, theme_params, platform);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.from_switch_webview ? (1 << 1) : 0;
		flags |= (this.theme_params != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.bot.write());
		bOut.write(new StringPrimitive(this.url).write());
		if(this.theme_params != null)
			bOut.write(this.theme_params.write());
		bOut.write(new StringPrimitive(this.platform).write());
		
		return bOut.toByteArray();
    }
}
