//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ToggleStickerSets extends TlObject {

    public static final int ID = -1257951254;

    public boolean uninstall;
	public boolean archive;
	public boolean unarchive;
	public ArrayList<TlObject> stickersets;

    public ToggleStickerSets(boolean uninstall, boolean archive, boolean unarchive, ArrayList<TlObject> stickersets) {
        this.uninstall = uninstall;
		this.archive = archive;
		this.unarchive = unarchive;
		this.stickersets = stickersets;
    }

    public static ToggleStickerSets read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean uninstall = (flags & (1 << 0)) != 0;
		boolean archive = (flags & (1 << 1)) != 0;
		boolean unarchive = (flags & (1 << 2)) != 0;
		ArrayList<TlObject> stickersets = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ToggleStickerSets(uninstall, archive, unarchive, stickersets);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.uninstall ? (1 << 0) : 0;
		flags |= this.archive ? (1 << 1) : 0;
		flags |= this.unarchive ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new VectorPrimitive(this.stickersets).write());
		
		return bOut.toByteArray();
    }
}
