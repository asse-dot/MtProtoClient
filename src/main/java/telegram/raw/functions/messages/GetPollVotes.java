//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetPollVotes extends TlObject {

    public static final int ID = -1200736242;

    public TlObject peer;
	public int id;
	public byte[] option;
	public String offset;
	public int limit;

    public GetPollVotes(TlObject peer, int id, byte[] option, String offset, int limit) {
        this.peer = peer;
		this.id = id;
		this.option = option;
		this.offset = offset;
		this.limit = limit;
    }

    public static GetPollVotes read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject peer = TlObject.read(bytes);
		int id = IntPrimitive.read(bytes).getValue();
		byte[] option = (flags & (1 << 0)) != 0 ?  BytesPrimitive.read(bytes).getValue() : null;
		String offset = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		int limit = IntPrimitive.read(bytes).getValue();
		
		return new GetPollVotes(peer, id, option, offset, limit);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.option != null) ? (1 << 0) : 0;
		flags |= (this.offset != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.id).write());
		if(this.option != null)
			bOut.write(new BytesPrimitive(this.option).write());
		if(this.offset != null)
			bOut.write(new StringPrimitive(this.offset).write());
		bOut.write(new IntPrimitive(this.limit).write());
		
		return bOut.toByteArray();
    }
}
