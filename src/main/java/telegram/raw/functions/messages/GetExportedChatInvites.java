//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetExportedChatInvites extends TlObject {

    public static final int ID = -1565154314;

    public boolean revoked;
	public TlObject peer;
	public TlObject admin_id;
	public int offset_date;
	public String offset_link;
	public int limit;

    public GetExportedChatInvites(boolean revoked, TlObject peer, TlObject admin_id, int offset_date, String offset_link, int limit) {
        this.revoked = revoked;
		this.peer = peer;
		this.admin_id = admin_id;
		this.offset_date = offset_date;
		this.offset_link = offset_link;
		this.limit = limit;
    }

    public static GetExportedChatInvites read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean revoked = (flags & (1 << 3)) != 0;
		TlObject peer = TlObject.read(bytes);
		TlObject admin_id = TlObject.read(bytes);
		int offset_date = (flags & (1 << 2)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String offset_link = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		int limit = IntPrimitive.read(bytes).getValue();
		
		return new GetExportedChatInvites(revoked, peer, admin_id, offset_date, offset_link, limit);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.revoked ? (1 << 3) : 0;
		flags |= (this.offset_date != 0) ? (1 << 2) : 0;
		flags |= (this.offset_link != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(this.admin_id.write());
		if(this.offset_date != 0)
			bOut.write(new IntPrimitive(this.offset_date).write());
		if(this.offset_link != null)
			bOut.write(new StringPrimitive(this.offset_link).write());
		bOut.write(new IntPrimitive(this.limit).write());
		
		return bOut.toByteArray();
    }
}
