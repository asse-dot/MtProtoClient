//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SetGameScore extends TlObject {

    public static final int ID = -1896289088;

    public boolean edit_message;
	public boolean force;
	public TlObject peer;
	public int id;
	public TlObject user_id;
	public int score;

    public SetGameScore(boolean edit_message, boolean force, TlObject peer, int id, TlObject user_id, int score) {
        this.edit_message = edit_message;
		this.force = force;
		this.peer = peer;
		this.id = id;
		this.user_id = user_id;
		this.score = score;
    }

    public static SetGameScore read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean edit_message = (flags & (1 << 0)) != 0;
		boolean force = (flags & (1 << 1)) != 0;
		TlObject peer = TlObject.read(bytes);
		int id = IntPrimitive.read(bytes).getValue();
		TlObject user_id = TlObject.read(bytes);
		int score = IntPrimitive.read(bytes).getValue();
		
		return new SetGameScore(edit_message, force, peer, id, user_id, score);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.edit_message ? (1 << 0) : 0;
		flags |= this.force ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.id).write());
		bOut.write(this.user_id.write());
		bOut.write(new IntPrimitive(this.score).write());
		
		return bOut.toByteArray();
    }
}
