//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DeleteChatUser extends TlObject {

    public static final int ID = -1575461717;

    public boolean revoke_history;
	public long chat_id;
	public TlObject user_id;

    public DeleteChatUser(boolean revoke_history, long chat_id, TlObject user_id) {
        this.revoke_history = revoke_history;
		this.chat_id = chat_id;
		this.user_id = user_id;
    }

    public static DeleteChatUser read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean revoke_history = (flags & (1 << 0)) != 0;
		long chat_id = LongPrimitive.read(bytes).getValue();
		TlObject user_id = TlObject.read(bytes);
		
		return new DeleteChatUser(revoke_history, chat_id, user_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.revoke_history ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.chat_id).write());
		bOut.write(this.user_id.write());
		
		return bOut.toByteArray();
    }
}
