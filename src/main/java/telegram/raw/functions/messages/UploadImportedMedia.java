//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UploadImportedMedia extends TlObject {

    public static final int ID = 713433234;

    public TlObject peer;
	public long import_id;
	public String file_name;
	public TlObject media;

    public UploadImportedMedia(TlObject peer, long import_id, String file_name, TlObject media) {
        this.peer = peer;
		this.import_id = import_id;
		this.file_name = file_name;
		this.media = media;
    }

    public static UploadImportedMedia read(ByteArrayInputStream bytes) throws IOException {
        TlObject peer = TlObject.read(bytes);
		long import_id = LongPrimitive.read(bytes).getValue();
		String file_name = StringPrimitive.read(bytes).getValue();
		TlObject media = TlObject.read(bytes);
		
		return new UploadImportedMedia(peer, import_id, file_name, media);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.peer.write());
		bOut.write(new LongPrimitive(this.import_id).write());
		bOut.write(new StringPrimitive(this.file_name).write());
		bOut.write(this.media.write());
		
		return bOut.toByteArray();
    }
}
