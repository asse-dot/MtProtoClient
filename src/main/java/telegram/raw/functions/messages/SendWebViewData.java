//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SendWebViewData extends TlObject {

    public static final int ID = -603831608;

    public TlObject bot;
	public long random_id;
	public String button_text;
	public String data;

    public SendWebViewData(TlObject bot, long random_id, String button_text, String data) {
        this.bot = bot;
		this.random_id = random_id;
		this.button_text = button_text;
		this.data = data;
    }

    public static SendWebViewData read(ByteArrayInputStream bytes) throws IOException {
        TlObject bot = TlObject.read(bytes);
		long random_id = LongPrimitive.read(bytes).getValue();
		String button_text = StringPrimitive.read(bytes).getValue();
		String data = StringPrimitive.read(bytes).getValue();
		
		return new SendWebViewData(bot, random_id, button_text, data);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.bot.write());
		bOut.write(new LongPrimitive(this.random_id).write());
		bOut.write(new StringPrimitive(this.button_text).write());
		bOut.write(new StringPrimitive(this.data).write());
		
		return bOut.toByteArray();
    }
}
