//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdatePinnedMessage extends TlObject {

    public static final int ID = -760547348;

    public boolean silent;
	public boolean unpin;
	public boolean pm_oneside;
	public TlObject peer;
	public int id;

    public UpdatePinnedMessage(boolean silent, boolean unpin, boolean pm_oneside, TlObject peer, int id) {
        this.silent = silent;
		this.unpin = unpin;
		this.pm_oneside = pm_oneside;
		this.peer = peer;
		this.id = id;
    }

    public static UpdatePinnedMessage read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean silent = (flags & (1 << 0)) != 0;
		boolean unpin = (flags & (1 << 1)) != 0;
		boolean pm_oneside = (flags & (1 << 2)) != 0;
		TlObject peer = TlObject.read(bytes);
		int id = IntPrimitive.read(bytes).getValue();
		
		return new UpdatePinnedMessage(silent, unpin, pm_oneside, peer, id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.silent ? (1 << 0) : 0;
		flags |= this.unpin ? (1 << 1) : 0;
		flags |= this.pm_oneside ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.id).write());
		
		return bOut.toByteArray();
    }
}
