//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SendBotRequestedPeer extends TlObject {

    public static final int ID = -29831141;

    public TlObject peer;
	public int msg_id;
	public int button_id;
	public TlObject requested_peer;

    public SendBotRequestedPeer(TlObject peer, int msg_id, int button_id, TlObject requested_peer) {
        this.peer = peer;
		this.msg_id = msg_id;
		this.button_id = button_id;
		this.requested_peer = requested_peer;
    }

    public static SendBotRequestedPeer read(ByteArrayInputStream bytes) throws IOException {
        TlObject peer = TlObject.read(bytes);
		int msg_id = IntPrimitive.read(bytes).getValue();
		int button_id = IntPrimitive.read(bytes).getValue();
		TlObject requested_peer = TlObject.read(bytes);
		
		return new SendBotRequestedPeer(peer, msg_id, button_id, requested_peer);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.msg_id).write());
		bOut.write(new IntPrimitive(this.button_id).write());
		bOut.write(this.requested_peer.write());
		
		return bOut.toByteArray();
    }
}
