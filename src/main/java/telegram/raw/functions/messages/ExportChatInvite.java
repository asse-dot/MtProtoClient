//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ExportChatInvite extends TlObject {

    public static final int ID = -1607670315;

    public boolean legacy_revoke_permanent;
	public boolean request_needed;
	public TlObject peer;
	public int expire_date;
	public int usage_limit;
	public String title;

    public ExportChatInvite(boolean legacy_revoke_permanent, boolean request_needed, TlObject peer, int expire_date, int usage_limit, String title) {
        this.legacy_revoke_permanent = legacy_revoke_permanent;
		this.request_needed = request_needed;
		this.peer = peer;
		this.expire_date = expire_date;
		this.usage_limit = usage_limit;
		this.title = title;
    }

    public static ExportChatInvite read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean legacy_revoke_permanent = (flags & (1 << 2)) != 0;
		boolean request_needed = (flags & (1 << 3)) != 0;
		TlObject peer = TlObject.read(bytes);
		int expire_date = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int usage_limit = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		String title = (flags & (1 << 4)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new ExportChatInvite(legacy_revoke_permanent, request_needed, peer, expire_date, usage_limit, title);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.legacy_revoke_permanent ? (1 << 2) : 0;
		flags |= this.request_needed ? (1 << 3) : 0;
		flags |= (this.expire_date != 0) ? (1 << 0) : 0;
		flags |= (this.usage_limit != 0) ? (1 << 1) : 0;
		flags |= (this.title != null) ? (1 << 4) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		if(this.expire_date != 0)
			bOut.write(new IntPrimitive(this.expire_date).write());
		if(this.usage_limit != 0)
			bOut.write(new IntPrimitive(this.usage_limit).write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		
		return bOut.toByteArray();
    }
}
