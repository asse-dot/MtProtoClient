//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SendScreenshotNotification extends TlObject {

    public static final int ID = -914493408;

    public TlObject peer;
	public int reply_to_msg_id;
	public long random_id;

    public SendScreenshotNotification(TlObject peer, int reply_to_msg_id, long random_id) {
        this.peer = peer;
		this.reply_to_msg_id = reply_to_msg_id;
		this.random_id = random_id;
    }

    public static SendScreenshotNotification read(ByteArrayInputStream bytes) throws IOException {
        TlObject peer = TlObject.read(bytes);
		int reply_to_msg_id = IntPrimitive.read(bytes).getValue();
		long random_id = LongPrimitive.read(bytes).getValue();
		
		return new SendScreenshotNotification(peer, reply_to_msg_id, random_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.reply_to_msg_id).write());
		bOut.write(new LongPrimitive(this.random_id).write());
		
		return bOut.toByteArray();
    }
}
