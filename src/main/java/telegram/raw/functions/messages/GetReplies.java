//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetReplies extends TlObject {

    public static final int ID = 584962828;

    public TlObject peer;
	public int msg_id;
	public int offset_id;
	public int offset_date;
	public int add_offset;
	public int limit;
	public int max_id;
	public int min_id;
	public long hash;

    public GetReplies(TlObject peer, int msg_id, int offset_id, int offset_date, int add_offset, int limit, int max_id, int min_id, long hash) {
        this.peer = peer;
		this.msg_id = msg_id;
		this.offset_id = offset_id;
		this.offset_date = offset_date;
		this.add_offset = add_offset;
		this.limit = limit;
		this.max_id = max_id;
		this.min_id = min_id;
		this.hash = hash;
    }

    public static GetReplies read(ByteArrayInputStream bytes) throws IOException {
        TlObject peer = TlObject.read(bytes);
		int msg_id = IntPrimitive.read(bytes).getValue();
		int offset_id = IntPrimitive.read(bytes).getValue();
		int offset_date = IntPrimitive.read(bytes).getValue();
		int add_offset = IntPrimitive.read(bytes).getValue();
		int limit = IntPrimitive.read(bytes).getValue();
		int max_id = IntPrimitive.read(bytes).getValue();
		int min_id = IntPrimitive.read(bytes).getValue();
		long hash = LongPrimitive.read(bytes).getValue();
		
		return new GetReplies(peer, msg_id, offset_id, offset_date, add_offset, limit, max_id, min_id, hash);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.msg_id).write());
		bOut.write(new IntPrimitive(this.offset_id).write());
		bOut.write(new IntPrimitive(this.offset_date).write());
		bOut.write(new IntPrimitive(this.add_offset).write());
		bOut.write(new IntPrimitive(this.limit).write());
		bOut.write(new IntPrimitive(this.max_id).write());
		bOut.write(new IntPrimitive(this.min_id).write());
		bOut.write(new LongPrimitive(this.hash).write());
		
		return bOut.toByteArray();
    }
}
