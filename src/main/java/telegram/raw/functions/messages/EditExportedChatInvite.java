//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EditExportedChatInvite extends TlObject {

    public static final int ID = -1110823051;

    public boolean revoked;
	public TlObject peer;
	public String link;
	public int expire_date;
	public int usage_limit;
	public TlObject request_needed;
	public String title;

    public EditExportedChatInvite(boolean revoked, TlObject peer, String link, int expire_date, int usage_limit, TlObject request_needed, String title) {
        this.revoked = revoked;
		this.peer = peer;
		this.link = link;
		this.expire_date = expire_date;
		this.usage_limit = usage_limit;
		this.request_needed = request_needed;
		this.title = title;
    }

    public static EditExportedChatInvite read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean revoked = (flags & (1 << 2)) != 0;
		TlObject peer = TlObject.read(bytes);
		String link = StringPrimitive.read(bytes).getValue();
		int expire_date = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		int usage_limit = (flags & (1 << 1)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		TlObject request_needed = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		String title = (flags & (1 << 4)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new EditExportedChatInvite(revoked, peer, link, expire_date, usage_limit, request_needed, title);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.revoked ? (1 << 2) : 0;
		flags |= (this.expire_date != 0) ? (1 << 0) : 0;
		flags |= (this.usage_limit != 0) ? (1 << 1) : 0;
		flags |= (this.request_needed != null) ? (1 << 3) : 0;
		flags |= (this.title != null) ? (1 << 4) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(new StringPrimitive(this.link).write());
		if(this.expire_date != 0)
			bOut.write(new IntPrimitive(this.expire_date).write());
		if(this.usage_limit != 0)
			bOut.write(new IntPrimitive(this.usage_limit).write());
		if(this.request_needed != null)
			bOut.write(this.request_needed.write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		
		return bOut.toByteArray();
    }
}
