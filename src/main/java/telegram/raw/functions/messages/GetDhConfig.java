//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetDhConfig extends TlObject {

    public static final int ID = 651135312;

    public int version;
	public int random_length;

    public GetDhConfig(int version, int random_length) {
        this.version = version;
		this.random_length = random_length;
    }

    public static GetDhConfig read(ByteArrayInputStream bytes) throws IOException {
        int version = IntPrimitive.read(bytes).getValue();
		int random_length = IntPrimitive.read(bytes).getValue();
		
		return new GetDhConfig(version, random_length);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.version).write());
		bOut.write(new IntPrimitive(this.random_length).write());
		
		return bOut.toByteArray();
    }
}
