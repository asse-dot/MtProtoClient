//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ToggleBotInAttachMenu extends TlObject {

    public static final int ID = 1777704297;

    public boolean write_allowed;
	public TlObject bot;
	public TlObject enabled;

    public ToggleBotInAttachMenu(boolean write_allowed, TlObject bot, TlObject enabled) {
        this.write_allowed = write_allowed;
		this.bot = bot;
		this.enabled = enabled;
    }

    public static ToggleBotInAttachMenu read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean write_allowed = (flags & (1 << 0)) != 0;
		TlObject bot = TlObject.read(bytes);
		TlObject enabled = TlObject.read(bytes);
		
		return new ToggleBotInAttachMenu(write_allowed, bot, enabled);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.write_allowed ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.bot.write());
		bOut.write(this.enabled.write());
		
		return bOut.toByteArray();
    }
}
