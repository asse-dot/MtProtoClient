//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.messages;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetSearchCounters extends TlObject {

    public static final int ID = 11435201;

    public TlObject peer;
	public int top_msg_id;
	public ArrayList<TlObject> filters;

    public GetSearchCounters(TlObject peer, int top_msg_id, ArrayList<TlObject> filters) {
        this.peer = peer;
		this.top_msg_id = top_msg_id;
		this.filters = filters;
    }

    public static GetSearchCounters read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject peer = TlObject.read(bytes);
		int top_msg_id = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		ArrayList<TlObject> filters = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new GetSearchCounters(peer, top_msg_id, filters);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.top_msg_id != 0) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		if(this.top_msg_id != 0)
			bOut.write(new IntPrimitive(this.top_msg_id).write());
		bOut.write(new VectorPrimitive(this.filters).write());
		
		return bOut.toByteArray();
    }
}
