//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.stickers;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ChangeStickerPosition extends TlObject {

    public static final int ID = -4795190;

    public TlObject sticker;
	public int position;

    public ChangeStickerPosition(TlObject sticker, int position) {
        this.sticker = sticker;
		this.position = position;
    }

    public static ChangeStickerPosition read(ByteArrayInputStream bytes) throws IOException {
        TlObject sticker = TlObject.read(bytes);
		int position = IntPrimitive.read(bytes).getValue();
		
		return new ChangeStickerPosition(sticker, position);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.sticker.write());
		bOut.write(new IntPrimitive(this.position).write());
		
		return bOut.toByteArray();
    }
}
