//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.stickers;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SetStickerSetThumb extends TlObject {

    public static final int ID = -1486204014;

    public TlObject stickerset;
	public TlObject thumb;
	public long thumb_document_id;

    public SetStickerSetThumb(TlObject stickerset, TlObject thumb, long thumb_document_id) {
        this.stickerset = stickerset;
		this.thumb = thumb;
		this.thumb_document_id = thumb_document_id;
    }

    public static SetStickerSetThumb read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject stickerset = TlObject.read(bytes);
		TlObject thumb = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		long thumb_document_id = (flags & (1 << 1)) != 0 ?  LongPrimitive.read(bytes).getValue() : 0;
		
		return new SetStickerSetThumb(stickerset, thumb, thumb_document_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.thumb != null) ? (1 << 0) : 0;
		flags |= (this.thumb_document_id != 0) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.stickerset.write());
		if(this.thumb != null)
			bOut.write(this.thumb.write());
		if(this.thumb_document_id != 0)
			bOut.write(new LongPrimitive(this.thumb_document_id).write());
		
		return bOut.toByteArray();
    }
}
