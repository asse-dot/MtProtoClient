//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetAdminLog extends TlObject {

    public static final int ID = 870184064;

    public TlObject channel;
	public String q;
	public TlObject events_filter;
	public ArrayList<TlObject> admins;
	public long max_id;
	public long min_id;
	public int limit;

    public GetAdminLog(TlObject channel, String q, TlObject events_filter, ArrayList<TlObject> admins, long max_id, long min_id, int limit) {
        this.channel = channel;
		this.q = q;
		this.events_filter = events_filter;
		this.admins = admins;
		this.max_id = max_id;
		this.min_id = min_id;
		this.limit = limit;
    }

    public static GetAdminLog read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject channel = TlObject.read(bytes);
		String q = StringPrimitive.read(bytes).getValue();
		TlObject events_filter = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		ArrayList<TlObject> admins = (flags & (1 << 1)) != 0 ?  ((VectorPrimitive)TlObject.read(bytes)).getValue() : null;
		long max_id = LongPrimitive.read(bytes).getValue();
		long min_id = LongPrimitive.read(bytes).getValue();
		int limit = IntPrimitive.read(bytes).getValue();
		
		return new GetAdminLog(channel, q, events_filter, admins, max_id, min_id, limit);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.events_filter != null) ? (1 << 0) : 0;
		flags |= (this.admins != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.channel.write());
		bOut.write(new StringPrimitive(this.q).write());
		if(this.events_filter != null)
			bOut.write(this.events_filter.write());
		if(this.admins != null)
			bOut.write(new VectorPrimitive(this.admins).write());
		bOut.write(new LongPrimitive(this.max_id).write());
		bOut.write(new LongPrimitive(this.min_id).write());
		bOut.write(new IntPrimitive(this.limit).write());
		
		return bOut.toByteArray();
    }
}
