//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EditForumTopic extends TlObject {

    public static final int ID = -186670715;

    public TlObject channel;
	public int topic_id;
	public String title;
	public long icon_emoji_id;
	public TlObject closed;
	public TlObject hidden;

    public EditForumTopic(TlObject channel, int topic_id, String title, long icon_emoji_id, TlObject closed, TlObject hidden) {
        this.channel = channel;
		this.topic_id = topic_id;
		this.title = title;
		this.icon_emoji_id = icon_emoji_id;
		this.closed = closed;
		this.hidden = hidden;
    }

    public static EditForumTopic read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject channel = TlObject.read(bytes);
		int topic_id = IntPrimitive.read(bytes).getValue();
		String title = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		long icon_emoji_id = (flags & (1 << 1)) != 0 ?  LongPrimitive.read(bytes).getValue() : 0;
		TlObject closed = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		TlObject hidden = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		
		return new EditForumTopic(channel, topic_id, title, icon_emoji_id, closed, hidden);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.title != null) ? (1 << 0) : 0;
		flags |= (this.icon_emoji_id != 0) ? (1 << 1) : 0;
		flags |= (this.closed != null) ? (1 << 2) : 0;
		flags |= (this.hidden != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.channel.write());
		bOut.write(new IntPrimitive(this.topic_id).write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		if(this.icon_emoji_id != 0)
			bOut.write(new LongPrimitive(this.icon_emoji_id).write());
		if(this.closed != null)
			bOut.write(this.closed.write());
		if(this.hidden != null)
			bOut.write(this.hidden.write());
		
		return bOut.toByteArray();
    }
}
