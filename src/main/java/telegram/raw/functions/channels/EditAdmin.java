//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class EditAdmin extends TlObject {

    public static final int ID = -751007486;

    public TlObject channel;
	public TlObject user_id;
	public TlObject admin_rights;
	public String rank;

    public EditAdmin(TlObject channel, TlObject user_id, TlObject admin_rights, String rank) {
        this.channel = channel;
		this.user_id = user_id;
		this.admin_rights = admin_rights;
		this.rank = rank;
    }

    public static EditAdmin read(ByteArrayInputStream bytes) throws IOException {
        TlObject channel = TlObject.read(bytes);
		TlObject user_id = TlObject.read(bytes);
		TlObject admin_rights = TlObject.read(bytes);
		String rank = StringPrimitive.read(bytes).getValue();
		
		return new EditAdmin(channel, user_id, admin_rights, rank);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.channel.write());
		bOut.write(this.user_id.write());
		bOut.write(this.admin_rights.write());
		bOut.write(new StringPrimitive(this.rank).write());
		
		return bOut.toByteArray();
    }
}
