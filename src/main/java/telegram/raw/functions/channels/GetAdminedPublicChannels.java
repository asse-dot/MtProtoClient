//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetAdminedPublicChannels extends TlObject {

    public static final int ID = -122669393;

    public boolean by_location;
	public boolean check_limit;

    public GetAdminedPublicChannels(boolean by_location, boolean check_limit) {
        this.by_location = by_location;
		this.check_limit = check_limit;
    }

    public static GetAdminedPublicChannels read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean by_location = (flags & (1 << 0)) != 0;
		boolean check_limit = (flags & (1 << 1)) != 0;
		
		return new GetAdminedPublicChannels(by_location, check_limit);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.by_location ? (1 << 0) : 0;
		flags |= this.check_limit ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		
		return bOut.toByteArray();
    }
}
