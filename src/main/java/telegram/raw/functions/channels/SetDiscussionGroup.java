//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SetDiscussionGroup extends TlObject {

    public static final int ID = 1079520178;

    public TlObject broadcast;
	public TlObject group;

    public SetDiscussionGroup(TlObject broadcast, TlObject group) {
        this.broadcast = broadcast;
		this.group = group;
    }

    public static SetDiscussionGroup read(ByteArrayInputStream bytes) throws IOException {
        TlObject broadcast = TlObject.read(bytes);
		TlObject group = TlObject.read(bytes);
		
		return new SetDiscussionGroup(broadcast, group);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.broadcast.write());
		bOut.write(this.group.write());
		
		return bOut.toByteArray();
    }
}
