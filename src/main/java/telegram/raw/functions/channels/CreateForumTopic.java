//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class CreateForumTopic extends TlObject {

    public static final int ID = -200539612;

    public TlObject channel;
	public String title;
	public int icon_color;
	public long icon_emoji_id;
	public long random_id;
	public TlObject send_as;

    public CreateForumTopic(TlObject channel, String title, int icon_color, long icon_emoji_id, long random_id, TlObject send_as) {
        this.channel = channel;
		this.title = title;
		this.icon_color = icon_color;
		this.icon_emoji_id = icon_emoji_id;
		this.random_id = random_id;
		this.send_as = send_as;
    }

    public static CreateForumTopic read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject channel = TlObject.read(bytes);
		String title = StringPrimitive.read(bytes).getValue();
		int icon_color = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		long icon_emoji_id = (flags & (1 << 3)) != 0 ?  LongPrimitive.read(bytes).getValue() : 0;
		long random_id = LongPrimitive.read(bytes).getValue();
		TlObject send_as = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new CreateForumTopic(channel, title, icon_color, icon_emoji_id, random_id, send_as);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.icon_color != 0) ? (1 << 0) : 0;
		flags |= (this.icon_emoji_id != 0) ? (1 << 3) : 0;
		flags |= (this.send_as != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.channel.write());
		bOut.write(new StringPrimitive(this.title).write());
		if(this.icon_color != 0)
			bOut.write(new IntPrimitive(this.icon_color).write());
		if(this.icon_emoji_id != 0)
			bOut.write(new LongPrimitive(this.icon_emoji_id).write());
		bOut.write(new LongPrimitive(this.random_id).write());
		if(this.send_as != null)
			bOut.write(this.send_as.write());
		
		return bOut.toByteArray();
    }
}
