//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.channels;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class CreateChannel extends TlObject {

    public static final int ID = -1862244601;

    public boolean broadcast;
	public boolean megagroup;
	public boolean for_import;
	public boolean forum;
	public String title;
	public String about;
	public TlObject geo_point;
	public String address;
	public int ttl_period;

    public CreateChannel(boolean broadcast, boolean megagroup, boolean for_import, boolean forum, String title, String about, TlObject geo_point, String address, int ttl_period) {
        this.broadcast = broadcast;
		this.megagroup = megagroup;
		this.for_import = for_import;
		this.forum = forum;
		this.title = title;
		this.about = about;
		this.geo_point = geo_point;
		this.address = address;
		this.ttl_period = ttl_period;
    }

    public static CreateChannel read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean broadcast = (flags & (1 << 0)) != 0;
		boolean megagroup = (flags & (1 << 1)) != 0;
		boolean for_import = (flags & (1 << 3)) != 0;
		boolean forum = (flags & (1 << 5)) != 0;
		String title = StringPrimitive.read(bytes).getValue();
		String about = StringPrimitive.read(bytes).getValue();
		TlObject geo_point = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		String address = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		int ttl_period = (flags & (1 << 4)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new CreateChannel(broadcast, megagroup, for_import, forum, title, about, geo_point, address, ttl_period);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.broadcast ? (1 << 0) : 0;
		flags |= this.megagroup ? (1 << 1) : 0;
		flags |= this.for_import ? (1 << 3) : 0;
		flags |= this.forum ? (1 << 5) : 0;
		flags |= (this.geo_point != null) ? (1 << 2) : 0;
		flags |= (this.address != null) ? (1 << 2) : 0;
		flags |= (this.ttl_period != 0) ? (1 << 4) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.title).write());
		bOut.write(new StringPrimitive(this.about).write());
		if(this.geo_point != null)
			bOut.write(this.geo_point.write());
		if(this.address != null)
			bOut.write(new StringPrimitive(this.address).write());
		if(this.ttl_period != 0)
			bOut.write(new IntPrimitive(this.ttl_period).write());
		
		return bOut.toByteArray();
    }
}
