//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.bots;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SetBotInfo extends TlObject {

    public static final int ID = 282013987;

    public TlObject bot;
	public String lang_code;
	public String name;
	public String about;
	public String description;

    public SetBotInfo(TlObject bot, String lang_code, String name, String about, String description) {
        this.bot = bot;
		this.lang_code = lang_code;
		this.name = name;
		this.about = about;
		this.description = description;
    }

    public static SetBotInfo read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject bot = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		String lang_code = StringPrimitive.read(bytes).getValue();
		String name = (flags & (1 << 3)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String about = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String description = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new SetBotInfo(bot, lang_code, name, about, description);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.bot != null) ? (1 << 2) : 0;
		flags |= (this.name != null) ? (1 << 3) : 0;
		flags |= (this.about != null) ? (1 << 0) : 0;
		flags |= (this.description != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.bot != null)
			bOut.write(this.bot.write());
		bOut.write(new StringPrimitive(this.lang_code).write());
		if(this.name != null)
			bOut.write(new StringPrimitive(this.name).write());
		if(this.about != null)
			bOut.write(new StringPrimitive(this.about).write());
		if(this.description != null)
			bOut.write(new StringPrimitive(this.description).write());
		
		return bOut.toByteArray();
    }
}
