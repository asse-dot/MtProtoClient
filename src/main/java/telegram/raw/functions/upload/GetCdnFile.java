//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.upload;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetCdnFile extends TlObject {

    public static final int ID = 962554330;

    public byte[] file_token;
	public long offset;
	public int limit;

    public GetCdnFile(byte[] file_token, long offset, int limit) {
        this.file_token = file_token;
		this.offset = offset;
		this.limit = limit;
    }

    public static GetCdnFile read(ByteArrayInputStream bytes) throws IOException {
        byte[] file_token = BytesPrimitive.read(bytes).getValue();
		long offset = LongPrimitive.read(bytes).getValue();
		int limit = IntPrimitive.read(bytes).getValue();
		
		return new GetCdnFile(file_token, offset, limit);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new BytesPrimitive(this.file_token).write());
		bOut.write(new LongPrimitive(this.offset).write());
		bOut.write(new IntPrimitive(this.limit).write());
		
		return bOut.toByteArray();
    }
}
