//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class DiscardCall extends TlObject {

    public static final int ID = -1295269440;

    public boolean video;
	public TlObject peer;
	public int duration;
	public TlObject reason;
	public long connection_id;

    public DiscardCall(boolean video, TlObject peer, int duration, TlObject reason, long connection_id) {
        this.video = video;
		this.peer = peer;
		this.duration = duration;
		this.reason = reason;
		this.connection_id = connection_id;
    }

    public static DiscardCall read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean video = (flags & (1 << 0)) != 0;
		TlObject peer = TlObject.read(bytes);
		int duration = IntPrimitive.read(bytes).getValue();
		TlObject reason = TlObject.read(bytes);
		long connection_id = LongPrimitive.read(bytes).getValue();
		
		return new DiscardCall(video, peer, duration, reason, connection_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.video ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.duration).write());
		bOut.write(this.reason.write());
		bOut.write(new LongPrimitive(this.connection_id).write());
		
		return bOut.toByteArray();
    }
}
