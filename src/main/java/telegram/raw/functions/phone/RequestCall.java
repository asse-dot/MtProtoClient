//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RequestCall extends TlObject {

    public static final int ID = 1124046573;

    public boolean video;
	public TlObject user_id;
	public int random_id;
	public byte[] g_a_hash;
	public TlObject protocol;

    public RequestCall(boolean video, TlObject user_id, int random_id, byte[] g_a_hash, TlObject protocol) {
        this.video = video;
		this.user_id = user_id;
		this.random_id = random_id;
		this.g_a_hash = g_a_hash;
		this.protocol = protocol;
    }

    public static RequestCall read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean video = (flags & (1 << 0)) != 0;
		TlObject user_id = TlObject.read(bytes);
		int random_id = IntPrimitive.read(bytes).getValue();
		byte[] g_a_hash = BytesPrimitive.read(bytes).getValue();
		TlObject protocol = TlObject.read(bytes);
		
		return new RequestCall(video, user_id, random_id, g_a_hash, protocol);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.video ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.user_id.write());
		bOut.write(new IntPrimitive(this.random_id).write());
		bOut.write(new BytesPrimitive(this.g_a_hash).write());
		bOut.write(this.protocol.write());
		
		return bOut.toByteArray();
    }
}
