//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ToggleGroupCallRecord extends TlObject {

    public static final int ID = -248985848;

    public boolean start;
	public boolean video;
	public TlObject call;
	public String title;
	public TlObject video_portrait;

    public ToggleGroupCallRecord(boolean start, boolean video, TlObject call, String title, TlObject video_portrait) {
        this.start = start;
		this.video = video;
		this.call = call;
		this.title = title;
		this.video_portrait = video_portrait;
    }

    public static ToggleGroupCallRecord read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean start = (flags & (1 << 0)) != 0;
		boolean video = (flags & (1 << 2)) != 0;
		TlObject call = TlObject.read(bytes);
		String title = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject video_portrait = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		
		return new ToggleGroupCallRecord(start, video, call, title, video_portrait);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.start ? (1 << 0) : 0;
		flags |= this.video ? (1 << 2) : 0;
		flags |= (this.title != null) ? (1 << 1) : 0;
		flags |= (this.video_portrait != null) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.call.write());
		if(this.title != null)
			bOut.write(new StringPrimitive(this.title).write());
		if(this.video_portrait != null)
			bOut.write(this.video_portrait.write());
		
		return bOut.toByteArray();
    }
}
