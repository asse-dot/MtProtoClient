//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetGroupParticipants extends TlObject {

    public static final int ID = -984033109;

    public TlObject call;
	public ArrayList<TlObject> ids;
	public ArrayList<TlObject> sources;
	public String offset;
	public int limit;

    public GetGroupParticipants(TlObject call, ArrayList<TlObject> ids, ArrayList<TlObject> sources, String offset, int limit) {
        this.call = call;
		this.ids = ids;
		this.sources = sources;
		this.offset = offset;
		this.limit = limit;
    }

    public static GetGroupParticipants read(ByteArrayInputStream bytes) throws IOException {
        TlObject call = TlObject.read(bytes);
		ArrayList<TlObject> ids = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		ArrayList<TlObject> sources = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		String offset = StringPrimitive.read(bytes).getValue();
		int limit = IntPrimitive.read(bytes).getValue();
		
		return new GetGroupParticipants(call, ids, sources, offset, limit);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.call.write());
		bOut.write(new VectorPrimitive(this.ids).write());
		bOut.write(new VectorPrimitive(this.sources).write());
		bOut.write(new StringPrimitive(this.offset).write());
		bOut.write(new IntPrimitive(this.limit).write());
		
		return bOut.toByteArray();
    }
}
