//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SetCallRating extends TlObject {

    public static final int ID = 1508562471;

    public boolean user_initiative;
	public TlObject peer;
	public int rating;
	public String comment;

    public SetCallRating(boolean user_initiative, TlObject peer, int rating, String comment) {
        this.user_initiative = user_initiative;
		this.peer = peer;
		this.rating = rating;
		this.comment = comment;
    }

    public static SetCallRating read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean user_initiative = (flags & (1 << 0)) != 0;
		TlObject peer = TlObject.read(bytes);
		int rating = IntPrimitive.read(bytes).getValue();
		String comment = StringPrimitive.read(bytes).getValue();
		
		return new SetCallRating(user_initiative, peer, rating, comment);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.user_initiative ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.peer.write());
		bOut.write(new IntPrimitive(this.rating).write());
		bOut.write(new StringPrimitive(this.comment).write());
		
		return bOut.toByteArray();
    }
}
