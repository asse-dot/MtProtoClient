//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.phone;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ToggleGroupCallSettings extends TlObject {

    public static final int ID = 1958458429;

    public boolean reset_invite_hash;
	public TlObject call;
	public TlObject join_muted;

    public ToggleGroupCallSettings(boolean reset_invite_hash, TlObject call, TlObject join_muted) {
        this.reset_invite_hash = reset_invite_hash;
		this.call = call;
		this.join_muted = join_muted;
    }

    public static ToggleGroupCallSettings read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean reset_invite_hash = (flags & (1 << 1)) != 0;
		TlObject call = TlObject.read(bytes);
		TlObject join_muted = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new ToggleGroupCallSettings(reset_invite_hash, call, join_muted);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.reset_invite_hash ? (1 << 1) : 0;
		flags |= (this.join_muted != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.call.write());
		if(this.join_muted != null)
			bOut.write(this.join_muted.write());
		
		return bOut.toByteArray();
    }
}
