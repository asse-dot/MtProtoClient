//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class AcceptAuthorization extends TlObject {

    public static final int ID = -202552205;

    public long bot_id;
	public String scope;
	public String public_key;
	public ArrayList<TlObject> value_hashes;
	public TlObject credentials;

    public AcceptAuthorization(long bot_id, String scope, String public_key, ArrayList<TlObject> value_hashes, TlObject credentials) {
        this.bot_id = bot_id;
		this.scope = scope;
		this.public_key = public_key;
		this.value_hashes = value_hashes;
		this.credentials = credentials;
    }

    public static AcceptAuthorization read(ByteArrayInputStream bytes) throws IOException {
        long bot_id = LongPrimitive.read(bytes).getValue();
		String scope = StringPrimitive.read(bytes).getValue();
		String public_key = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> value_hashes = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		TlObject credentials = TlObject.read(bytes);
		
		return new AcceptAuthorization(bot_id, scope, public_key, value_hashes, credentials);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.bot_id).write());
		bOut.write(new StringPrimitive(this.scope).write());
		bOut.write(new StringPrimitive(this.public_key).write());
		bOut.write(new VectorPrimitive(this.value_hashes).write());
		bOut.write(this.credentials.write());
		
		return bOut.toByteArray();
    }
}
