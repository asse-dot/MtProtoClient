//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InstallTheme extends TlObject {

    public static final int ID = -953697477;

    public boolean dark;
	public TlObject theme;
	public String format;
	public TlObject base_theme;

    public InstallTheme(boolean dark, TlObject theme, String format, TlObject base_theme) {
        this.dark = dark;
		this.theme = theme;
		this.format = format;
		this.base_theme = base_theme;
    }

    public static InstallTheme read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean dark = (flags & (1 << 0)) != 0;
		TlObject theme = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		String format = (flags & (1 << 2)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject base_theme = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		
		return new InstallTheme(dark, theme, format, base_theme);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.dark ? (1 << 0) : 0;
		flags |= (this.theme != null) ? (1 << 1) : 0;
		flags |= (this.format != null) ? (1 << 2) : 0;
		flags |= (this.base_theme != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.theme != null)
			bOut.write(this.theme.write());
		if(this.format != null)
			bOut.write(new StringPrimitive(this.format).write());
		if(this.base_theme != null)
			bOut.write(this.base_theme.write());
		
		return bOut.toByteArray();
    }
}
