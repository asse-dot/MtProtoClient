//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UploadWallPaper extends TlObject {

    public static final int ID = -476410109;

    public boolean for_chat;
	public TlObject file;
	public String mime_type;
	public TlObject settings;

    public UploadWallPaper(boolean for_chat, TlObject file, String mime_type, TlObject settings) {
        this.for_chat = for_chat;
		this.file = file;
		this.mime_type = mime_type;
		this.settings = settings;
    }

    public static UploadWallPaper read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean for_chat = (flags & (1 << 0)) != 0;
		TlObject file = TlObject.read(bytes);
		String mime_type = StringPrimitive.read(bytes).getValue();
		TlObject settings = TlObject.read(bytes);
		
		return new UploadWallPaper(for_chat, file, mime_type, settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.for_chat ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.file.write());
		bOut.write(new StringPrimitive(this.mime_type).write());
		bOut.write(this.settings.write());
		
		return bOut.toByteArray();
    }
}
