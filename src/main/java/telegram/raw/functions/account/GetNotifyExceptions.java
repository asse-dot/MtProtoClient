//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetNotifyExceptions extends TlObject {

    public static final int ID = 1398240377;

    public boolean compare_sound;
	public TlObject peer;

    public GetNotifyExceptions(boolean compare_sound, TlObject peer) {
        this.compare_sound = compare_sound;
		this.peer = peer;
    }

    public static GetNotifyExceptions read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean compare_sound = (flags & (1 << 1)) != 0;
		TlObject peer = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new GetNotifyExceptions(compare_sound, peer);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.compare_sound ? (1 << 1) : 0;
		flags |= (this.peer != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.peer != null)
			bOut.write(this.peer.write());
		
		return bOut.toByteArray();
    }
}
