//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InitTakeoutSession extends TlObject {

    public static final int ID = -1896617296;

    public boolean contacts;
	public boolean message_users;
	public boolean message_chats;
	public boolean message_megagroups;
	public boolean message_channels;
	public boolean files;
	public long file_max_size;

    public InitTakeoutSession(boolean contacts, boolean message_users, boolean message_chats, boolean message_megagroups, boolean message_channels, boolean files, long file_max_size) {
        this.contacts = contacts;
		this.message_users = message_users;
		this.message_chats = message_chats;
		this.message_megagroups = message_megagroups;
		this.message_channels = message_channels;
		this.files = files;
		this.file_max_size = file_max_size;
    }

    public static InitTakeoutSession read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean contacts = (flags & (1 << 0)) != 0;
		boolean message_users = (flags & (1 << 1)) != 0;
		boolean message_chats = (flags & (1 << 2)) != 0;
		boolean message_megagroups = (flags & (1 << 3)) != 0;
		boolean message_channels = (flags & (1 << 4)) != 0;
		boolean files = (flags & (1 << 5)) != 0;
		long file_max_size = (flags & (1 << 5)) != 0 ?  LongPrimitive.read(bytes).getValue() : 0;
		
		return new InitTakeoutSession(contacts, message_users, message_chats, message_megagroups, message_channels, files, file_max_size);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.contacts ? (1 << 0) : 0;
		flags |= this.message_users ? (1 << 1) : 0;
		flags |= this.message_chats ? (1 << 2) : 0;
		flags |= this.message_megagroups ? (1 << 3) : 0;
		flags |= this.message_channels ? (1 << 4) : 0;
		flags |= this.files ? (1 << 5) : 0;
		flags |= (this.file_max_size != 0) ? (1 << 5) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.file_max_size != 0)
			bOut.write(new LongPrimitive(this.file_max_size).write());
		
		return bOut.toByteArray();
    }
}
