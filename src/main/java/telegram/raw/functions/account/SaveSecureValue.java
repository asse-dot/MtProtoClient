//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SaveSecureValue extends TlObject {

    public static final int ID = -1986010339;

    public TlObject value;
	public long secure_secret_id;

    public SaveSecureValue(TlObject value, long secure_secret_id) {
        this.value = value;
		this.secure_secret_id = secure_secret_id;
    }

    public static SaveSecureValue read(ByteArrayInputStream bytes) throws IOException {
        TlObject value = TlObject.read(bytes);
		long secure_secret_id = LongPrimitive.read(bytes).getValue();
		
		return new SaveSecureValue(value, secure_secret_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.value.write());
		bOut.write(new LongPrimitive(this.secure_secret_id).write());
		
		return bOut.toByteArray();
    }
}
