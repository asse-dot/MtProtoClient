//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UploadTheme extends TlObject {

    public static final int ID = 473805619;

    public TlObject file;
	public TlObject thumb;
	public String file_name;
	public String mime_type;

    public UploadTheme(TlObject file, TlObject thumb, String file_name, String mime_type) {
        this.file = file;
		this.thumb = thumb;
		this.file_name = file_name;
		this.mime_type = mime_type;
    }

    public static UploadTheme read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject file = TlObject.read(bytes);
		TlObject thumb = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		String file_name = StringPrimitive.read(bytes).getValue();
		String mime_type = StringPrimitive.read(bytes).getValue();
		
		return new UploadTheme(file, thumb, file_name, mime_type);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.thumb != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.file.write());
		if(this.thumb != null)
			bOut.write(this.thumb.write());
		bOut.write(new StringPrimitive(this.file_name).write());
		bOut.write(new StringPrimitive(this.mime_type).write());
		
		return bOut.toByteArray();
    }
}
