//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SaveAutoSaveSettings extends TlObject {

    public static final int ID = -694451359;

    public boolean users;
	public boolean chats;
	public boolean broadcasts;
	public TlObject peer;
	public TlObject settings;

    public SaveAutoSaveSettings(boolean users, boolean chats, boolean broadcasts, TlObject peer, TlObject settings) {
        this.users = users;
		this.chats = chats;
		this.broadcasts = broadcasts;
		this.peer = peer;
		this.settings = settings;
    }

    public static SaveAutoSaveSettings read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean users = (flags & (1 << 0)) != 0;
		boolean chats = (flags & (1 << 1)) != 0;
		boolean broadcasts = (flags & (1 << 2)) != 0;
		TlObject peer = (flags & (1 << 3)) != 0 ?  TlObject.read(bytes) : null;
		TlObject settings = TlObject.read(bytes);
		
		return new SaveAutoSaveSettings(users, chats, broadcasts, peer, settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.users ? (1 << 0) : 0;
		flags |= this.chats ? (1 << 1) : 0;
		flags |= this.broadcasts ? (1 << 2) : 0;
		flags |= (this.peer != null) ? (1 << 3) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.peer != null)
			bOut.write(this.peer.write());
		bOut.write(this.settings.write());
		
		return bOut.toByteArray();
    }
}
