//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdatePasswordSettings extends TlObject {

    public static final int ID = -1516564433;

    public TlObject password;
	public TlObject new_settings;

    public UpdatePasswordSettings(TlObject password, TlObject new_settings) {
        this.password = password;
		this.new_settings = new_settings;
    }

    public static UpdatePasswordSettings read(ByteArrayInputStream bytes) throws IOException {
        TlObject password = TlObject.read(bytes);
		TlObject new_settings = TlObject.read(bytes);
		
		return new UpdatePasswordSettings(password, new_settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(this.password.write());
		bOut.write(this.new_settings.write());
		
		return bOut.toByteArray();
    }
}
