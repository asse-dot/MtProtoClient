//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.account;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UnregisterDevice extends TlObject {

    public static final int ID = 1779249670;

    public int token_type;
	public String token;
	public ArrayList<TlObject> other_uids;

    public UnregisterDevice(int token_type, String token, ArrayList<TlObject> other_uids) {
        this.token_type = token_type;
		this.token = token;
		this.other_uids = other_uids;
    }

    public static UnregisterDevice read(ByteArrayInputStream bytes) throws IOException {
        int token_type = IntPrimitive.read(bytes).getValue();
		String token = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> other_uids = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new UnregisterDevice(token_type, token, other_uids);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.token_type).write());
		bOut.write(new StringPrimitive(this.token).write());
		bOut.write(new VectorPrimitive(this.other_uids).write());
		
		return bOut.toByteArray();
    }
}
