//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.auth;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RecoverPassword extends TlObject {

    public static final int ID = 923364464;

    public String code;
	public TlObject new_settings;

    public RecoverPassword(String code, TlObject new_settings) {
        this.code = code;
		this.new_settings = new_settings;
    }

    public static RecoverPassword read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String code = StringPrimitive.read(bytes).getValue();
		TlObject new_settings = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new RecoverPassword(code, new_settings);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.new_settings != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.code).write());
		if(this.new_settings != null)
			bOut.write(this.new_settings.write());
		
		return bOut.toByteArray();
    }
}
