//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.auth;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class BindTempAuthKey extends TlObject {

    public static final int ID = -841733627;

    public long perm_auth_key_id;
	public long nonce;
	public int expires_at;
	public byte[] encrypted_message;

    public BindTempAuthKey(long perm_auth_key_id, long nonce, int expires_at, byte[] encrypted_message) {
        this.perm_auth_key_id = perm_auth_key_id;
		this.nonce = nonce;
		this.expires_at = expires_at;
		this.encrypted_message = encrypted_message;
    }

    public static BindTempAuthKey read(ByteArrayInputStream bytes) throws IOException {
        long perm_auth_key_id = LongPrimitive.read(bytes).getValue();
		long nonce = LongPrimitive.read(bytes).getValue();
		int expires_at = IntPrimitive.read(bytes).getValue();
		byte[] encrypted_message = BytesPrimitive.read(bytes).getValue();
		
		return new BindTempAuthKey(perm_auth_key_id, nonce, expires_at, encrypted_message);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.perm_auth_key_id).write());
		bOut.write(new LongPrimitive(this.nonce).write());
		bOut.write(new IntPrimitive(this.expires_at).write());
		bOut.write(new BytesPrimitive(this.encrypted_message).write());
		
		return bOut.toByteArray();
    }
}
