//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.auth;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class ExportLoginToken extends TlObject {

    public static final int ID = -1210022402;

    public int api_id;
	public String api_hash;
	public ArrayList<TlObject> except_ids;

    public ExportLoginToken(int api_id, String api_hash, ArrayList<TlObject> except_ids) {
        this.api_id = api_id;
		this.api_hash = api_hash;
		this.except_ids = except_ids;
    }

    public static ExportLoginToken read(ByteArrayInputStream bytes) throws IOException {
        int api_id = IntPrimitive.read(bytes).getValue();
		String api_hash = StringPrimitive.read(bytes).getValue();
		ArrayList<TlObject> except_ids = ((VectorPrimitive)TlObject.read(bytes)).getValue();
		
		return new ExportLoginToken(api_id, api_hash, except_ids);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new IntPrimitive(this.api_id).write());
		bOut.write(new StringPrimitive(this.api_hash).write());
		bOut.write(new VectorPrimitive(this.except_ids).write());
		
		return bOut.toByteArray();
    }
}
