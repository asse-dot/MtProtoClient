//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.auth;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SignIn extends TlObject {

    public static final int ID = -1923962543;

    public String phone_number;
	public String phone_code_hash;
	public String phone_code;
	public TlObject email_verification;

    public SignIn(String phone_number, String phone_code_hash, String phone_code, TlObject email_verification) {
        this.phone_number = phone_number;
		this.phone_code_hash = phone_code_hash;
		this.phone_code = phone_code;
		this.email_verification = email_verification;
    }

    public static SignIn read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String phone_number = StringPrimitive.read(bytes).getValue();
		String phone_code_hash = StringPrimitive.read(bytes).getValue();
		String phone_code = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject email_verification = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		
		return new SignIn(phone_number, phone_code_hash, phone_code, email_verification);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.phone_code != null) ? (1 << 0) : 0;
		flags |= (this.email_verification != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.phone_number).write());
		bOut.write(new StringPrimitive(this.phone_code_hash).write());
		if(this.phone_code != null)
			bOut.write(new StringPrimitive(this.phone_code).write());
		if(this.email_verification != null)
			bOut.write(this.email_verification.write());
		
		return bOut.toByteArray();
    }
}
