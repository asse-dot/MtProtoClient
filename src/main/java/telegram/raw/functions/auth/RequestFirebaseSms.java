//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.auth;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class RequestFirebaseSms extends TlObject {

    public static final int ID = -1991881904;

    public String phone_number;
	public String phone_code_hash;
	public String safety_net_token;
	public String ios_push_secret;

    public RequestFirebaseSms(String phone_number, String phone_code_hash, String safety_net_token, String ios_push_secret) {
        this.phone_number = phone_number;
		this.phone_code_hash = phone_code_hash;
		this.safety_net_token = safety_net_token;
		this.ios_push_secret = ios_push_secret;
    }

    public static RequestFirebaseSms read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		String phone_number = StringPrimitive.read(bytes).getValue();
		String phone_code_hash = StringPrimitive.read(bytes).getValue();
		String safety_net_token = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String ios_push_secret = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		
		return new RequestFirebaseSms(phone_number, phone_code_hash, safety_net_token, ios_push_secret);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.safety_net_token != null) ? (1 << 0) : 0;
		flags |= (this.ios_push_secret != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new StringPrimitive(this.phone_number).write());
		bOut.write(new StringPrimitive(this.phone_code_hash).write());
		if(this.safety_net_token != null)
			bOut.write(new StringPrimitive(this.safety_net_token).write());
		if(this.ios_push_secret != null)
			bOut.write(new StringPrimitive(this.ios_push_secret).write());
		
		return bOut.toByteArray();
    }
}
