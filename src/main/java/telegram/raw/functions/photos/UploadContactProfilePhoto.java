//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.photos;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UploadContactProfilePhoto extends TlObject {

    public static final int ID = -515093903;

    public boolean suggest;
	public boolean save;
	public TlObject user_id;
	public TlObject file;
	public TlObject video;
	public TlObject video_start_ts;
	public TlObject video_emoji_markup;

    public UploadContactProfilePhoto(boolean suggest, boolean save, TlObject user_id, TlObject file, TlObject video, TlObject video_start_ts, TlObject video_emoji_markup) {
        this.suggest = suggest;
		this.save = save;
		this.user_id = user_id;
		this.file = file;
		this.video = video;
		this.video_start_ts = video_start_ts;
		this.video_emoji_markup = video_emoji_markup;
    }

    public static UploadContactProfilePhoto read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean suggest = (flags & (1 << 3)) != 0;
		boolean save = (flags & (1 << 4)) != 0;
		TlObject user_id = TlObject.read(bytes);
		TlObject file = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		TlObject video = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		TlObject video_start_ts = (flags & (1 << 2)) != 0 ?  TlObject.read(bytes) : null;
		TlObject video_emoji_markup = (flags & (1 << 5)) != 0 ?  TlObject.read(bytes) : null;
		
		return new UploadContactProfilePhoto(suggest, save, user_id, file, video, video_start_ts, video_emoji_markup);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.suggest ? (1 << 3) : 0;
		flags |= this.save ? (1 << 4) : 0;
		flags |= (this.file != null) ? (1 << 0) : 0;
		flags |= (this.video != null) ? (1 << 1) : 0;
		flags |= (this.video_start_ts != null) ? (1 << 2) : 0;
		flags |= (this.video_emoji_markup != null) ? (1 << 5) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.user_id.write());
		if(this.file != null)
			bOut.write(this.file.write());
		if(this.video != null)
			bOut.write(this.video.write());
		if(this.video_start_ts != null)
			bOut.write(this.video_start_ts.write());
		if(this.video_emoji_markup != null)
			bOut.write(this.video_emoji_markup.write());
		
		return bOut.toByteArray();
    }
}
