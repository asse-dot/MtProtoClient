//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.photos;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class UpdateProfilePhoto extends TlObject {

    public static final int ID = 166207545;

    public boolean fallback;
	public TlObject bot;
	public TlObject id;

    public UpdateProfilePhoto(boolean fallback, TlObject bot, TlObject id) {
        this.fallback = fallback;
		this.bot = bot;
		this.id = id;
    }

    public static UpdateProfilePhoto read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean fallback = (flags & (1 << 0)) != 0;
		TlObject bot = (flags & (1 << 1)) != 0 ?  TlObject.read(bytes) : null;
		TlObject id = TlObject.read(bytes);
		
		return new UpdateProfilePhoto(fallback, bot, id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.fallback ? (1 << 0) : 0;
		flags |= (this.bot != null) ? (1 << 1) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		if(this.bot != null)
			bOut.write(this.bot.write());
		bOut.write(this.id.write());
		
		return bOut.toByteArray();
    }
}
