//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.payments;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class SendPaymentForm extends TlObject {

    public static final int ID = 755192367;

    public long form_id;
	public TlObject invoice;
	public String requested_info_id;
	public String shipping_option_id;
	public TlObject credentials;
	public long tip_amount;

    public SendPaymentForm(long form_id, TlObject invoice, String requested_info_id, String shipping_option_id, TlObject credentials, long tip_amount) {
        this.form_id = form_id;
		this.invoice = invoice;
		this.requested_info_id = requested_info_id;
		this.shipping_option_id = shipping_option_id;
		this.credentials = credentials;
		this.tip_amount = tip_amount;
    }

    public static SendPaymentForm read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		long form_id = LongPrimitive.read(bytes).getValue();
		TlObject invoice = TlObject.read(bytes);
		String requested_info_id = (flags & (1 << 0)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		String shipping_option_id = (flags & (1 << 1)) != 0 ?  StringPrimitive.read(bytes).getValue() : null;
		TlObject credentials = TlObject.read(bytes);
		long tip_amount = (flags & (1 << 2)) != 0 ?  LongPrimitive.read(bytes).getValue() : 0;
		
		return new SendPaymentForm(form_id, invoice, requested_info_id, shipping_option_id, credentials, tip_amount);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.requested_info_id != null) ? (1 << 0) : 0;
		flags |= (this.shipping_option_id != null) ? (1 << 1) : 0;
		flags |= (this.tip_amount != 0) ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new LongPrimitive(this.form_id).write());
		bOut.write(this.invoice.write());
		if(this.requested_info_id != null)
			bOut.write(new StringPrimitive(this.requested_info_id).write());
		if(this.shipping_option_id != null)
			bOut.write(new StringPrimitive(this.shipping_option_id).write());
		bOut.write(this.credentials.write());
		if(this.tip_amount != 0)
			bOut.write(new LongPrimitive(this.tip_amount).write());
		
		return bOut.toByteArray();
    }
}
