//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.payments;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetPaymentForm extends TlObject {

    public static final int ID = 924093883;

    public TlObject invoice;
	public TlObject theme_params;

    public GetPaymentForm(TlObject invoice, TlObject theme_params) {
        this.invoice = invoice;
		this.theme_params = theme_params;
    }

    public static GetPaymentForm read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		TlObject invoice = TlObject.read(bytes);
		TlObject theme_params = (flags & (1 << 0)) != 0 ?  TlObject.read(bytes) : null;
		
		return new GetPaymentForm(invoice, theme_params);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= (this.theme_params != null) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.invoice.write());
		if(this.theme_params != null)
			bOut.write(this.theme_params.write());
		
		return bOut.toByteArray();
    }
}
