//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class InvokeAfterMsg extends TlObject {

    public static final int ID = -878758099;

    public long msg_id;
	public TlObject query;

    public InvokeAfterMsg(long msg_id, TlObject query) {
        this.msg_id = msg_id;
		this.query = query;
    }

    public static InvokeAfterMsg read(ByteArrayInputStream bytes) throws IOException {
        long msg_id = LongPrimitive.read(bytes).getValue();
		TlObject query = TlObject.read(bytes);
		
		return new InvokeAfterMsg(msg_id, query);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		
		
		bOut.write(new LongPrimitive(this.msg_id).write());
		bOut.write(this.query.write());
		
		return bOut.toByteArray();
    }
}
