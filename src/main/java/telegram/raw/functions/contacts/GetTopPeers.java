//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.contacts;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetTopPeers extends TlObject {

    public static final int ID = -1758168906;

    public boolean correspondents;
	public boolean bots_pm;
	public boolean bots_inline;
	public boolean phone_calls;
	public boolean forward_users;
	public boolean forward_chats;
	public boolean groups;
	public boolean channels;
	public int offset;
	public int limit;
	public long hash;

    public GetTopPeers(boolean correspondents, boolean bots_pm, boolean bots_inline, boolean phone_calls, boolean forward_users, boolean forward_chats, boolean groups, boolean channels, int offset, int limit, long hash) {
        this.correspondents = correspondents;
		this.bots_pm = bots_pm;
		this.bots_inline = bots_inline;
		this.phone_calls = phone_calls;
		this.forward_users = forward_users;
		this.forward_chats = forward_chats;
		this.groups = groups;
		this.channels = channels;
		this.offset = offset;
		this.limit = limit;
		this.hash = hash;
    }

    public static GetTopPeers read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean correspondents = (flags & (1 << 0)) != 0;
		boolean bots_pm = (flags & (1 << 1)) != 0;
		boolean bots_inline = (flags & (1 << 2)) != 0;
		boolean phone_calls = (flags & (1 << 3)) != 0;
		boolean forward_users = (flags & (1 << 4)) != 0;
		boolean forward_chats = (flags & (1 << 5)) != 0;
		boolean groups = (flags & (1 << 10)) != 0;
		boolean channels = (flags & (1 << 15)) != 0;
		int offset = IntPrimitive.read(bytes).getValue();
		int limit = IntPrimitive.read(bytes).getValue();
		long hash = LongPrimitive.read(bytes).getValue();
		
		return new GetTopPeers(correspondents, bots_pm, bots_inline, phone_calls, forward_users, forward_chats, groups, channels, offset, limit, hash);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.correspondents ? (1 << 0) : 0;
		flags |= this.bots_pm ? (1 << 1) : 0;
		flags |= this.bots_inline ? (1 << 2) : 0;
		flags |= this.phone_calls ? (1 << 3) : 0;
		flags |= this.forward_users ? (1 << 4) : 0;
		flags |= this.forward_chats ? (1 << 5) : 0;
		flags |= this.groups ? (1 << 10) : 0;
		flags |= this.channels ? (1 << 15) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.offset).write());
		bOut.write(new IntPrimitive(this.limit).write());
		bOut.write(new LongPrimitive(this.hash).write());
		
		return bOut.toByteArray();
    }
}
