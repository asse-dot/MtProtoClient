//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.contacts;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class GetLocated extends TlObject {

    public static final int ID = -750207932;

    public boolean background;
	public TlObject geo_point;
	public int self_expires;

    public GetLocated(boolean background, TlObject geo_point, int self_expires) {
        this.background = background;
		this.geo_point = geo_point;
		this.self_expires = self_expires;
    }

    public static GetLocated read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean background = (flags & (1 << 1)) != 0;
		TlObject geo_point = TlObject.read(bytes);
		int self_expires = (flags & (1 << 0)) != 0 ?  IntPrimitive.read(bytes).getValue() : 0;
		
		return new GetLocated(background, geo_point, self_expires);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.background ? (1 << 1) : 0;
		flags |= (this.self_expires != 0) ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.geo_point.write());
		if(this.self_expires != 0)
			bOut.write(new IntPrimitive(this.self_expires).write());
		
		return bOut.toByteArray();
    }
}
