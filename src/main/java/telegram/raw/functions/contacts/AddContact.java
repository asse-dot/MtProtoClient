//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.contacts;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class AddContact extends TlObject {

    public static final int ID = -386636848;

    public boolean add_phone_privacy_exception;
	public TlObject id;
	public String first_name;
	public String last_name;
	public String phone;

    public AddContact(boolean add_phone_privacy_exception, TlObject id, String first_name, String last_name, String phone) {
        this.add_phone_privacy_exception = add_phone_privacy_exception;
		this.id = id;
		this.first_name = first_name;
		this.last_name = last_name;
		this.phone = phone;
    }

    public static AddContact read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean add_phone_privacy_exception = (flags & (1 << 0)) != 0;
		TlObject id = TlObject.read(bytes);
		String first_name = StringPrimitive.read(bytes).getValue();
		String last_name = StringPrimitive.read(bytes).getValue();
		String phone = StringPrimitive.read(bytes).getValue();
		
		return new AddContact(add_phone_privacy_exception, id, first_name, last_name, phone);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.add_phone_privacy_exception ? (1 << 0) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(this.id.write());
		bOut.write(new StringPrimitive(this.first_name).write());
		bOut.write(new StringPrimitive(this.last_name).write());
		bOut.write(new StringPrimitive(this.phone).write());
		
		return bOut.toByteArray();
    }
}
