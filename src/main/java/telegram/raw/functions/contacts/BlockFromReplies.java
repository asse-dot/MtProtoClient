//this is an autogenerated file!!
//!!!!!!!!!!
package telegram.raw.functions.contacts;

import telegram.raw.core.TlObject;
import telegram.raw.core.primitives.BytesPrimitive;
import telegram.raw.core.primitives.StringPrimitive;
import telegram.raw.core.primitives.VectorPrimitive;
import telegram.raw.core.primitives.integers.IntPrimitive;
import telegram.raw.core.primitives.integers.LongPrimitive;

import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.IOException;
import java.util.ArrayList;

public class BlockFromReplies extends TlObject {

    public static final int ID = 698914348;

    public boolean delete_message;
	public boolean delete_history;
	public boolean report_spam;
	public int msg_id;

    public BlockFromReplies(boolean delete_message, boolean delete_history, boolean report_spam, int msg_id) {
        this.delete_message = delete_message;
		this.delete_history = delete_history;
		this.report_spam = report_spam;
		this.msg_id = msg_id;
    }

    public static BlockFromReplies read(ByteArrayInputStream bytes) throws IOException {
        int flags = IntPrimitive.read(bytes).getValue();
		boolean delete_message = (flags & (1 << 0)) != 0;
		boolean delete_history = (flags & (1 << 1)) != 0;
		boolean report_spam = (flags & (1 << 2)) != 0;
		int msg_id = IntPrimitive.read(bytes).getValue();
		
		return new BlockFromReplies(delete_message, delete_history, report_spam, msg_id);
    }

    @Override
    public byte[] write() throws IOException {
        ByteArrayOutputStream bOut = new ByteArrayOutputStream();
		
		bOut.write(new IntPrimitive(ID).write());
		int flags = 0;
		flags |= this.delete_message ? (1 << 0) : 0;
		flags |= this.delete_history ? (1 << 1) : 0;
		flags |= this.report_spam ? (1 << 2) : 0;
		
		
		bOut.write(new IntPrimitive(flags).write());
		bOut.write(new IntPrimitive(this.msg_id).write());
		
		return bOut.toByteArray();
    }
}
